\section{模型分析检验}

\subsection{检验证据的分类}

不同检验支撑的结论强度不同。表\ref{tab:evidence}按证据类型汇总各问的检验，同一问中的
内插、观测验证、半合成校准与外推分别表述，不互相替代。

\begin{table}[htbp]
  \centering
  \caption{各问检验按证据类型的归类}
  \label{tab:evidence}
  \begin{tabular}{l*{4}{>{\raggedright\arraybackslash}p{3.2cm}}}
    \toprule
    问题 & 内插或训练内 & 观测验证 & 半合成校准 & 外推 \\
    \midrule
    问题一 & A4/A5 交叉验证（仅用于模型选择） & A6/A7 同规模留出集：通过；A8/A9：水平不迁移、形状迁移；A10/A11：未通过 & -- & A13/A15 为估算参照，仅作压力诊断 \\
    问题二 & B1 拟合（生成公式恢复，不作验证） & B5：7.52\%；B4 剔除同族行：8.07\% & B2：32.15\%；B7 候选形式的内部检验 & 有效域以外，含 B9/B10 所涉规模 \\
    问题四 & 历史前沿拟合 & 6 个月滚动回测；映射的逐一留出检验 & -- & 24 个月压力预测；A 组预测 \\
    \bottomrule
  \end{tabular}
\end{table}
% STAGE B: add the Question 3 row after T-010 is accepted.

\subsection{灵敏度分析}

\textbf{问题一。}11 种预先声明的方法变体下，域级质量评分的最高与最低领域不变，中间领域的
排序随方法变化；缺失记录的三种极端取值使评分变化不超过 0.0002。配比模型的领域效应在 60M 的
配对设计上与 1M 保持 96.2\% 的符号一致，但在规模与设计同时改变的 1B 上只有 68.3\%。

\textbf{问题二。}删去任一条训练轨迹，参数变化不超过估计值的 0.0905\%；剖面目标函数在各参数
偏离 2\% 时都显著上升。二者说明参数由 B1 唯一确定，但由于 B1 与已发表公式逐点一致，这种
稳定性来自数据的生成方式，而不说明真实模型的规律同样确定。

\textbf{问题四。}12 个月预测对时间口径最敏感（50.37 至 65.31），其次是总体定义（纳入模型
合并、放宽开源条件、分组）与算力占比的转移区间；时间口径的影响超过模型本身的预测区间宽度。

% STAGE B: Question 3 sensitivity after T-010 is accepted.

\subsection{误差分析}

\textbf{问题一。}在同规模留出集上，13 个目标中 Pile-CC 的相对误差最小（1.6\%，$R^2=0.865$），
DM Mathematics 最大（34.0\%，$R^2=0.413$），各领域的可预测程度差异明显，宏平均指标会掩盖
这种差异，因此表\ref{tab:q1-validation}之外还需要逐目标查看。

\textbf{问题二。}经典标度律在独立观测数据上的中位相对误差约为 8\%，远大于在 B1 上的
$3.26\times10^{-5}$；前者才是其对真实模型的预测误差量级。

\textbf{问题四。}6 个月回测中时间趋势模型的平均偏差为 $-5.49$ 分，每次都低估了前沿，说明
线性延续在近期是保守的；正式预测区间因此计入了回测误差。
