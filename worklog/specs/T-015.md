# T-015 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — reproducible Python environment stabilization

| Field | Value |
| --- | --- |
| Task | T-015 |
| Prompt version | v1 |
| Level | L2 |
| Stage | environment stabilization |
| Issued by | M1 |
| Execution status | archived prospectively, before execution |

### Archival note

This exact formal instruction is archived prospectively, before any T-015
implementation begins. It is archived from the issued Markdown carrier, not
reconstructed from memory. `worklog/specs/` is owned by T-013; this block is
written under M1's T-013 prompt-spec authority and does not alter T-015
ownership. **Archival records the instruction only; it does not imply
scientific acceptance or task completion.**

Only the line endings were normalised to LF, as required for a tracked file in
this repository; the prompt text itself is unaltered. The issued carrier ends
without a final newline, so the closing fence below follows the last line of
the prompt directly. The L1 launcher that dispatched this specification is not
archived.

### Canonical execution prompt

```text
You are M1 executing T-015.

FORMAL EXECUTION REQUIREMENT
============================

Model:
    Claude Opus 5.5

Reasoning effort:
    high

Automatic model fallback:
    OFF

If Claude Opus 5.5 at high effort is unavailable:

    STOP.

Do not silently switch model or effort.

Task:
    T-015 — reproducible Python environment stabilization

Level:
    L2

ARCHIVE:
    YES

This task is project-support / reproducibility work.
It is NOT a scientific dependency and must not alter any accepted Q1/Q2
scientific result.

Current accepted main at dispatch:

    191ba4eabccb65abb76b0d4a0194842664511d7c

Current task state:

    T-015 = todo
    Owner = M1

Owned T-015 paths:

    environment.yml
    reviews/T-015/
    worklog/M1.md

Important governance fact:

    worklog/specs/ is owned by T-013, not by T-015.

Therefore archive this formal specification prospectively under M1's T-013
central prompt-spec authority BEFORE starting T-015 implementation.

Do not change the scientific dependency graph.

T-010 and T-011 remain operationally held until T-015 is accepted.


======================================================================
0. OBJECTIVE
======================================================================

Stabilize the project's declared Python environment so that a clean environment
created from the repository resolves the same direct scientific runtime that
produced the accepted T-007 and T-008 results.

The accepted runtime recorded independently by both Q1 and Q2 is:

    Python  3.11.16
    NumPy   2.4.6
    SciPy   1.17.1
    pandas  3.0.6
    PyYAML  6.0.3

The current environment.yml declares only broad versions:

    python=3.11
    pip
    numpy
    scipy
    pandas
    pyyaml

The goal is to remove that direct-package ambiguity and verify that a newly
created environment reproduces the accepted executable behavior.

Do NOT overclaim what this accomplishes.

Exact direct-package pins are NOT a full cross-platform lock of every conda
transitive dependency or build string unless the evidence demonstrates that.

The final review must distinguish:

    direct scientific runtime stabilization

from:

    full platform/build-level lock

Do not call the latter achieved unless it actually is.


======================================================================
1. READ FIRST
======================================================================

Read:

    README.md
    AGENTS.md
    TASKS.md
    environment.yml

    results/tables/q1-reproduction.md
    results/tables/q2-reproduction.md

    reviews/T-007/HANDOVER.md
    reviews/T-008/HANDOVER.md

Also inspect the relevant environment-sensitive validation entry points:

    scripts/selftest_quality.py
    scripts/selftest_interfaces.py
    scripts/selftest_q2_closure.py

Do not rely on chat summaries where tracked evidence exists.


======================================================================
2. VERIFY DISPATCH BASE
======================================================================

Run:

    git fetch --all --prune
    git status
    git rev-parse HEAD
    git rev-parse origin/main

Require:

    local main == origin/main
    HEAD == 191ba4eabccb65abb76b0d4a0194842664511d7c

or, if main has advanced legitimately since dispatch:

    inspect every intervening commit before continuing.

If any intervening change touches:

    environment.yml
    T-015 ownership
    T-010 / T-011 launch status
    accepted T-007/T-008 runtime evidence

STOP and report rather than assuming compatibility.

Require a clean working tree before beginning.


======================================================================
3. PROSPECTIVELY ARCHIVE THIS EXACT SPECIFICATION
======================================================================

This exact instruction is the formal T-015 v1 specification.

Because worklog/specs/ is centrally owned by T-013:

- archive it under T-013 authority;
- do not pretend worklog/specs/ is owned by T-015;
- do not alter T-015 ownership merely for archival convenience.

Create:

    worklog/specs/T-015.md

using the repository's canonical prompt-spec format.

Record:

    Task: T-015
    Prompt version: v1
    Level: L2
    Issued by: M1
    Stage: environment stabilization

Archive the substantive instruction exactly.

Do not include:
- absolute local paths;
- usernames;
- account details;
- provider quotas;
- machine-specific secrets.

If the source was dispatched from a local Markdown carrier, normalize line
endings only and verify the archived body matches the issued source
character-for-character after that normalization.

This archival change belongs to T-013 governance.

Commit it separately on main, before T-015 implementation, using:

    docs(spec): archive T-015 environment stabilization [T-013]

Run:
    git diff --check
    public-safety PreCommit
    public-safety PrePush

Push main.

Then re-sync and use the resulting main as the base for the T-015 branch.


======================================================================
4. CREATE THE T-015 HIGH-IMPACT BRANCH
======================================================================

environment.yml is a high-impact shared surface.

Create a short-lived branch from the post-archive main:

    chore/m1-T015-pin-python-env

Do NOT edit environment.yml directly on main.

Do NOT use rebase or force-push later.


======================================================================
5. ESTABLISH THE ACCEPTED RUNTIME FROM TRACKED EVIDENCE
======================================================================

Read the runtime dictionaries from:

    results/tables/q1-reproduction.md
    results/tables/q2-reproduction.md

Require them to agree exactly on:

    python
    numpy
    scipy
    pandas
    pyyaml

Expected accepted runtime:

    python = 3.11.16
    numpy = 2.4.6
    scipy = 1.17.1
    pandas = 3.0.6
    pyyaml = 6.0.3

Do not hard-code these as authority if the tracked records disagree.

If Q1 and Q2 tracked runtime records disagree:

    STOP.

The tracked accepted reproduction records are the authority.


======================================================================
6. INSPECT THE CURRENT ACCEPTED ENVIRONMENT
======================================================================

Inspect the currently working accepted project environment read-only.

Use conda to record, at minimum:

    python --version

and imported versions of:

    numpy
    scipy
    pandas
    yaml

Also inspect:

    conda list

for the scientific packages and their resolved build/channel metadata.

Record enough information in the local execution notes to diagnose a clean-env
failure if one occurs.

Do NOT dump the entire local environment into tracked files merely because it
is available.

Do NOT add unrelated packages to environment.yml.

Do NOT speculate about future T-010/T-011 dependencies.


======================================================================
7. PIN THE CURRENT DIRECT SCIENTIFIC RUNTIME
======================================================================

Modify environment.yml narrowly.

Pin the direct scientific dependencies to the accepted runtime:

    python=3.11.16
    numpy=2.4.6
    scipy=1.17.1
    pandas=3.0.6
    pyyaml=6.0.3

Retain:

    pip

unless there is a demonstrated reproducibility reason to change it.

Do not pin pip merely for aesthetic consistency if no accepted runtime record
defines its required version.

Preserve:

    conda-forge
    nodefaults

Do not introduce:
- a second dependency file;
- requirements.txt;
- speculative downstream packages;
- platform-specific packages without evidence;
- unrelated formatting churn.

Update the environment.yml comments so they accurately state:

- these versions are pinned to the runtime that produced accepted T-007/T-008
  reproduction;
- this stabilizes the declared direct scientific runtime;
- it is not by itself a complete platform/build-level lock of every transitive
  dependency.

Do not write conclusions stronger than the verification supports.


======================================================================
8. TEST SOLVABILITY WITHOUT WEAKENING THE PINS
======================================================================

Before accepting the edit, test whether a fresh environment can be created from
the modified environment.yml.

Create a disposable conda environment with a distinct temporary name.

Do NOT destroy or mutate the accepted working environment.

Do NOT reuse an already-populated environment for this gate.

If exact pins fail to resolve:

    STOP.

Do NOT:
- loosen versions;
- change channels;
- silently substitute nearby releases;
- use pip to bypass a failed conda resolution.

Report the solver failure exactly.


======================================================================
9. VERIFY THE CLEAN ENVIRONMENT'S DIRECT RUNTIME
======================================================================

In the newly created environment, require exact equality:

    Python  3.11.16
    NumPy   2.4.6
    SciPy   1.17.1
    pandas  3.0.6
    PyYAML  6.0.3

Import every package.

Require imports to succeed.

Record:
- version;
- conda build;
- channel;

for the five scientific runtime components where conda reports them.

If a direct package version differs:

    FAIL T-015.

Do not treat "same major/minor" as sufficient.


======================================================================
10. EXECUTABLE REGRESSION VALIDATION IN THE CLEAN ENVIRONMENT
======================================================================

Run the existing READ-ONLY validation surface from the fresh environment.

At minimum:

    python scripts/selftest_quality.py
    python scripts/selftest_interfaces.py
    python scripts/selftest_q2_closure.py

Use the clean environment through conda activation or `conda run`.

Do not call another Python installation accidentally.

Expected accepted behavior:

    selftest_quality:
        PASS 47 / FAIL 0

    selftest_interfaces:
        all 21 assertions pass

    selftest_q2_closure:
        PASS 73 / FAIL 0

The exact count is a regression expectation from accepted T-008 evidence.
If current main has legitimately changed one of these tests after T-008,
inspect the change rather than blindly forcing the old count.

All tests must pass.

Do not weaken a failing test.

Do not edit Q1/Q2 scientific code as part of T-015.

If a test fails because of environment resolution:

    STOP and report.

If a test exposes a scientific/code defect unrelated to environment.yml:

    STOP and report it as out-of-scope.


======================================================================
11. NUMERICAL RUNTIME SANITY
======================================================================

Because the original environment defect affected SciPy/L-BFGS-B and BLAS/LAPACK
loading, explicitly verify that the clean environment can execute the existing
Q2 optimization path without interpreter/library crash.

Use an existing read-only fitting/self-test path.

Do not create a new scientific method.

Require:
- optimizer completes;
- no DLL / BLAS / LAPACK loading error;
- no process-level crash;
- resulting accepted self-test comparison passes.

Do not interpret this as a new scientific validation.
It is environment/runtime validation only.


======================================================================
12. DO NOT MODIFY ACCEPTED SCIENCE
======================================================================

T-015 must not alter:

    src/
    scripts/
    configs/
    results/
    paper/
    TASKS.md
    accepted interface files
    T-007 or T-008 scientific conclusions

The only T-015 tracked implementation changes allowed are:

    environment.yml
    reviews/T-015/HANDOVER.md
    worklog/M1.md

The earlier T-013 spec archival is a separate governance commit.

If another tracked path appears necessary:

    STOP.

Do not expand scope silently.


======================================================================
13. REVIEW PACKAGE
======================================================================

Create:

    reviews/T-015/HANDOVER.md

Keep it concise and evidence-oriented.

It must include:

- task and owner;
- base main SHA;
- branch;
- accepted runtime source:
      q1-reproduction.md
      q2-reproduction.md
- old environment.yml declaration;
- new direct pins;
- exact clean environment creation command;
- clean environment resolved versions;
- relevant build/channel metadata;
- self-test commands and outcomes;
- SciPy optimization/runtime sanity result;
- whether the accepted working environment was mutated;
- scientific files changed: none;
- limitations.

The limitations section must state explicitly:

    The repository now pins the direct scientific Python runtime used by the
    accepted Q1/Q2 reproduction.

and, unless stronger evidence genuinely exists:

    This is not a full cross-platform lock of all transitive conda packages,
    build strings, BLAS implementation, operating-system libraries or hardware.

Do not call it "bitwise environment reproducibility" unless that was actually
established.


======================================================================
14. WORKLOG
======================================================================

Append an accurate T-015 block to:

    worklog/M1.md

Record:

- direct runtime pins established;
- clean environment creation;
- validation actually run;
- approximate active effort rounded to 0.5 h;
- AI execution configuration:
      Claude Opus 5.5 + high
- limitations;
- Draft PR state.

Do not rewrite historical entries.

Do not use token count, LOC, commit count or prompt count as a contribution
metric.


======================================================================
15. VALIDATION BEFORE COMMIT
======================================================================

Require:

    git diff --check

Run:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PreCommit

Verify the branch diff contains only:

    environment.yml
    reviews/T-015/HANDOVER.md
    worklog/M1.md

No other tracked path may differ.

Confirm no:
- absolute machine path;
- username;
- secret;
- provider metadata;
- local conda path;
- environment directory;
- large binary file

entered Git.


======================================================================
16. COMMIT AND DRAFT PR
======================================================================

Use focused commits on:

    chore/m1-T015-pin-python-env

Suggested implementation commit:

    chore(env): pin accepted scientific runtime [T-015]

If review/worklog is a separate commit:

    docs(review): record environment verification [T-015]

Subjects must follow repository rules:
- ASCII
- one line
- <=72 characters
- no co-author trailer.

Before push run:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PrePush

Push the branch.

Open a Draft PR.

Suggested title:

    chore(env): pin accepted scientific runtime [T-015]

The PR body must summarize:

- why broad dependency declarations were insufficient;
- exact accepted direct versions;
- clean-env creation result;
- executable validation result;
- scope limitation: direct-runtime stabilization, not full platform lock;
- no scientific code/result changed.

Do NOT merge.

Do NOT mark T-015 done.

Leave T-010 and T-011 at todo.


======================================================================
17. DISPOSABLE ENVIRONMENT CLEANUP
======================================================================

After all clean-environment validation results have been captured and the PR is
safely published, remove ONLY the temporary verification conda environment.

Do not remove:
- the accepted working project environment;
- any team member environment;
- accepted interface objects;
- local datasets.

Verify the temporary environment no longer exists.

This cleanup is operational and is not a scientific result.


======================================================================
18. STOP CONDITIONS
======================================================================

STOP rather than improvising if:

- tracked Q1 and Q2 runtime records disagree;
- exact accepted direct versions cannot be solved from the declared channels;
- fresh environment resolves a different direct version;
- any required package fails to import;
- SciPy/L-BFGS-B crashes or loads incompatibly;
- any accepted self-test fails;
- fixing the failure would require modifying Q1/Q2 science;
- a new dependency is required without an existing task needing it;
- environment.yml would need channel changes;
- T-015 would need to modify a path outside its owned scope;
- public-safety fails;
- main or T-015 ownership changes unexpectedly.

Do not weaken versions or validation to obtain a pass.


======================================================================
19. FINAL RESPONSE
======================================================================

Return a concise structured report containing:

1. T-013 prompt-spec archival commit SHA;
2. T-015 branch base main SHA;
3. T-015 branch HEAD;
4. Draft PR number and state;
5. exact old dependency declarations;
6. exact new pinned direct dependencies;
7. clean environment creation result;
8. resolved direct runtime versions;
9. conda build/channel metadata for the scientific packages;
10. selftest_quality result;
11. selftest_interfaces result;
12. selftest_q2_closure result;
13. SciPy/L-BFGS-B runtime sanity result;
14. confirmation accepted working environment was not mutated;
15. branch diff path list;
16. public-safety / whitespace result;
17. limitations: direct runtime stabilization vs full platform lock;
18. confirmation T-015 remains not-done pending supervisory review;
19. confirmation T-010 and T-011 remain todo / paused;
20. confirmation the temporary verification environment was removed.

End exactly:

T-015 ENVIRONMENT STABILIZATION COMPLETE — DRAFT PR READY FOR SUPERVISORY REVIEW
```

### Status

- Archived before execution; outcome **pending** at archival time.
- Evidence belongs in `reviews/T-015/HANDOVER.md` on branch
  `chore/m1-T015-pin-python-env` and its Draft PR.
- T-015 remains `todo` in `TASKS.md` until supervisory review.

---
