# T-012 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — pre-integration readiness

| Field | Value |
| --- | --- |
| Task | T-012 |
| Prompt version | v1 |
| Level | L2 |
| Stage | pre-integration readiness |
| Issued by | M1 |
| Issued | 2026-09-24T10:00:00+08:00 |

### Objective

Prepare the integration architecture, evidence map, acceptance gates,
manuscript plan and reproducibility checklist **before** all scientific
dependencies have completed.

This stage must **not** perform final scientific integration. The repository's
dependency graph remains authoritative, and final T-012 stays blocked by T-010
and T-011.

### Prerequisites

Accepted `main` only. The pre-integration branch is cut from `main`, not from
any task branch, and contains only work derived from accepted `main` plus
explicit readiness metadata about pending tasks.

### Allowed paths

    paper/INTEGRATION_PLAN.md
    results/tables/t012-integration-readiness.md
    reviews/T-012/HANDOVER.md
    worklog/specs/T-012.md
    worklog/M1.md

### Forbidden

- Performing final scientific integration, or marking T-012 `done` / `review`.
- Weakening or removing `T-010 + T-011 -> T-012` from the dependency graph, or
  inventing a new dependency to justify preparatory work.
- Merging, cherry-picking, copying or rewriting another member's work; editing
  their branches, worklogs or review packages.
- Continuing T-008 modelling, or modifying T-008 code or pull-request content.
- Editing scientific implementation code under `src/quality/`, `src/mixture/`,
  `src/scaling/`, `src/panel/`, `src/alloc/`, `src/evolution/`.
- Writing final manuscript conclusions whose evidence is not yet reviewed and
  accepted, including a final abstract.
- Adopting a numerical or scientific claim from an unreviewed branch as
  accepted project evidence.

Branch metadata and pull-request metadata **may** be inspected to establish that
pending work exists. Anything obtained that way is marked
`UNREVIEWED / NON-AUTHORITATIVE / NOT FOR MANUSCRIPT CLAIMS` and its scientific
conclusions are not copied into the plan as facts.

### Acceptance criteria

The stage fails if any of the following is not true.

1. The branch is cut from accepted `main` and contains no content merged,
   cherry-picked or copied from a task branch.
2. A cross-question interface matrix exists covering IF1-IF4 with, per row:
   producer, interface, consumer, schema/semantic contract, unit/scale,
   provenance class, validity region, required uncertainty, acceptance gate,
   current readiness, and prohibited downstream interpretation.
3. An evidence-to-claim map exists covering Q1-Q4 and the cross-cutting
   classes, each claim carrying its minimum supporting artifact, mandatory
   caveat and one of `READY` / `PENDING REVIEW` / `BLOCKED` / `NOT STARTED`.
   Where evidence does not exist the entry reads
   `BLOCKED - DO NOT WRITE AS A RESULT`, never a placeholder number.
4. A five-level claim hierarchy (A direct, B model-conditional, C
   semi-synthetic calibration, D extrapolation, E unresolved/diagnostic) is
   defined, and every future quantitative claim is required to be classifiable
   into it. The hierarchy is not used to score people or tasks.
5. A figure and table inventory records, per item: ID, question, purpose,
   producer, source artifact, dependency, provenance class, whether uncertainty
   must be shown, manuscript destination and readiness. No speculative final
   figure is generated and no plot type is locked before its producing task is
   accepted.
6. A units, scales and direction audit exists for N, D, Loss, Q, p, benchmark
   scores, compute, time, context length, uncertainty types and the validity
   box, each with canonical representation, source contract, known conversion,
   downstream risk and acceptance check. An ambiguous convention is recorded as
   ambiguous rather than silently resolved.
7. Every manuscript section is classified as `STRUCTURAL PLACEHOLDER`,
   `SAFE GENERIC PROSE`, `ACCEPTED SCIENTIFIC CONTENT` or
   `PREMATURE / MUST NOT FINALIZE YET`, with an evidence owner and the trigger
   that makes it eligible for final writing.
8. Final acceptance gates G1-G12 are specified with owner, evidence, pass
   condition and current state.
9. The review package states explicitly that pre-integration readiness work
   exists but final T-012 remains dependency-blocked, and that no unreviewed
   scientific result was adopted.
10. Interface self-test, whitespace check and the public-safety gate pass; no
    absolute machine path, ignored dataset, or another member's path enters the
    commit.

### Canonical execution prompt

```text
Run from the intended `modeling-research-2026` repository root.

FORMAL T-012 v1 PRE-INTEGRATION STAGE

Purpose: prepare the integration architecture, evidence map, acceptance gates,
manuscript plan and reproducibility checklist BEFORE all scientific
dependencies have completed. This stage MUST NOT perform final scientific
integration.

The dependency graph remains authoritative:

    T-007 -> T-008
    T-008 -> T-010
    T-008 + T-009 -> T-011
    T-010 + T-011 -> T-012

Therefore T-012 final integration remains blocked by T-010 and T-011; this
stage is PREPARATORY ONLY; it must not convert unreviewed branch outputs into
accepted scientific facts; it must not merge, cherry-pick, copy or rewrite
another member's work; it must not continue T-008 modelling; and it must not
write final manuscript conclusions whose evidence is not yet reviewed and
accepted.

0. START FROM CLEAN MAIN, NOT FROM THE T-008 BRANCH

Verify the repository root, remote, status, current branch, and the log across
all refs. The T-008 draft pull request is a REVIEWED/FROZEN checkpoint: do not
continue working on its branch and do not merge it. Require the T-008 working
tree to be clean before leaving it.

Check out `main`, fast-forward, and require local `main` to equal `origin/main`
with a clean tracked working tree. Create `chore/m1-T012-preintegration` and
push it early. Do not branch from the T-008 pull request. Do not merge or
cherry-pick the Q1, Q4 or T-008 task branches. This branch must contain only
work derived from accepted `main` plus explicit readiness metadata about
pending tasks.

1. READ THE GOVERNANCE AND CURRENT CONTRACTS

Read the repository README, the agent contract, the coordinator instructions,
the task register, the shared interface module, the review-package and
prompt-spec conventions and templates, the coordinator worklog, the manuscript
entry point, the 2026 format conformance record, the bibliography and every
manuscript section. Inspect only the ACCEPTED main-branch artifacts relevant to
Q1-Q4.

Branch and pull-request metadata may be inspected to know that pending work
exists. Numerical or scientific claims from an unreviewed branch may NOT be
adopted as accepted project evidence. Anything read from a pending branch
solely to understand interface or readiness state is marked
UNREVIEWED / NON-AUTHORITATIVE / NOT FOR MANUSCRIPT CLAIMS, and its scientific
conclusions are not copied into the integration plan as facts.

2. ARCHIVE THIS PROMPT

Create the T-012 prompt specification using the existing convention, recording
the substantive instructions. It must be machine-independent: no drive letters,
no local usernames, no private machine information, no provider account,
endpoint or quota information. Its execution premise is to run from the
intended repository root. Do not modify earlier prompt specifications.

3. DO NOT MISREPRESENT T-012 STATUS

Final scientific integration has not started in the dependency-graph sense
because T-010 and T-011 are incomplete. Do not mark T-012 `done` or `review`;
prefer leaving it `todo`. Do not weaken or remove `T-010 + T-011 -> T-012`. The
review package must state that pre-integration readiness work exists but final
T-012 remains dependency-blocked. Do not invent a new scientific dependency to
justify this preparatory work.

4. BUILD A CROSS-QUESTION INTERFACE MATRIX

Create the integration plan as a technical document, not manuscript prose, with
a matrix covering at least: producer task, interface/artifact, consumer task,
schema/semantic contract, unit/scale, provenance class, validity region,
uncertainty required, acceptance gate, current readiness, and prohibited
downstream interpretation.

Cover IF1 domain quality (producer Q1; consumers Q2 and Q3 where applicable),
preserving: quality in the unit interval; accepted indicator directions
standardized higher-is-better; quality domains are not identical to the
seventeen mixture domains; mapping may be partial; coverage fraction is
empirical mapped mixture mass, not a ratio of domain counts; domain interval
and sample size travel with the score; unmapped domains must not receive
invented quality scores. Status PENDING REVIEW until independently reviewed.

Cover IF2 mixture response (producer Q1; consumers Q2 and Q3), preserving:
seventeen-domain simplex semantics; declared contrast basis; zero policy;
renormalisation; validation split; domains without loss; provenance; and that
no coefficient may be interpreted without its reference basis. Status PENDING
REVIEW.

Cover IF3 scaling law (producer Q2; consumers Q3 and Q4 where justified),
distinguishing the classic object available locally with quality pinned to one
from a generalized object NOT YET EMITTED. Preserve: the organizer label and
the numerical fingerprint of the principal training-log table are separate
facts; the classic fit supports estimator/generator recovery on that table;
empirical external support comes from independent validation such as the
cross-family table excluding overlapping same-family rows, and the literature
table; the quality-bearing tables are not ordinary observed quality
experiments; the quarantined table's quality semantics remain unresolved; no
final empirical quality exponent exists; and candidate analytical identities
are not equivalent to an adopted empirical generalized law. Generalized status
BLOCKED ON REVIEWED IF1/IF2 AND T-008 CLOSURE.

Cover IF4 loss/benchmark bridge (producer Q4 forecasting task; consumer the
manuscript integration), requiring explicit comparability strata, primary
stratum, prediction error, anchor count, benchmark score scale and provenance.
Status NOT YET PRODUCED. Do not infer it from preliminary panel work.

5. BUILD THE EVIDENCE-TO-CLAIM MAP

Add a second matrix mapping future manuscript claims to required evidence, to
prevent a polished manuscript from silently turning diagnostics into
conclusions, semi-synthetic fits into empirical facts, extrapolation into
validation, and unreviewed branches into accepted evidence.

For every claim class give: claim, minimum supporting task/artifact, mandatory
caveat, and current state among READY, PENDING REVIEW, BLOCKED, NOT STARTED.

Cover at least - Q1: definition of overall and domain quality; reliability and
uncertainty; indicator conflict findings; replication of the secondary
attachments; domain mapping and mapped-mass coverage; mixture effects and
interactions; extrapolation across scale. Q2: classic scaling; principal-table
fingerprint interpretation; observed external validation; quality dependence;
quality/parameter substitution; large-model extrapolation. Q3: compute-feasible
optimization; fixed-quality analytical benchmark; optimized quality; quality
budget share; critical context length; structural transitions; comparison of
quality-cost functions. Q4: panel population; model-type strata; detailed-task
reconciliation; loss/benchmark bridge; scale versus non-scale decomposition;
historical technical progress; twelve-month frontier; twenty-four-month stress
forecast. Cross-cutting: uncertainty; external validity; extrapolation;
provenance; negative results.

Where evidence does not yet exist write `BLOCKED - DO NOT WRITE AS A RESULT`
rather than a placeholder numerical conclusion.

6. DEFINE THE MANUSCRIPT CLAIM HIERARCHY

Define levels A direct supported result (accepted task output plus reproducible
tracked evidence); B model-conditional result (exact consequence conditional on
an explicitly stated model form); C semi-synthetic calibration (numerical
behaviour supported primarily by organizer-labelled semi-synthetic or generated
data); D extrapolation (evaluation outside the identifying support or validity
region); E unresolved or diagnostic only. Require every future quantitative
claim to be classifiable into one level. Do not use the hierarchy to score team
members or tasks.

7. PREPARE A FIGURE AND TABLE INVENTORY - DO NOT CREATE FINAL FIGURES YET

Specify the minimal final figure and table set. Do not generate speculative
final figures. Record per item: ID, question, scientific purpose, producer
task, source artifact or script, dependency, provenance class, whether
uncertainty must be shown, manuscript destination and current readiness. Prefer
a compact final paper. Do not lock exact plot types before the producing task
is accepted.

8. AUDIT UNITS, SCALES AND DIRECTION CONVENTIONS

Create a readiness audit table - not a scientific-results table - covering the
cross-question conventions currently encoded in accepted main: parameter count;
token count; loss; quality; mixture vector; benchmark scores; compute; time;
context length; confidence and uncertainty types; and the validity box. For
each state canonical representation, source contract, known conversion,
downstream risk and acceptance check. Do not silently choose a unit where the
contract is ambiguous.

9. AUDIT THE CURRENT MANUSCRIPT SKELETON

Inspect every manuscript section. Do not write the final scientific paper. For
each section classify the current state as STRUCTURAL PLACEHOLDER, SAFE GENERIC
PROSE, ACCEPTED SCIENTIFIC CONTENT, or PREMATURE / MUST NOT FINALIZE YET, and
define the intended evidence owner. State when each section becomes eligible
for final writing. In particular do not finalize the abstract before all four
questions have accepted results; do not write Q1 numerical conclusions before
the Q1 task is reviewed; do not write generalized Q2 numerical conclusions
before the Q2 task closes; do not write Q3 conclusions before the Q3 task; do
not write Q4 decomposition or forecast conclusions before the Q4 forecasting
task; and require the validation and evaluation sections to distinguish
interpolation, observed validation, semi-synthetic calibration and
extrapolation.

10. DEFINE FINAL ACCEPTANCE GATES

Write an explicit final-integration gate checklist. Final T-012 must not begin
or close until all applicable gates pass. At minimum: G1 Q1 accepted with
reviewed IF1 and IF2; G2 Q2 closed with a final decision on the generalized law
and a final IF3 or an explicit justified alternative; G3 Q3 accepted with
optimizer and sensitivity evidence; G4 panel task accepted with its foundation
and reconciliation limits; G5 forecasting task accepted with decomposition,
IF4, forecast and uncertainty; G6 all emitted interface objects validate and
schema versions are compatible; G7 all manuscript numbers regenerate from
tracked scripts; G8 no semi-synthetic or generated result is presented as
observed evidence; G9 every headline estimate uses the uncertainty type its
data justifies; G10 the manuscript format rules still pass after scientific
content is inserted; G11 no raw, private or local-only source enters version
control; G12 a final rendered-document visual audit covering pagination,
figures, tables, glyphs, references and identity rules. For each gate specify
owner, evidence, pass condition and current state.

11. DEFINE HANDOFF AND REVIEW ORDER

Record the intended integration order: Q1 review, then Q2 closure, then Q3; and
in parallel the panel-task review, then the forecasting task; then Q3 plus the
forecasting task into final integration. Do not require the panel task merely
to unblock Q2. Do not require the third member to finish before the coordinator
can close Q2 once reviewed IF1/IF2 exist. Do not begin Q3 scientific
conclusions before the final IF3 exists. Do not allow the forecasting task to
treat provisional Q2 or unreviewed panel artifacts as accepted inputs.

12. PREPARE THE REVIEW PACKAGE

Create the T-012 review package describing PRE-INTEGRATION ONLY, stating the
status as pre-integration readiness complete with final T-012 still
dependency-blocked. Include branch and base; what was inspected; the
integration matrix; the evidence-to-claim map; the unit and scale audit; the
manuscript readiness audit; the final acceptance gates; unresolved
dependencies; an explicit statement that no unreviewed scientific result from
another member was adopted; and the exact next trigger for resuming final
integration. Do not imply final scientific integration is complete.

13. WORKLOG

Append one accurate pre-integration entry to the coordinator worklog recording:
planning only; artifacts created; active effort rounded to half an hour; the
assistant used; validation performed; dependencies intentionally not consumed;
and status as a preparatory checkpoint with final integration blocked. Do not
rewrite older entries.

14. VALIDATION

Run the interface self-test, the whitespace check and the public-safety gate in
pre-commit mode. Verify manually: no absolute machine path in tracked files; no
ignored or local dataset staged; no path owned by another member modified; no
T-008 code or pull-request content modified; no scientific value copied from an
unreviewed branch; T-012 not marked complete; the dependency graph intact;
every planned manuscript claim carrying an evidence owner and gate; every
cross-question quantity carrying explicit units, direction and provenance; and
the abstract still unwritten as a scientific summary.

15. GIT DISCIPLINE

Allowed tracked paths for this stage are the integration plan, the readiness
audit table, the T-012 review package, the T-012 prompt specification and the
coordinator worklog. Do not edit scientific implementation code, another
member's worklog or review package, or another member's branches. Use focused
commits of the form `docs(integration): ... [T-012]`, ASCII, one line, at most
72 characters, preferring a small number of conceptually coherent commits. Push
the branch and open a DRAFT pull request into the main branch as a preparatory
architecture and readiness checkpoint. Do not merge it. Do not mark T-012 done.

16. STOP CONDITIONS

Stop rather than improvise if a required interface semantic is ambiguous in the
shared interface module; if accepted main contains contradictory units or
conventions that cannot be reconciled without a scientific decision; if
preparing the plan would require modifying a path owned by another member; if a
pending branch contains a tempting result that has not been reviewed; if the
dependency graph would need changing; if a new shared interface field appears
necessary; or if the public-safety gate fails. Record the issue in the handover
rather than silently solving a scientific problem inside an
integration-planning task.
```

### Outcome

- Commit / PR: see `reviews/T-012/HANDOVER.md`
- Review package: `reviews/T-012/HANDOVER.md`
- Result: pre-integration readiness prepared; final T-012 remains
  dependency-blocked on T-010 and T-011, so the stage is a preparatory
  checkpoint rather than an accepted integration.

---

## v2 — supervisory delta: Q4 readiness, context-length scope, IF3 unit gate

| Field | Value |
| --- | --- |
| Task | T-012 |
| Prompt version | v2 |
| Type | supervisory delta |
| Level | L2 |
| Stage | pre-integration readiness |
| Issued by | M1 |
| Issued | 2026-09-24T14:00:00+08:00 |

### Objective

Documentation and integration-governance corrections only, on the existing
pre-integration branch and its draft pull request. No scientific modelling, and
no unreviewed branch result consumed as accepted evidence.

Three corrections and one gate strengthening:

1. the Q4 detailed-task reconciliation was recorded as READY when accepted
   `main` carries only a partial reconciliation;
2. the open context-length question was recorded as a unit question when the
   unit is settled and the open item is the analysis scope;
3. G2 did not defend against the Q2 unit-regime hazard already recorded in the
   readiness audit.

### Allowed paths

    worklog/specs/T-012.md
    paper/INTEGRATION_PLAN.md
    results/tables/t012-integration-readiness.md
    reviews/T-012/HANDOVER.md

### Forbidden

Scientific modelling; consuming unreviewed branch results as accepted evidence;
modifying scientific code, numerical artifacts, `TASKS.md`, another member's
path, `src/interfaces.py`, or the T-008 pull request; merging the T-012 draft
pull request.

### Acceptance criteria

1. v1 of this specification is preserved unchanged.
2. The evidence-to-claim map does **not** mark the detailed-task reconciliation
   as READY. Its overall state is PENDING REVIEW, justified by the accepted
   artifact reconciling four of six dimensions and quarantining the other two.
   The separable scale convention — that raw accuracy and baseline-rescaled
   score are distinct quantities requiring explicit rescaling — may remain
   READY. The unreviewed corrective result on the Q4 panel branch is not copied
   in, not reproduced, and not counted toward readiness.
3. The context-length row keeps **tokens** as the canonical unit and reframes
   the open T-010 item as the feasible attachment-supported range, the
   representative regimes and the sensitivity grid used for the critical-context
   analysis. Both the integration plan and the readiness audit say the same
   thing.
4. G2 carries explicit unit-regime acceptance conditions for the final or
   generalized scaling-law interface: one canonical raw-parameter / raw-token
   convention for anything emitted externally; an internal fit may use billions
   for conditioning but must transform parameters **and** validity box
   consistently before emission; an executable unit-equivalence check requiring
   prediction at the same physical point to be invariant across the two
   representations within a stated tolerance; and rejection of any interface
   object whose parameter units and validity-box units disagree. The shared
   interface module is not changed.
5. The review package and the draft pull-request body carry the same
   corrections.
6. Interface self-test, whitespace check and public-safety gate pass. T-012
   stays `todo` and the pull request stays draft and unmerged.

### Canonical execution prompt

```text
Run from the intended `modeling-research-2026` repository root, on the existing
pre-integration branch and its draft pull request.

This is documentation / integration-governance only. Do not perform scientific
modeling and do not consume unreviewed branch results as accepted evidence.

1. Append a v2 supervisory-delta block to the T-012 execution specification,
   preserving v1 unchanged.

2. Correct Q4 readiness. In the evidence-to-claim map, do NOT mark
   "Detailed-task reconciliation" as READY. Accepted main contains a PARTIAL
   reconciliation: four of six dimensions reconcile, while the two
   knowledge/mathematics dimensions remain quarantined there. A later
   correction to one of them exists only on the unreviewed panel branch.
   Therefore set the overall claim to PENDING REVIEW. You may separately retain
   as READY the already-established scale convention: raw accuracy and
   baseline-rescaled score are distinct quantities and require explicit
   rescaling. Do not copy the unreviewed corrective result into this task as
   accepted evidence.

3. Correct the context-length convention. Keep the canonical unit as tokens.
   The unresolved downstream decision is NOT the physical unit; it is the
   feasible attachment-supported range, the representative regimes, and the
   sensitivity grid used for the critical-context analysis. Make this
   distinction consistent in both the integration plan and the readiness audit
   table.

4. Strengthen the scaling-law gate against the unit-regime hazard. Add explicit
   acceptance conditions for the final or generalized scaling-law interface:
   an externally emitted object must use one canonical convention consistent
   with the classic object, namely raw parameter count and raw token count; an
   internal fit may use billions for numerical conditioning, but its parameters
   and validity box must be transformed consistently before emission; add a
   future executable unit-equivalence check requiring that prediction at the
   same physical point be invariant under the internal-billions versus
   external-raw representation, up to numerical tolerance; and require
   rejection of any object whose parameter units and validity box units
   disagree. Do NOT change the shared interface module in this stage.

5. Synchronize the review package and the draft pull-request body with these
   corrections.

6. Run the interface self-test, the whitespace check and the public-safety gate
   in pre-commit mode. Make one focused docs commit and push it.

Do not modify scientific code, numerical artifacts, the task register, another
member's path, or the other open pull request. Keep the pull request draft and
unmerged for supervisory recheck.
```

### Outcome

- Commit / PR: see `reviews/T-012/HANDOVER.md`, v2 delta section.
- Review package: `reviews/T-012/HANDOVER.md`
- Result: corrections applied; T-012 remains `todo` and final integration
  remains blocked by T-010 and T-011.

---

## v3 — accepted-content manuscript fast-track and 2026 LaTeX template arbitration

| Field | Value |
| --- | --- |
| Task | T-012 |
| Prompt version | v3 |
| Level | L3 |
| Stage | accepted-content manuscript fast-track and 2026 LaTeX template arbitration |
| Issuer / executor | M1 |
| Dependency status | final T-012 still blocked on T-010 |
| Archived | 2026-09-27T00:24:31+08:00 |
| Execution status | archived prospectively, before execution |

### Archival note

This block is appended; the v1 and v2 blocks above are unchanged byte for byte.
It is archived prospectively, before any Stage-A work, from the instruction
exactly as issued. The instruction was issued directly in the dispatching
session rather than through a separate carrier, so its text was taken from that
issued instruction, not reconstructed from memory. It contains no drive letter,
user name, private path or provider detail; the three incoming files are named
only by their repository-root filenames, as the instruction requires.
**Archival records the instruction only; it is not scientific acceptance, and
final T-012 integration remains blocked on T-010.**

SHA-256 of the prompt text below (UTF-8, LF, with a final newline):
`a30943d80056964063b18372fef8874e1f9e707e8a02e7ad58a7c065d248c77a`.

### Canonical execution prompt

```text
ARCHIVE: YES — T-012 v3, L3 accepted-content manuscript fast-track and
2026 LaTeX template arbitration.

You are M1.

Execute a time-critical but scientifically conservative manuscript Stage A
while T-010 remains under M4 execution.

This stage has TWO simultaneous objectives:

A. resolve the final 2026 LaTeX implementation by comparing the current
   repository template, the newly supplied M3 LaTeX template, and a possible
   hybrid against the TWO OFFICIAL 2026 organizer Word files;

B. immediately draft every manuscript component that can be supported by
   already accepted evidence, without consuming pending T-010/Q3 results.

This is NOT final T-012 scientific integration.

T-010 remains a hard gate for:
- final Q3 manuscript content;
- final abstract;
- final cross-question synthesis;
- final conclusions that depend on Q3;
- final T-012 acceptance.

Do not weaken the dependency:

    T-010 + T-011 -> T-012

Do not mark T-012 done or review.
Do not start or modify T-010.
Do not consume M4's unreviewed branch as manuscript evidence.

======================================================================
0. FRESH START AND GOVERNANCE
======================================================================

Run from the repository root.

Read completely:

    README.md
    AGENTS.md
    TASKS.md
    worklog/specs/T-012.md
    worklog/specs/T-014.md
    paper/INTEGRATION_PLAN.md
    paper/FORMAT_2026.md
    paper/main.tex
    paper/format_2026.tex
    paper/references.bib
    all paper/sections/*.tex
    scripts/build_paper.ps1
    scripts/setup_template.ps1

Inspect the currently provisioned ignored:

    paper/template/

if present.

Freshly fetch origin/main.

Require at minimum that current main contains the accepted ownership-transfer
checkpoint:

    583b8db6660ac852e62642e7f9c38f1773b9a528

or a legitimate inspected descendant.

Require:

    T-007 = done
    T-008 = done
    T-009 = done
    T-010 = wip, owner M4
    T-011 = done
    T-012 = todo
    T-014 = done
    T-015 = done

Do not modify TASKS.md in this stage.

Create a clean short-lived branch from accepted current main:

    docs/m1-T012-manuscript-stage-a

Do not branch from M4's T-010 branch.
Do not merge or cherry-pick M4's scientific work.
Do not inspect pending Q3 numbers for manuscript use.

======================================================================
1. ARCHIVE THIS V3 SPECIFICATION PROSPECTIVELY
======================================================================

Append a new v3 block to:

    worklog/specs/T-012.md

Preserve v1 and v2 byte-for-byte.

Record:

    Task: T-012
    Prompt version: v3
    Level: L3
    Stage:
        accepted-content manuscript fast-track and
        2026 LaTeX template arbitration
    Issuer / executor: M1
    Dependency status:
        final T-012 still blocked on T-010

The archived canonical prompt must be machine-independent.

Do not place drive letters, usernames, private paths, provider/API details,
or other machine-specific information in the tracked specification.

The three incoming files are referred to only by their repository-root
filenames:

    华为杯LaTeX模板.zip

    附件3：“华为杯”第二十三届中国研究生数学建模竞赛论文模板.doc

    附件2：“华为杯”第二十三届中国研究生数学建模竞赛论文格式规范.docx

======================================================================
2. FIRST REMOVE THE THREE FORMAT FILES FROM THE REPOSITORY ROOT SAFELY
======================================================================

The repository root currently contains three local source/template files that
must NOT remain there.

They are local-only evidence and must never become tracked or staged.

Canonical destinations:

OFFICIAL textual format specification:

    docs_local/gmcm-2026/source/
        paper_format_specification.official.docx

OFFICIAL Word manuscript template:

    docs_local/gmcm-2026/source/
        paper_template.official.doc

M3-contributed, unofficial LaTeX implementation:

    docs_local/gmcm-2026/contrib/
        m3_latex_template.contributed.zip

Use:

    docs_local/gmcm-2026/contrib/PROVENANCE.md

for M3-template provenance.

The existing:

    docs_local/gmcm-2026/source/PROVENANCE.md

remains the provenance record for organizer originals.

For EACH incoming root file:

1. compute SHA-256 and byte size before archival;

2. check whether its canonical destination already exists;

3. if the official DOC/DOCX destination exists:
   - compare source and destination SHA-256 and byte size;
   - if exactly identical, retain the canonical archived copy and remove the
     redundant root copy only after verification;
   - do not create another duplicate;

4. if the canonical official destination does not exist:
   - copy/move it under the canonical renamed filename;
   - re-hash it;
   - require byte-for-byte equality;
   - only then remove the root copy;

5. if an existing canonical official copy DIFFERS from the new root copy:
   - do NOT overwrite either;
   - move the incoming copy under:

         docs_local/gmcm-2026/incoming-conflict/

     with a safe descriptive filename;
   - record both hashes;
   - STOP all format/template decisions and report the conflict;

6. archive the M3 ZIP analogously under contrib/;

7. after archival, re-hash all canonical files;

8. require NONE of the three original filenames to remain at repository root;

9. require none of these local files to appear in Git staging or tracked
   content.

The supervisor-inspected copy of the M3 ZIP supplied for this stage has:

    byte size:
        2145953

    SHA-256:
        71f4b24c19a048eff5ed1c97fe85eb1c5509fe3090f3942f094ff4ed64ac586c

Compare the local incoming ZIP against that receipt.

If it differs, do not silently assume it is the same M3 package.
Record the discrepancy and STOP the M3-template comparison until the correct
copy is identified.

Do not archive any of the three source files into Git.

======================================================================
3. EXTRACT THE M3 TEMPLATE ONLY INTO IGNORED AUDIT SPACE
======================================================================

Do NOT unzip M3's package into:

    repository root
    paper/
    paper/template/
    tracked source directories

Extract it only to an ignored audit location such as:

    docs_local/gmcm-2026/audit/m3-latex-template/

Preserve the original ZIP unchanged.

Inventory every archive member before using it.

The supervisor-inspected package is expected to contain, among other items:

    main.tex
    setup.tex
    config.tex
    fonts.tex
    fonts-overleaf.tex
    latexmkrc
    README.md

    sections/
        cover.tex
        abstract.tex
        body.tex
        references.tex
        appendix.tex

    assets/
        cpipc.png
        modeling.png
        huawei.jpeg
        xjtu.png

It also contains generated/duplicate material such as:

    main.pdf
    main.log
    main.aux
    main.out
    main(1).*
    latexmkrc(1)
    setup(1).tex
    config(1).tex

These generated or duplicate files are NOT template authority and must not be
copied into the project manuscript.

Do not trust README assertions merely because they state that the template is
based on official material.

M3's template is:

    CONTRIBUTED IMPLEMENTATION EVIDENCE

not:

    organizer-issued LaTeX
    official format authority
    proof of conformance

======================================================================
4. FORMAT AUTHORITY HIERARCHY
======================================================================

Use this hierarchy for every template decision:

1. OFFICIAL 2026 textual manuscript-format specification:

       paper_format_specification.official.docx

   This is normative for explicit typography, pagination and textual rules.

2. OFFICIAL 2026 Word manuscript template:

       paper_template.official.doc

   This is authoritative for cover structure, visible layout and structural
   intent where it does not contradict level 1.

3. This repository's accepted T-014 conformance evidence:

       paper/FORMAT_2026.md
       paper/format_2026.tex

4. Candidate LaTeX implementations:

       a. current gmcmthesis-based implementation
       b. M3 ctexart/XeLaTeX implementation
       c. a carefully justified hybrid

5. README files, third-party defaults and aesthetic preference.

Never reverse this hierarchy.

A candidate that is easier to use but conflicts with an official rule loses.

A candidate that visually resembles the official Word file but violates an
explicit textual rule also loses.

Do not optimize for superficial appearance ahead of compliance.

======================================================================
5. AUDIT ALL THREE LATEX STRATEGIES
======================================================================

Evaluate THREE candidates:

A. CURRENT

    existing gmcmthesis class
    + paper/main.tex
    + paper/format_2026.tex
    + existing build/provisioning scripts

B. M3

    M3's standalone ctexart/XeLaTeX implementation

C. HYBRID

    current audited manuscript infrastructure with only objectively superior,
    officially verified elements borrowed/reimplemented from M3.

Do not assume in advance which one wins.

Create:

    paper/TEMPLATE_DECISION_2026.md

with a requirement-by-requirement comparison.

At minimum compare:

- 2026 competition title and edition;
- cover composition;
- four-logo row;
- host-university identity;
- school / team number / three-member fields;
- title placement;
- anonymous-body boundary;
- body identity leakage;
- PDF metadata identity leakage;
- abstract-page structure;
- abstract maximum-length rule;
- absence/presence of an English abstract;
- whether body starts immediately after abstract;
- table-of-contents behavior;
- abstract page numbering;
- cover numbering;
- no running header;
- centred footer page number;
- paper-title font and size;
- level-1 heading font, size and alignment;
- ordinary Chinese body font and size;
- English font;
- line spacing;
- paragraph indentation and spacing;
- page geometry / margins;
- figure/table caption conventions;
- equation numbering;
- bibliography style;
- first-citation ordering;
- page information for books;
- journal/web reference behavior;
- appendix behavior;
- AI-tool-use disclosure support;
- build reproducibility;
- XeLaTeX/compiler requirements;
- font availability/fallback behavior;
- hidden text layers;
- stale competition-edition text;
- stale logos or host-university assets;
- unsupported embedded metadata;
- redistribution/public-repository risk;
- compatibility with current paper/sections structure;
- compatibility with generated result figures;
- compatibility with scripts/build_paper.ps1;
- clean-clone behavior;
- final-submission reliability.

For each item record:

    official requirement/source
    current implementation
    M3 implementation
    hybrid option if relevant
    evidence
    compliance status
    decision

Do not use an arbitrary aesthetic score.

Use dominance rules:

1. official compliance is mandatory;
2. identity/anonymity safety is mandatory;
3. scientific content must remain unaffected by template selection;
4. among compliant options, prefer the one with:
   - lower submission risk;
   - stronger auditability;
   - better reproducibility;
   - smaller required change;
   - clearer local-only handling of copyrighted assets;
   - fewer hidden assumptions.

======================================================================
6. SPECIFIC CURRENT-TEMPLATE FINDINGS THAT MUST BE RECHECKED
======================================================================

Do not blindly trust the old T-014 audit, but use it as accepted historical
evidence that must be revalidated against the newly supplied materials.

The current implementation previously found:

- gmcmthesis originally used non-single line spacing;
- it originally put page numbers in the wrong place and carried a header;
- paper/format_2026.tex corrected those;
- the table of contents was removed so body begins after abstract;
- the old class logo asset was stale;
- that stale logo asset also carried a hidden stale text layer;
- the stale logo row was therefore removed;
- the current class's competition-title asset was retained because it appeared
  to show the correct 2026 edition;
- current T-014 left omission of the logo row as a known compromise.

Re-test these findings.

In particular determine whether the M3 package offers a better, safe solution
to the 2026 logo-row problem.

M3 contains:

    cpipc.png
    modeling.png
    huawei.jpeg
    xjtu.png

Do NOT adopt them merely because filenames look correct.

Verify:

- visible content against the OFFICIAL Word template;
- correct 2026 host institution;
- crop and aspect ratio;
- absence of stale competition edition;
- absence of hidden/selectable stale text where applicable;
- absence of identity metadata;
- provenance;
- whether the assets may remain local-only rather than redistributed.

If M3's four-logo composition is demonstrably faithful to the official Word
template and safer than the stale current class asset, a HYBRID may be
preferable.

If so, implement it with the smallest robust change.

Do not copy an entire unofficial template merely to obtain four images.

======================================================================
7. SPECIFIC M3-TEMPLATE FINDINGS THAT MUST BE VERIFIED
======================================================================

The supervisor-inspected M3 package appears to use:

    \documentclass[UTF8,zihao=-4,fontset=none,a4paper]{ctexart}

with XeLaTeX and local setup/config files.

Its setup appears to specify explicit geometry approximately:

    left   22.51 mm
    right  22.47 mm
    top    30.02 mm
    bottom 18.49 mm

and to define:

- small-four body;
- Songti/Heiti families;
- centred footer numbering;
- no header;
- level-1 heading formatting;
- manual four-logo cover;
- abstract then body without TOC.

Treat these as AUDIT EXPECTATIONS ONLY.

Verify them from the actual local archived ZIP and the official Word files.

Do not assume those geometry values are official merely because they appear in
the contributed template.

Do not preserve duplicated `(1)` files, build logs, AUX files or preview PDFs
as source.

Do not adopt its placeholder references as real references.

Do not adopt its placeholder scientific prose.

======================================================================
8. SELECT AND IMPLEMENT THE BEST TEMPLATE STRATEGY
======================================================================

After completing the comparison, choose exactly one final strategy:

    RETAIN CURRENT
    ADOPT M3 CORE
    HYBRID

The decision must be evidence-based.

Strong preference under time pressure:

    if the current audited system is already fully compliant and M3 provides
    no verified material improvement, RETAIN CURRENT;

    if M3 solves a real official-conformance defect with lower risk, use the
    minimum HYBRID change;

    adopt M3 CORE only if the official evidence establishes that the current
    architecture is materially inferior and replacement creates less overall
    submission risk than patching.

Do not rewrite a functioning manuscript stack for stylistic reasons.

Do not fork or track the full M3 source bundle.

Do not commit organizer Word files, contributed ZIPs, fonts or restricted
assets.

If local-only official/M3 assets are needed for the final PDF, design a
repository-relative OPTIONAL local-asset mechanism with a safe fallback.

Never introduce a user-specific absolute path into tracked TeX or scripts.

Any non-redistributable asset remains local-only.

If the clean public clone cannot reproduce the exact cover because an official
asset is intentionally local-only, document that accurately rather than
embedding copyrighted or private material.

======================================================================
9. UPDATE THE FORMAT AUDIT ONLY WHERE EVIDENCE CHANGES
======================================================================

If the selected implementation changes current behavior, update:

    paper/FORMAT_2026.md

and, only where necessary:

    paper/format_2026.tex
    paper/main.tex
    scripts/build_paper.ps1
    scripts/setup_template.ps1

Do not rewrite the historical audit gratuitously.

Clearly distinguish:

    historical T-014 finding
    newly verified M3-template evidence
    final selected implementation

Keep:

    paper/TEMPLATE_DECISION_2026.md

as the concise comparison/decision record.

======================================================================
10. BEGIN MANUSCRIPT WRITING NOW — ACCEPTED EVIDENCE ONLY
======================================================================

Do not wait for T-010 to finish before writing material whose evidence is
already accepted.

Use ONLY accepted main-branch evidence.

Primary accepted scientific sources include:

Q1 / T-007:

    reviews/T-007/HANDOVER.md
    accepted q1-* tracked result tables
    accepted IF1/IF2 receipts

Q2 / T-008:

    reviews/T-008/HANDOVER.md
    results/tables/q2-final-closure.md
    results/tables/q2-classic-fit.md
    results/tables/q2-uncertainty-robustness.md
    accepted IF3 receipt

Panel foundation / T-009:

    reviews/T-009/HANDOVER.md
    accepted panel/reconciliation artifacts

Q4 / T-011:

    reviews/T-011/HANDOVER.md
    accepted q4-* result tables
    accepted IF4 limitations and forecast/decomposition evidence

Cross-question rules:

    paper/INTEGRATION_PLAN.md

Official problem wording:

    canonical permitted problem-statement source already established by the
    project.

Do NOT use:

- M4 T-010 branch results;
- old PR #17 results as final Q3 evidence;
- M2 frozen handoff draft as manuscript evidence;
- the quarantined original data-description PDF;
- the sanitized derivative PDF as scientific authority;
- social-media screenshots;
- external contestants' parameter estimates.

======================================================================
11. WRITE THESE MANUSCRIPT SECTIONS SUBSTANTIVELY
======================================================================

Replace placeholder-only prose with publication-quality Chinese where accepted
evidence permits it.

Prioritize:

    paper/sections/01-restatement.tex
    paper/sections/02-analysis.tex
    paper/sections/03-assumptions.tex
    paper/sections/04-preprocessing.tex
    paper/sections/05-1-model-q1.tex
    paper/sections/05-2-model-q2.tex
    paper/sections/05-4-model-q4.tex
    paper/sections/06-validation.tex
    paper/sections/07-evaluation.tex
    paper/references.bib

You may adjust section decomposition if the official format and final
scientific narrative justify it.

The paper should read as one coherent manuscript, not four task reports pasted
together.

For every substantive numerical statement:

- trace it to accepted tracked evidence;
- preserve units;
- preserve uncertainty type;
- preserve validity region;
- preserve claim level;
- do not add unjustified precision;
- do not turn a sensitivity into an identified effect;
- do not turn a diagnostic into empirical validation;
- do not turn a model-conditional consequence into observed fact.

======================================================================
12. Q1 WRITING CONTRACT
======================================================================

Write Q1 from the accepted T-007 closure only.

Preserve important limitations including:

- IF1 quality is not a causal quality effect;
- mapping from quality domains to mixture domains is partial;
- mapped-mass coverage is empirical mixture mass, not domain-count ratio;
- IF2 is valid only at its accepted 1M scope;
- A10/A11 broad transfer failed;
- do not imply validated 10B/70B mixture transfer;
- preserve the accepted interaction/model-selection evidence;
- preserve uncertainty and negative results where they matter to downstream
  interpretation.

Do not oversell Q1.

======================================================================
13. Q2 WRITING CONTRACT
======================================================================

Write the classic scaling-law result accurately:

    L(N,D) = E + A N^{-alpha} + B D^{-beta}

Use accepted parameters and uncertainty only from accepted artifacts.

Preserve:

- raw N parameter counts;
- raw D token counts;
- accepted validity box;
- classic IF3 has Q pinned/sentinel semantics;
- no accepted generalized quality-bearing law exists;
- B1 appears consistent with deterministic published-law
  generator/storage-precision recovery;
- this is not ordinary empirical discovery of a new scaling law;
- LOO vectors are robustness evidence;
- bootstrap marginal intervals and profile evidence remain distinct.

Do not fabricate a quality exponent.

======================================================================
14. Q4 WRITING CONTRACT
======================================================================

Write Q4 from accepted T-011 evidence.

Preserve at minimum:

- panel population and strata definitions;
- IF4 is degenerate/no-slope and does NOT support a transferable
  loss-to-benchmark bridge;
- no bridge-based frontier forecast exists;
- broad BC trend decomposition;
- distinction between parameter-scale and non-scale components;
- sparse C4 compute metadata;
- primary compute-aware A subset and its limitations;
- transfer of compute-associated slowdown to BC is assumption-based
  sensitivity, not an identified BC causal effect;
- 12-month primary and 24-month stress framing;
- date-clock sensitivity is materially important;
- C3 is auxiliary;
- accepted BBH/MUSR detailed-task evidence may be used;
- quarantined C8 MATH children must not be used.

Use language such as:

    "在给定转移假设下的敏感性结果"

where appropriate, rather than causal language.

======================================================================
15. Q3 MUST REMAIN BLOCKED IN THE TRACKED MANUSCRIPT
======================================================================

Do NOT write pending Q3 numerical results.

Keep:

    paper/sections/05-3-model-q3.tex

as a controlled structural placeholder.

You may improve only its NON-SCIENTIFIC structure if useful, for example:

    求解思路
    模型建立
    约束与有效域
    结构转移判据
    稳健性分析
    结果与讨论

but do not insert:

- unreviewed budget allocations;
- 1e24 numbers;
- regime thresholds;
- LOO ranges;
- quality-cost crossings;
- final context conclusions;
- any value derived solely from M4's pending branch.

Use comments rather than fake placeholder numbers.

No manuscript sentence may make a Q3 result claim until T-010 is accepted.

======================================================================
16. ABSTRACT AND FINAL CONCLUSION POLICY
======================================================================

Do NOT finalize:

    paper/sections/00-abstract.tex

while Q3 is pending.

However, to save time you MAY create an ignored local-only scaffold:

    scratch/t012-manuscript/abstract-scaffold.md

containing:

- accepted Q1 paragraph;
- accepted Q2 paragraph;
- an explicit `Q3 BLOCKED` slot;
- accepted Q4 paragraph;
- final synthesis slot.

This scaffold must never be presented as a completed abstract and must not be
tracked.

Likewise, do not write a final global conclusion that depends on Q3.

If the manuscript architecture benefits from a conclusion section, you may
prepare its structure and accepted Q1/Q2/Q4 components, but any cross-question
conclusion remains explicitly incomplete until T-010 acceptance.

======================================================================
17. MANUSCRIPT QUALITY STANDARD
======================================================================

Write in formal competition-paper Chinese.

Requirements:

- concise but technically complete;
- mathematical objects defined before use;
- equations followed by interpretation;
- every figure/table introduced and interpreted;
- no raw worklog prose;
- no Git/PR/task identifiers in the manuscript;
- no "we asked AI" language in scientific prose;
- no code-internal variable names unless mathematically meaningful;
- no unsupported adjectives such as “显著优于” without evidence;
- no vague “效果很好” statements;
- no repeated restatement of tables;
- no false causal language;
- no result introduced only in the conclusion;
- no excessive implementation detail that belongs in the appendix;
- no absolute machine paths;
- no identities outside the permitted cover mechanism.

Prefer a compact, evidence-dense paper.

Do not pad page count.

======================================================================
18. FIGURES AND TABLES
======================================================================

Reuse accepted generated figures/tables where they communicate the result well.

Do not manually copy numbers from screenshots.

If a new manuscript summary table or figure is needed:

- derive it only from accepted tracked artifacts;
- put new integration-owned generated output under a clearly T-012-owned path;
- make the transformation reproducible;
- preserve uncertainty semantics;
- do not overwrite another task's accepted result artifact.

Prefer a small number of high-information figures/tables.

Every visible number must have a traceable accepted source.

======================================================================
19. REFERENCES
======================================================================

Audit paper/references.bib.

Remove placeholder or unused references.

Do not invent bibliographic metadata.

For every retained reference:

- verify that it was actually used;
- verify authors/title/venue/year;
- include required pages for books;
- preserve first-citation ordering through the selected bibliography system;
- prefer primary/original literature over secondary summaries.

Do not cite social media or another team's competition result as scientific
support.

If a reference cannot be verified in time, omit it rather than fabricate it.

======================================================================
20. AI-USE / APPENDIX PREPARATION
======================================================================

Read the official 2026 AI-use requirement if it is part of the supplied
organizer materials already available locally.

Do not infer rules from M3's README.

Prepare the appendix structure necessary for:

- reproducibility;
- code/data description where required;
- actual AI tool-use disclosure required by official competition material.

Do not fabricate a declaration of non-use.

Do not put private API endpoints, credentials, local proxy details, or
irrelevant internal workflow into the submission.

======================================================================
21. BUILD BOTH CANDIDATE FORMATS DURING TEMPLATE ARBITRATION
======================================================================

Where technically feasible, build:

A. the current repository manuscript implementation;

B. an isolated local-only M3-template fixture populated with neutral dummy
   content sufficient to examine formatting;

C. the selected final implementation.

Do not use scientific branch output merely to populate a comparison fixture.

Compare rendered pages visually.

At minimum inspect:

- cover;
- abstract;
- first body page;
- representative equation page;
- representative figure/table page;
- references;
- appendix.

Do not rely only on PDF text extraction.

The previous T-014 audit demonstrated that:

- visible rendering can be correct while extracted text is mojibake;
- hidden stale text can exist even when the visible asset looks acceptable.

Therefore perform BOTH:

- rendered-page inspection;
- metadata/text-layer sanity checks where useful.

But never use OCR as the primary validation mechanism.

======================================================================
22. FINAL FORMAT ACCEPTANCE GATES FOR THIS STAGE
======================================================================

The selected LaTeX implementation must satisfy:

F1. exact official competition edition;
F2. official typography/pagination requirements;
F3. official cover structure to the greatest legally/reproducibly achievable
    extent;
F4. identity limited to permitted cover fields;
F5. no identity in body or PDF metadata;
F6. no stale host/competition edition;
F7. no hidden stale identity/edition text;
F8. abstract starts page numbering correctly;
F9. body begins where official rule requires;
F10. no unauthorized TOC/header;
F11. required fonts or a clearly documented final-build prerequisite;
F12. bibliography behavior compliant;
F13. clean build with no `??`, missing references or missing glyph warnings
     that affect submission;
F14. local-only official/contributed assets never become tracked;
F15. a clean public clone degrades explicitly/safely rather than silently
     producing a false official cover;
F16. final local submission build uses the verified compliant asset/font set.

If any mandatory official rule cannot be satisfied, do not call the format
accepted.

======================================================================
23. TIME-PRESSURE RULE
======================================================================

Do not spend the remaining project time on a wholesale template rewrite unless
a real official-conformance defect requires it.

Template engineering is subordinate to producing the scientifically correct
paper.

Once one option is demonstrably compliant and safe:

    STOP template experimentation.

Do not pursue cosmetic micro-optimizations.

Immediately continue manuscript writing.

======================================================================
24. VALIDATE THE STAGE-A PAPER
======================================================================

Build with the selected final manuscript implementation.

Use the repository build entry point where retained:

    powershell -File scripts/build_paper.ps1 -Clean

Require successful compilation.

Run:

    git diff --check

and the repository public-safety PreCommit gate.

Inspect the rendered PDF page by page.

Check at minimum:

- no root-format source files remain;
- no organizer DOC/DOCX/ZIP staged;
- no extracted M3 files staged;
- no fonts staged;
- no local absolute paths;
- no personal identity in body;
- no unresolved references;
- no missing figures;
- no stale competition edition;
- no stale host-university logo;
- no placeholder manuscript text accidentally presented as a result;
- Q3 remains visibly unfinished in source but not filled with fake numbers;
- abstract is not falsely finalized;
- Q1/Q2/Q4 numbers agree with accepted artifacts;
- units and significant digits are consistent;
- uncertainty labels are accurate;
- references resolve.

======================================================================
25. TRACKED PATH DISCIPLINE
======================================================================

This stage may modify only what is necessary under M1-owned manuscript and
integration scope, including:

    paper/
    results/              only new T-012 integration-owned artifacts
    reviews/T-012/
    worklog/specs/T-012.md
    worklog/M1.md

and manuscript build/setup scripts only if the template decision genuinely
requires them:

    scripts/build_paper.ps1
    scripts/setup_template.ps1

Local-only format evidence belongs under:

    docs_local/gmcm-2026/

and remains ignored.

Do NOT modify:

    TASKS.md
    src/alloc/
    scripts/q3_*
    results/tables/q3-*
    results/figures/q3-*
    reviews/T-010/
    worklog/M4.md
    M4's branch
    accepted upstream Q1/Q2/Q4 producer artifacts

Do not alter accepted interface bytes.

======================================================================
26. REVIEW PACKAGE
======================================================================

Update:

    reviews/T-012/HANDOVER.md

without claiming final T-012 completion.

Include:

A. manuscript Stage-A scope;

B. exact accepted evidence consumed;

C. exact pending evidence deliberately not consumed;

D. root-file archival receipts;

E. M3 ZIP hash and inventory summary;

F. template comparison outcome:

       RETAIN CURRENT
       ADOPT M3 CORE
       or HYBRID

G. reasons tied to official requirements;

H. exact format implementation changes;

I. rendered-build audit results;

J. manuscript sections drafted and their evidence sources;

K. sections still blocked on T-010;

L. current page count, only as a mechanical build fact;

M. remaining manuscript risks;

N. exact trigger for Stage B:

       T-010 accepted and merged to main after independent supervisory review.

State explicitly:

    T-012 final integration remains blocked on T-010.

======================================================================
27. WORKLOG
======================================================================

Append an accurate entry to:

    worklog/M1.md

Record:

- T-012 v3 Stage-A manuscript drafting;
- format-source archival;
- template arbitration;
- selected strategy;
- sections drafted;
- evidence used;
- Q3 deliberately excluded;
- validation performed;
- active effort rounded to 0.5 h;
- assistant/model used;
- final T-012 still blocked.

Do not rewrite prior entries.

======================================================================
28. COMMITS AND DRAFT PR
======================================================================

Use a small number of coherent commits.

Examples:

    docs(spec): archive T-012 manuscript fast-track [T-013]

    fix(format): reconcile 2026 manuscript template [T-012]

    docs(paper): draft accepted q1 q2 q4 content [T-012]

    docs(review): record manuscript stage-a handover [T-012]

Subjects must follow repository policy:

    type(scope): lowercase imperative [T-0xx]

ASCII, one line, <=72 characters.

Run PreCommit before every commit.

Run PrePush before pushing.

Push only:

    docs/m1-T012-manuscript-stage-a

Open a Draft PR into main.

Do NOT merge it in this stage.

Do NOT mark it ready.

Do NOT mark T-012 done.

If origin/main advances during execution because M4 progresses T-010, do not
automatically consume T-010.

Inspect the new main changes first.

Pending T-010 results remain unavailable for manuscript claims until
supervisory acceptance and merge.

======================================================================
29. STOP CONDITIONS
======================================================================

STOP rather than improvise if:

- either official organizer file conflicts with the existing canonical archive;
- M3 ZIP hash differs from the supplied reference copy;
- official textual specification and Word template contain an unresolved
  material conflict;
- the selected template would require redistributing an asset whose status is
  uncertain;
- format work would require modifying M4-owned T-010 paths;
- accepted Q1/Q2/Q4 artifacts disagree on a load-bearing number;
- a manuscript statement would require pending T-010 evidence;
- a required citation cannot be verified;
- public-safety checks fail;
- the final local build leaks team identity into body/metadata;
- any stale competition edition or stale host identity remains;
- any root source/template file would remain as an untracked Git hazard.

Record the blocker precisely.

Do not paper over it.

======================================================================
30. FINAL REPORT
======================================================================

Return:

1. starting main SHA;
2. T-012 v3 archival commit;
3. three incoming root-file source hashes/sizes;
4. canonical archive destinations and post-archive hashes;
5. confirmation the three original root files no longer remain there;
6. M3 ZIP verification against:
       71f4b24c19a048eff5ed1c97fe85eb1c5509fe3090f3942f094ff4ed64ac586c
7. template comparison summary;
8. selected strategy:
       RETAIN CURRENT / ADOPT M3 CORE / HYBRID
9. exact reasons for the selection;
10. exact LaTeX/build files changed;
11. official-format gates and results;
12. sections substantively drafted;
13. accepted evidence consumed;
14. Q3/pending evidence explicitly excluded;
15. current rendered page count;
16. rendered visual-audit result;
17. bibliography/reference audit result;
18. public-safety results;
19. final branch HEAD;
20. Draft PR number/state;
21. remaining blockers;
22. exact Stage-B trigger;
23. confirmation:
       T-010 remains M4/wip unless separately changed by its owner;
       T-012 remains todo/finally blocked;
       no pending Q3 result was promoted into manuscript evidence.

End exactly:

T-012 STAGE A MANUSCRIPT READY — FINAL INTEGRATION STILL BLOCKED ON T-010
```

### Status

- Archived before execution; outcome **pending** at archival time.
- Evidence belongs in `reviews/T-012/HANDOVER.md` on branch
  `docs/m1-T012-manuscript-stage-a` and its Draft PR.

---
