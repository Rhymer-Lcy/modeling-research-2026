# T-011 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — Q4 decomposition, loss/benchmark bridge and frontier forecast

| Field | Value |
| --- | --- |
| Task | T-011 |
| Prompt version | v1 |
| Level | L2 |
| Issued by | M1 |
| Task owner | M3 |
| Execution status | archived prospectively, before execution |

### Archival note

This exact formal instruction is archived prospectively, before any T-011
execution begins. It is archived from the issued Markdown carrier, not
reconstructed from memory. `worklog/specs/` is owned by T-013; this block is
written under M1's T-013 prompt-spec authority. **Archival records the
instruction only; it does not imply scientific acceptance or task completion.**

Only the line endings were normalised to LF, as required for a tracked file in
this repository; the prompt text itself is unaltered. The issued carrier ends
without a final newline, so the closing fence below follows the last line of
the prompt directly. The L1 launcher that dispatched this specification is not
archived.

### Canonical execution prompt

```text
T-011 FORMAL EXECUTION SPECIFICATION
====================================

Task:
    T-011 — Q4 decomposition, loss/benchmark bridge and frontier forecast

Owner:
    M3

Level:
    L2

ARCHIVE:
    YES — formal scientific task execution specification

This specification must be archived prospectively by M1 under the repository's
central prompt-spec governance before execution.

Do not reconstruct, shorten, reinterpret, or silently strengthen this
specification after execution begins.


======================================================================
0. PURPOSE
======================================================================

Complete Problem F Question 4 on top of the accepted T-009 panel foundation and
the accepted T-008 classic scaling interface.

T-011 has four linked but distinct scientific jobs:

    1. define a defensible historical analysis population and time convention;
    2. build and validate the IF4 loss-to-benchmark bridge;
    3. decompose historical benchmark improvement into scale-associated and
       residual non-scale components without making causal claims;
    4. produce a 12-month primary frontier forecast and a 24-month stress
       forecast with honest uncertainty.

The task must preserve all T-009 quarantines and all T-008 downstream
restrictions.

Do not improve apparent forecast quality by weakening provenance rules.


======================================================================
1. GOVERNANCE AND STARTUP
======================================================================

Before making changes, read:

    README.md
    AGENTS.md
    TASKS.md

Then read:

    reviews/T-008/HANDOVER.md
    reviews/T-009/HANDOVER.md
    results/tables/q2-final-closure.md
    results/tables/q2-classic-fit.md
    results/tables/q4-c8-reconciliation.md
    results/tables/q4-c8-diagnosis.md
    results/tables/q4-detailed-task-analysis.md
    results/tables/q4-panel-schema.md
    results/tables/q4-panel-coverage.md
    results/tables/q4-model-strata.md
    results/tables/q4-date-coverage.md
    results/tables/q4-c4-metadata-coverage.md
    results/tables/q4-eligibility.md
    paper/INTEGRATION_PLAN.md
    src/interfaces.py

Inspect the canonical first-party C5/C6/C7/C8 material needed for Q4 through
repository-relative local paths.

Do not use chat summaries when first-party or accepted tracked evidence exists.

Require TASKS.md to show:

    T-008 = done
    T-009 = done
    T-011 = wip
    T-015 = done

If not:

    STOP.

Do not modify:

    worklog/specs/
    TASKS.md
    environment.yml
    src/interfaces.py
    src/scaling/
    src/panel/
    scripts/q4_panel_*
    accepted T-009 result artifacts
    another member's worklog or review package
    paper/


======================================================================
2. RUNTIME
======================================================================

Use the environment declared by environment.yml.

Require direct runtime:

    Python  3.11.16
    NumPy   2.4.6
    SciPy   1.17.1
    pandas  3.0.6
    PyYAML  6.0.3

Run through an activated environment or conda run.

Do not use machine-default Python.

If the runtime cannot be made consistent with environment.yml:

    STOP.


======================================================================
3. ACCEPTED T-008 SCALING RECEIPT
======================================================================

Require the byte-exact accepted classic IF3.

Accepted SHA-256:

    720efea859d3be3b39ac2ee1976f8adaf7a31b8b5b1a71eb72e8ee8f987c8514

Resolve its path through src.paths.

Hash and validate it before use.

Its external units are:

    N = raw parameter count
    D = raw token count

Read its exact validity box from the object.

The Q interval [1,1] is a sentinel meaning:

    no quality dimension.

The generalized quality-aware IF3 was NOT ADOPTED.

Therefore:

- no B7 gamma enters T-011;
- IF1 Q cannot be fed into a Q2 quality law;
- B8 is quarantined;
- IF2 does not establish cross-scale composition transfer.

Only classic IF3 predictions inside its validity box may be treated as accepted
Q2 model-conditional predictions.

Outside-box evaluations are extrapolation and must be labelled as such.


======================================================================
4. ACCEPTED T-009 PANEL FOUNDATION
======================================================================

Regenerate or load the accepted panel through the T-009 code path.

Do not rebuild panel logic inside T-011.

Accepted T-009 facts that must remain visible include:

    4,497 unique models
    48 panel columns

Date provenance:

    Epoch publication date is primary where available
    leaderboard submission date is fallback
    11 models have no date

C4 linkage:

    94 / 4,497 linked
    only 62 carry training compute
    linkage is concentrated and not representative

C3:

    auxiliary timeseries only
    not canonical panel rows
    duplicate keys carry distinct score vectors

Detailed-task evidence:

    BBH and MUSR are the only dimensions with accepted detailed subtask matrices

Summary-dimension status:

    IFEval reconciled
    BBH reconciled
    GPQA reconciled
    MUSR reconciled
    MMLU-PRO reconciled
    MATH Lvl 5 summary score may be used from C1
    C8 MATH child records are quarantined and MUST NOT be consumed

Do not weaken the MATH quarantine.

Do not use fuzzy identity matching.


======================================================================
5. FREEZE THE ANALYSIS POPULATION BEFORE OUTCOME FITTING
======================================================================

Define the primary T-011 analysis population from accepted T-009 fields before
fitting outcome/time models.

The choice must be based on:

    provenance
    comparability
    completeness
    model type
    date availability

not on which subset produces the strongest trend or best forecast.

At minimum record:

    inclusion criteria
    excluded strata
    date rule
    score completeness rule
    scale-data requirement
    number retained
    number excluded by each rule

Use T-009 model-type strata rather than inventing a new taxonomy.

If no single pooled population is scientifically defensible:

    use stratified analyses rather than forcing a pool.

Keep chat/fine-tuned and pretrained/base behavior separate where the accepted
strata imply that distinction.

Do not silently interpret an aggregate across heterogeneous strata as one
technology frontier.


======================================================================
6. TIME CONVENTION
======================================================================

The canonical row-level time field is the accepted T-009 analysis-date rule:

    publication date where Epoch metadata exists;
    leaderboard submission date otherwise.

Retain each row's date provenance.

Do not call every fallback date a publication date.

For any headline time trend perform sensitivity separating:

    publication-date-primary subset

from:

    fallback-date rows

where sample size permits.

Do not infer a causal "technical progress rate" from a calendar coefficient.

Permitted language:

    residual time association
    non-scale-associated improvement
    conditional time trend

Prohibited language unless separately established:

    time causes technical progress
    calendar coefficient equals algorithmic progress


======================================================================
7. SCORE SCALE AND AGGREGATION
======================================================================

Verify the score direction and scale of every C1 summary dimension before
constructing an aggregate.

Do not assume a score is higher-is-better merely because its numeric range is
0-100.

If all six accepted summary dimensions are on a common higher-is-better
baseline-rescaled scale, a simple equal-weight macro score may be used as a
transparent primary summary.

If not, STOP aggregate construction and analyze dimensions separately.

Do not use PCA or outcome-optimized weights as the primary score.

Always retain dimension-specific results so a macro average cannot hide
heterogeneous movement.

C8 MATH child records remain excluded even if the C1 MATH summary enters the
macro score.


======================================================================
8. IF4 LOSS-TO-BENCHMARK BRIDGE
======================================================================

Construct IF4 only from source records that genuinely link:

    comparable language-model loss

to:

    accepted benchmark score

for the same model/evaluation meaning.

Inspect the official C5/C6 material and derive the actual bridge sample and
comparability classes from first-party metadata.

Do not type remembered sample counts into code.

Do not fuzzy-match anchors.

Use exact / normalized / explicitly approved aliases only.

Before fitting, declare comparability strata based on provenance, not fit
quality.

Examples of relevant distinctions may include:

    same model family / training convention
    cross-family but comparable
    literature-derived
    incompatible / excluded

Use the actual first-party evidence.

Do not pool strata merely to enlarge n.

If the scientifically most comparable stratum is too small to support the
proposed bridge:

    simplify the bridge or STOP the bridge claim.

Do not compensate by adding lower-comparability rows without disclosure.


======================================================================
9. BRIDGE MODEL SELECTION
======================================================================

Use a deliberately small candidate set of low-complexity bridge forms.

The bridge maps:

    cross-entropy loss -> benchmark score

and must respect the observed score direction.

Candidate forms must be declared before comparing their errors.

Avoid high-degree polynomials, splines with outcome-selected complexity or
black-box regressors.

Use structure-respecting validation appropriate to the anchor count, such as:

    leave-one-anchor-out
    family/block-out where families repeat
    deterministic cross-validation

Do not report in-sample fit as bridge validation.

Choose the final form by a predeclared criterion balancing:

    comparability
    predictive error
    stability
    simplicity

not by visual preference.

Record prediction error explicitly.

The shared IF4 contract requires:

    strata
    primary_stratum
    form
    params
    prediction_error
    n_anchor_models
    score_scale
    provenance

Construct the dataclass using the existing shared schema.

Do not modify src/interfaces.py.


======================================================================
10. IF4 RELEASE GATE
======================================================================

Emit a local-only IF4 only if:

- anchor identities are reconciled;
- the primary stratum is scientifically defensible;
- the bridge has positive anchor count;
- validation error is computed out of sample / out of fold;
- score scale is explicit;
- provenance is explicit;
- uncertainty is sufficient for downstream propagation;
- `.validate()` passes.

Use a clear canonical local filename, for example:

    data_local/problem-f/interfaces/q4-if4-loss-benchmark-bridge.json

Resolve the directory through src.paths.

Record the exact emitted SHA-256 in the handover.

If a defensible IF4 cannot be released:

    do not fabricate one.

T-011 may still report direct score-based historical analyses, but bridge-based
forecast claims remain blocked.


======================================================================
11. BROAD HISTORICAL FRONTIER
======================================================================

Define the historical benchmark frontier before forecasting.

Do not use the single best observed model as the only primary frontier statistic
unless sample density makes a robust frontier estimator impossible.

Prefer a robust, predeclared upper-frontier estimator.

A reasonable default is an upper quantile such as the 90th percentile, but use
it only if bin/sample density supports it.

Do not tune the frontier quantile to maximize apparent trend fit.

Observed maximum may be reported as sensitivity.

If time bins are used:

- choose their width from sample density and source cadence before seeing
  forecast performance;
- report counts per bin;
- do not treat sparse-bin maxima as stable estimates.

Anchor forecasts to the last observed panel date, not the machine's current
date.


======================================================================
12. SCALE VERSUS NON-SCALE DECOMPOSITION
======================================================================

Perform at least two clearly distinguished decompositions.

A. BROAD PANEL PARAMETER-SCALE DECOMPOSITION
--------------------------------------------

Use the broad accepted panel where parameters and benchmark/date data exist.

Model benchmark performance using a low-complexity scale representation such as
log parameter count plus time / stratum controls.

Use matched-scale, fixed-effect or stratified diagnostics to ask whether
performance changes remain after conditioning on scale.

The residual time component is:

    non-scale-associated progress

not proven algorithmic causation.

B. CLASSIC-IF3 COMPUTE-AWARE DECOMPOSITION
------------------------------------------

Use classic IF3 only for rows with defensible N and D.

If D must be inferred from recorded training compute, first verify the exact
compute convention and units from first-party metadata.

Do not infer D for rows lacking the required data.

Given T-009's sparse C4 linkage, any compute-aware subset analysis must state:

    linked n
    training-compute n
    strata distribution
    selection bias

Do not present the 62-row compute-bearing subset as representative of all 4,497
models.

Require inferred N and D to lie inside IF3 validity for primary IF3-based
comparisons.

Outside-box rows may be shown only as extrapolation sensitivity.


======================================================================
13. DECOMPOSITION ROBUSTNESS
======================================================================

At minimum examine:

    primary population vs reasonable predeclared alternatives
    model-type strata
    publication-date-primary subset
    fallback-date inclusion
    scale specification
    frontier definition

Do not run a large specification search and select the most favorable result.

Prefer a small declared sensitivity matrix.

Use uncertainty methods respecting the data structure.

Where time clustering matters, use time/block bootstrap or another defensible
method rather than pretending all models are temporally independent.


======================================================================
14. FORECAST DESIGN
======================================================================

The primary forecast horizon is:

    12 months from the last observed panel date.

The stress horizon is:

    24 months from the last observed panel date.

The 24-month forecast is not co-equal evidence if it extends far beyond the
observed history.

Label it explicitly as:

    stress / longer-horizon extrapolation.

Do not anchor forecasts to the system date.

Do not use data after the panel's last accepted observation.


======================================================================
15. FORECAST COMPONENTS
======================================================================

Where evidence permits, forecast the frontier by separating:

    scale-associated evolution
    residual non-scale-associated evolution

rather than fitting an opaque time-only curve and calling it progress.

Do not assume either component continues indefinitely.

Use parsimonious trend forms.

Avoid high-degree temporal polynomials.

If multiple trend forms are compared, predeclare them and use rolling-origin or
pseudo-out-of-time validation.

If the panel is too short to validate a 12-month forecast directly, use the
longest defensible shorter rolling-origin horizon and state the limitation.

Do not manufacture a 12-month backtest that the history cannot support.


======================================================================
16. BRIDGE ERROR PROPAGATION
======================================================================

Any forecast that translates predicted loss through IF4 MUST propagate bridge
prediction error.

Do not show a narrow forecast band that conditions on a fitted bridge as
error-free.

Where feasible include distinct contributions from:

    historical frontier estimation
    scale / time model uncertainty
    classic IF3 uncertainty
    IF4 prediction error

Do not collapse fundamentally different uncertainty sources into one
mislabelled confidence interval.

State what each band means.

If bridge uncertainty dominates, say so.


======================================================================
17. CLASSIC IF3 PROVENANCE IN Q4
======================================================================

Carry the T-008 caveat into every IF3-based Q4 statement:

The principal B1 fit supports estimator / generator recovery.

It does NOT by itself establish precise real-world scaling.

Observed external support comes from:

    B4 with overlapping Pythia rows excluded
    B5 literature evidence

Do not write narrow B1 fit intervals as real-world forecast certainty.

Do not use B4 with overlapping Pythia rows as independent validation.


======================================================================
18. DETAILED-TASK ANALYSIS
======================================================================

Detailed-task heterogeneity is optional and should be included only if it helps
interpret the frontier/decomposition.

Permitted primary detailed matrices:

    BBH
    MUSR

Do not consume quarantined C8 MATH child records.

Do not treat GPQA grouped children as an ordinary per-subtask matrix merely
because the summary dimension reconciles.

If detailed-task results do not materially help the Q4 question:

    omit the extra figure rather than manufacturing complexity.


======================================================================
19. REQUIRED EXECUTABLE GUARDS
======================================================================

Add a T-011 self-test or equivalent executable checks covering at minimum:

1. accepted classic IF3 hash;
2. classic IF3 `.validate()`;
3. raw N/raw D unit convention;
4. Q sentinel not interpreted as quality;
5. no B8 consumption;
6. no IF2 cross-scale composition use;
7. T-009 panel row uniqueness;
8. accepted date-source semantics;
9. no-date rows excluded where time is required;
10. MATH C8 children cannot enter detailed primary analysis;
11. C3 cannot become canonical panel rows;
12. ambiguous C4 identities remain unlinked;
13. bridge anchors are unique and deterministically matched;
14. bridge primary stratum is declared;
15. bridge prediction error is nonempty;
16. IF4 `.validate()` passes when emitted;
17. score direction is explicit;
18. forecast anchor equals final observed panel date;
19. 12-month and 24-month horizons are computed from that anchor;
20. bridge uncertainty enters bridge-based forecast bands;
21. outside-IF3-box rows cannot enter primary IF3 decomposition;
22. training-compute-conditioned analysis reports its subset size.

Where practical include mutation fixtures proving the guards can fail.

Never weaken a guard merely to get a green run.


======================================================================
20. REQUIRED ARTIFACTS
======================================================================

Do not overwrite accepted T-009 artifacts.

Create new T-011-specific artifacts with distinct names, for example:

    results/tables/q4-evolution-population.md
    results/tables/q4-bridge.md
    results/tables/q4-decomposition.md
    results/tables/q4-frontier-forecast.md
    results/tables/q4-forecast-robustness.md

and only manuscript-useful figures, for example:

    results/figures/q4-frontier.*
    results/figures/q4-decomposition.*
    results/figures/q4-forecast.*

Use scripts named under the T-011 namespace, for example:

    scripts/q4_evolution_*
    scripts/q4_forecast_*

Do NOT modify:

    scripts/q4_panel_*

Every tracked numerical artifact must regenerate from code.


======================================================================
21. CLAIM LANGUAGE
======================================================================

Direct panel summaries:

    observed / descriptive.

Bridge-calibrated results:

    model-conditional and bridge-conditional.

Residual time trend:

    non-scale-associated historical trend.

Do NOT automatically call it:

    algorithmic progress
    technical progress
    efficiency improvement caused by time

Classic-IF3 predictions:

    model-conditional.

12-month forecast:

    forecast with explicit uncertainty and assumptions.

24-month forecast:

    stress extrapolation.

Outside validity:

    extrapolation.

Never convert an association into a causal statement.


======================================================================
22. REPRODUCTION
======================================================================

Provide deterministic reproduction for all T-011 tracked numerical artifacts.

Require:

- two complete passes byte-identical;
- accepted T-009 panel artifacts unchanged;
- accepted T-008 IF3 bytes unchanged;
- source data unchanged;
- fixed random seeds where needed;
- LF-only generated text artifacts;
- runtime direct versions equal environment.yml.

If the local derived panel is missing, regenerate it using accepted T-009
scripts rather than inventing an alternative panel.


======================================================================
23. REVIEW PACKAGE
======================================================================

Create:

    reviews/T-011/HANDOVER.md

It must contain:

- task / owner / branch / Draft PR;
- accepted T-008 IF3 receipt/hash;
- accepted T-009 panel receipt/reproduction status;
- primary population and exclusions;
- time convention;
- score scale / aggregation definition;
- bridge anchor construction and strata;
- final IF4 form;
- IF4 path and SHA-256;
- bridge validation error;
- broad scale decomposition;
- compute-aware subset decomposition;
- exact subset counts and bias caveats;
- residual non-scale interpretation;
- frontier definition;
- forecast anchor date;
- 12-month result;
- 24-month stress result;
- uncertainty decomposition;
- robustness checks;
- detailed-task use or explicit omission;
- exact commands run;
- deterministic reproduction result;
- negative findings;
- scientific limitations;
- exact downstream claims T-012 may consume.

Do not include local absolute paths or raw-data dumps.


======================================================================
24. WORKLOG
======================================================================

Append one accurate T-011 block to:

    worklog/M3.md

Record:

- work advanced;
- approximate active effort rounded to 0.5 h;
- validation actually run;
- important negative findings;
- Draft PR state;
- AI assistance and validation following repository policy.

Do not rewrite historical entries.

Do not use commits, LOC, tokens or prompt count as contribution metrics.


======================================================================
25. GIT AND REVIEW DISCIPLINE
======================================================================

Create a fresh branch from synchronized accepted main:

    exp/m3-T011-q4-evolution

Do not continue development on the old T-009 branch.

No rebase.
No force-push.

Commit subjects:

    type(scope): lowercase imperative [T-011]

ASCII, one line, <=72 characters.

Before every commit:

    git diff --check

and:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PreCommit

Before push:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PrePush

Before the final review checkpoint:

- fetch current main;
- if main advanced because of concurrent T-010 or coordination work, merge
  current main into the branch;
- do not rebase;
- if another active owner's paths conflict, STOP.

Push and open a Draft PR.

Do NOT merge.

Do NOT mark T-011 done.

Do NOT start T-012.


======================================================================
26. STOP CONDITIONS
======================================================================

STOP rather than improvise if:

- accepted IF3 hash differs;
- accepted panel cannot be reproduced;
- bridge identities cannot be reconciled deterministically;
- a usable bridge requires pooling scientifically incompatible strata;
- IF4 would lack defensible out-of-sample prediction error;
- a forecast requires treating submission date as publication date without
  disclosure;
- C8 MATH children would be required;
- C3 duplicate-key rows would be required as panel observations;
- C4 sparse linkage would be represented as population-wide;
- IF3 use requires outside-box evaluation for the primary decomposition;
- a causal "technical progress" interpretation is required to make the result
  work;
- a 12-month backtest is claimed where history cannot support it;
- forecast bands omit bridge error despite using IF4;
- a shared schema change is needed;
- another active owner's path must be modified;
- raw input modification is required;
- public-safety fails.

Failure of one optional decomposition does not invalidate other independently
supported Q4 results.

Failure to release IF4 DOES block any claim whose logic requires loss-to-score
translation.


======================================================================
27. FINAL RESPONSE
======================================================================

Return a concise structured report containing:

1. synchronized main SHA used;
2. final branch HEAD;
3. Draft PR number/state;
4. accepted IF3 hash/validation;
5. T-009 panel reproduction result;
6. primary population definition and n;
7. time convention and date-source composition;
8. score aggregation definition;
9. bridge anchor counts by comparability stratum;
10. bridge model and validation error;
11. IF4 release status/path/hash;
12. broad scale vs residual-time decomposition;
13. compute-aware subset result and subset size;
14. frontier definition;
15. forecast anchor date;
16. 12-month forecast result;
17. 24-month stress result;
18. uncertainty components;
19. robustness summary;
20. detailed-task result or omission;
21. test summary;
22. deterministic reproduction summary;
23. public-safety result;
24. exact claims T-012 may consume;
25. confirmation T-011 remains under review and unmerged.

End exactly:

T-011 Q4 EVOLUTION COMPLETE — DRAFT PR READY FOR SUPERVISORY REVIEW
```

### Status

- Archived before execution; outcome **pending** at archival time.
- Evidence belongs in `reviews/T-011/HANDOVER.md` on branch
  `exp/m3-T011-q4-evolution` and its Draft PR.

---
