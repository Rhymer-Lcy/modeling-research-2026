# T-010 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — Q3 compute-constrained resource optimization

| Field | Value |
| --- | --- |
| Task | T-010 |
| Prompt version | v1 |
| Level | L2 |
| Issued by | M1 |
| Task owner | M2 |
| Execution status | archived prospectively, before execution |

### Archival note

This exact formal instruction is archived prospectively, before any T-010
execution begins. It is archived from the issued Markdown carrier, not
reconstructed from memory. `worklog/specs/` is owned by T-013; this block is
written under M1's T-013 prompt-spec authority. **Archival records the
instruction only; it does not imply scientific acceptance or task completion.**

Only the line endings were normalised to LF, as required for a tracked file in
this repository; the prompt text itself is unaltered. The issued carrier ends
without a final newline, so the closing fence below follows the last line of
the prompt directly. The L1 launcher that dispatched this specification is not
archived.

The prompt's final line reads `...SUPERVISORY REVIEWs`, with a trailing
lowercase `s`. It is reproduced exactly as issued and has not been
corrected here.

### Canonical execution prompt

```text
T-010 FORMAL EXECUTION SPECIFICATION
====================================

Task:
    T-010 — Q3 compute-constrained resource optimization

Owner:
    M2

Level:
    L2

ARCHIVE:
    YES — formal scientific task execution specification

This specification must be archived prospectively by M1 under the repository's
central prompt-spec governance before execution.

Do not reconstruct, shorten, reinterpret, or silently strengthen this
specification after execution begins.


======================================================================
0. PURPOSE
======================================================================

Solve Problem F Question 3 under the evidence actually accepted by T-007 and
T-008.

The primary objective is to build a mathematically correct, reproducible and
scientifically honest compute-allocation model over:

    model parameters N
    training tokens D
    context length L_ctx
    any organizer-declared quality/preprocessing cost variable

subject to a fixed compute budget.

The accepted scaling interface is the CLASSIC N-D law only.

There is NO accepted empirical cross-scale quality law.

Therefore T-010 must distinguish sharply between:

    A. canonical optimization under the accepted classic IF3;
    B. exact model-conditional analytical identities;
    C. scenario / sensitivity analysis involving quality or preprocessing cost;
    D. unsupported cross-scale quality claims, which are prohibited.

A negative result — including the conclusion that the accepted model does not
identify a nontrivial optimum in Q — is a valid scientific outcome.


======================================================================
1. GOVERNANCE AND STARTUP
======================================================================

Before making changes, read:

    README.md
    AGENTS.md
    TASKS.md

Then read the accepted upstream evidence:

    reviews/T-007/HANDOVER.md
    reviews/T-008/HANDOVER.md
    results/tables/q1-if1-summary.md
    results/tables/q1-if2-summary.md
    results/tables/q2-final-closure.md
    results/tables/q2-classic-fit.md
    results/tables/q2-uncertainty-robustness.md
    paper/INTEGRATION_PLAN.md
    src/interfaces.py

Read the official/local Problem-F material needed for Q3 through canonical
repository-relative paths only.

Do not rely on chat summaries when tracked or first-party source material
exists.

Require TASKS.md to show:

    T-008 = done
    T-010 = wip
    T-015 = done

If not:

    STOP.

Work only inside T-010's current owned paths.

Do not modify:

    worklog/specs/
    TASKS.md
    environment.yml
    src/interfaces.py
    src/scaling/
    src/quality/
    src/mixture/
    src/panel/
    paper/
    another member's worklog or review package


======================================================================
2. RUNTIME
======================================================================

Use the project environment declared by environment.yml.

Before scientific execution, require the direct runtime to equal:

    Python  3.11.16
    NumPy   2.4.6
    SciPy   1.17.1
    pandas  3.0.6
    PyYAML  6.0.3

Run through an activated environment or conda run.

Do not use the machine-default Python.

If the direct runtime differs and cannot be made consistent with
environment.yml:

    STOP.


======================================================================
3. ACCEPTED INTERFACE RECEIPT
======================================================================

T-010 is a CONSUMER of accepted interface objects.

Do not regenerate T-007 or T-008 producer outputs.

Require the canonical local interface objects.

IF1 accepted SHA-256:

    b8ec99ca38f2e466b427fd1a7d88858048924cc590bbe881a966d141eb9bdeb8

IF2 accepted SHA-256:

    9f2a83da45633858822c8416b5fa70a9537a06b07ac9056e50d871492b16ff91

Classic IF3 accepted SHA-256:

    720efea859d3be3b39ac2ee1976f8adaf7a31b8b5b1a71eb72e8ee8f987c8514

Resolve their canonical locations through src.paths.

Hash each object before use and validate it against the current shared
interface contract.

If any required hash differs:

    STOP.

Do not "repair" an accepted interface by reserializing or regenerating it.


======================================================================
4. LOAD-BEARING UPSTREAM CONTRACT
======================================================================

The canonical scaling law is:

    L(N,D) = E + A N^(-alpha) + B D^(-beta)

with externally consumed units:

    N = raw parameter count
    D = raw token count

The accepted validity box is approximately:

    N:
        7.054e7 to 1.197e10

    D:
        1.34e8 to 2.999e11

Read the exact values from IF3 rather than copying rounded values from this
specification.

The IF3 Q interval [1,1] is a SENTINEL meaning:

    no quality dimension in the accepted law

It MUST NOT be interpreted as:

    IF1 Q = 1.

The accepted generalized Q-aware law was NOT ADOPTED.

Therefore:

- do not feed IF1 Q into the B7 quality exponent;
- do not infer any Q elasticity from B7;
- do not use B8;
- do not use IF2 outside 1M;
- do not synthesize IF1 + IF2 + classic IF3 into a cross-scale quality law.

IF1 remains a real domain-quality interface with partial mixture coverage.

IF2 remains a real 1M composition-response interface.

They may support bounded scenario analysis only within their accepted meanings.


======================================================================
5. RECONSTRUCT THE Q3 COMPUTE MODEL FROM FIRST-PARTY EVIDENCE
======================================================================

Identify the organizer-defined Q3 compute-cost model and all relevant Q3
attachment tables before numerical optimization.

For every term record:

    definition
    physical unit
    source
    supported range
    whether observed, specified, estimated or assumed

The accepted integration audit already establishes:

    base dense-training compute convention uses coefficient 6
    context length is measured in tokens

Verify both from canonical source material before use.

If the official Q3 material supports a context-attention term of the form

    C_attn = eta * N * D * L_ctx

derive eta from the official source.

Do NOT type eta from memory.

If the verified value is eta = 2e-4, then the simple cost-parity point

    eta * L_ctx = 6

implies

    L_ctx = 30000 tokens.

If so, report 30000 only as a COST-PARITY identity.

Do NOT automatically call it:
    the empirical optimum,
    a phase transition,
    or the discovered critical context length.

Those are separate claims requiring separate evidence.

For any organizer-defined quality/preprocessing cost, preserve the exact
definition.

If the supported structure is

    C_Q = D * [g(Q) - g(Q0)]_+

then use exactly that positive-part convention.

Do not invent a quality-cost function that is absent from first-party material.


======================================================================
6. CANONICAL RESOURCE CONSTRAINT
======================================================================

After source verification, express the compute constraint in a single
dimensionally consistent convention.

If the source supports the structure above, define:

    kappa(L_ctx) = 6 + eta * L_ctx

    delta_g(Q) = [g(Q) - g(Q0)]_+

and:

    C_total
      = D * [kappa(L_ctx) * N + delta_g(Q)]

Require:

    C_total <= C_budget

At an interior loss-minimizing optimum with monotone benefit from N and D, the
budget should be saturated.

Test that property rather than assuming it silently.

All compute quantities must use one consistent unit.

Do not mix FLOPs, PFLOP-days, raw operations or normalized compute without an
explicit conversion.


======================================================================
7. FIXED-Q ANALYTICAL BENCHMARK
======================================================================

Derive the fixed-Q benchmark analytically before trusting a numerical solver.

For fixed:

    Q
    L_ctx
    C

and saturated budget:

    D = C / (kappa*N + delta_g)

the accepted classic loss becomes:

    L(N)
      = E
        + A*N^(-alpha)
        + B*C^(-beta)*(kappa*N + delta_g)^beta

Let:

    K = B*C^(-beta)

For an interior stationary point derive and verify:

    N^(alpha+1)
    * (kappa*N + delta_g)^(beta-1)
      =
    alpha*A / (K*beta*kappa)

Do not merely copy this identity:
derive it symbolically and test it numerically.

Establish whether the positive stationary point is unique over the feasible
domain.

For:

    delta_g = 0

derive the closed-form solution and test it against numerical optimization.

The equivalent form should reduce to:

    N* =
      [alpha*A/(beta*B)]^(1/(alpha+beta))
      * (C/kappa)^(beta/(alpha+beta))

with:

    D* = C / (kappa*N*)

subject to the IF3 validity box.

Use this as an executable solver oracle.

If the implemented optimizer cannot reproduce the analytical solution to a
justified floating-point tolerance:

    STOP.


======================================================================
8. THE QUALITY VARIABLE UNDER THE ACCEPTED LAW
======================================================================

This point is scientifically load-bearing.

The accepted classic IF3 has NO empirical Q dependence.

Therefore an imposed quality/preprocessing cost that consumes compute but does
not change the accepted loss law cannot generate an empirically identified
interior quality optimum.

If:

    delta_g(Q) >= 0

and the accepted objective contains no Q benefit, then under the canonical
model the loss-minimizing choice is the minimum-cost admissible Q / baseline
quality setting.

State this as a consequence of the accepted model.

Do NOT misstate it as:

    "higher quality is useless in reality."

The correct conclusion is:

    the accepted cross-scale loss model does not identify a benefit term that
    could justify spending compute on higher Q.

Any nontrivial optimized Q requires an additional benefit model not accepted by
T-008.

Such analysis, if scientifically useful, must be labelled:

    model-conditional sensitivity only

and must never become the canonical Q3 optimum.


======================================================================
9. IF1 / IF2 SCENARIO ANALYSIS
======================================================================

IF1 and IF2 may be used only within their actual scope.

IF1:

- carries seven domain-quality scores;
- carries uncertainty and sample sizes;
- maps only part of the 17-domain mixture;
- mapped mixture mass is approximately 0.5646;
- unmapped domains receive NO invented Q.

IF2:

- is released for absolute use at 1M only;
- A8/A9 cross-scale transfer failed;
- A10/A11 out-of-design transfer failed;
- scale invariance is false;
- 10B/70B extrapolation is not released.

Permitted T-010 use:

- describe how a fixed 1M composition scenario changes IF2 loss;
- attach IF1 quality information to the mapped subset;
- examine quality/preprocessing cost scenarios separately.

Prohibited:

- export IF2 coefficients across model scales;
- infer D- or N-dependent Q effects from IF2;
- infer a cross-scale benefit of quality;
- replace unmapped quality mass with a mean;
- optimize Q by pretending IF1's Q is B7's Q_score.

If quality-cost scenarios are included, keep them visibly separate from the
canonical N-D allocation result.


======================================================================
10. QUALITY-COST FUNCTION SENSITIVITY
======================================================================

If the organizer specifies multiple quality-cost functions, compare them exactly
as specified.

If families such as logarithmic, power or exponential cost are present:

- do not claim a universal ordering between them;
- do not write "log >= power >= exp" or any analogous ordering as a theorem;
- evaluate the ordering numerically only over the declared range;
- disclose crossings.

If the functions require calibration to a common endpoint, slope or baseline
for a fair comparison, use the organizer's calibration if supplied.

If no calibration is supplied, do not silently outcome-tune one.

Any additional normalization must be predeclared, mathematically justified and
reported as sensitivity.

For each scenario report, where defined:

    total compute
    base training share
    context-attention share
    quality/preprocessing share

At:

    Q = Q0

the quality-cost share must be exactly zero if the positive-part formulation
above applies.

Test this explicitly.


======================================================================
11. CONTEXT-LENGTH ANALYSIS
======================================================================

The context-length unit is settled:

    tokens.

The unresolved question is the ANALYSIS SCOPE.

Read C7 / the first-party Q3 context material and establish BEFORE searching
for a threshold:

    supported L_ctx range
    observed or specified grid
    representative operating regimes
    any compute budgets associated with it

Freeze the sensitivity grid before evaluating results.

Separate three concepts:

    1. base-vs-attention cost parity;
    2. a change in the optimizer's N/D allocation regime;
    3. a boundary of the source-supported L_ctx range.

Do not call one of them another.

If a computed transition occurs only at the end of the searched grid:

    report it as a boundary result,
    not an identified interior critical value.

If no transition exists inside support:

    say so.

Do not extend the C7-supported range merely to manufacture a crossing.


======================================================================
12. BUDGET GRID AND VALIDITY
======================================================================

Use compute budgets supported by the Q3 source material.

If a broader illustrative grid is scientifically useful, separate:

    source-supported budgets

from:

    stress / extrapolation budgets.

For every optimum record whether:

    N is inside IF3 validity
    D is inside IF3 validity
    context is inside Q3 source support

Primary quantitative conclusions must remain inside the accepted IF3 box.

If the unconstrained optimum falls outside the box:

- solve the constrained problem correctly;
- identify the active boundary;
- label any outside-box unconstrained solution diagnostic only.

Do not silently clip and report the clipped point as an unconstrained optimum.


======================================================================
13. UNCERTAINTY
======================================================================

Do not report only point-estimate allocation curves.

Propagate accepted classic-IF3 uncertainty where the available upstream object
supports it.

Preserve the distinction between:

    clustered bootstrap
    leave-one-trajectory-out
    profile / identifiability diagnostics

Do not merge these into one pseudo-confidence interval.

At minimum quantify sensitivity of:

    N*
    D*
    minimum predicted loss
    any claimed context transition

to defensible IF3 parameter uncertainty.

If upstream serialization does not carry full draws, use the accepted tracked
uncertainty evidence in a way that does not invent unavailable samples.

State exactly which uncertainty source each band represents.


======================================================================
14. NUMERICAL SOLVER
======================================================================

Implement reusable optimization code under:

    src/alloc/

and task entry points under:

    scripts/q3_*

The solver must support:

    box constraints
    compute feasibility
    exact saturation check
    fixed-Q analytical benchmark
    boundary optima
    deterministic execution

Prefer dimensional reduction where the budget constraint analytically removes
D.

Do not solve a higher-dimensional problem numerically when an exact reduction
is available.

Use a deterministic global or grid-bracketed outer search only where required.

Do not assume unimodality in Q unless it is proven for the exact chosen model.

Where Q is only a scenario axis, do not call the scenario sweep an optimizer.


======================================================================
15. REQUIRED TEST SURFACE
======================================================================

Add executable tests or a self-test covering at minimum:

1. classic IF3 hash and validate receipt;
2. raw N / raw D unit convention;
3. Q sentinel is never read as IF1 Q;
4. IF2 absolute use outside 1M is rejected;
5. B8 is never consumed;
6. compute dimensions reconcile;
7. budget feasibility;
8. budget saturation for interior N-D optima;
9. analytical fixed-Q stationary condition;
10. delta_g = 0 closed-form solution;
11. numerical solver reproduces the closed form;
12. constrained optimum handles N lower/upper boundary;
13. constrained optimum handles D lower/upper boundary;
14. Q = Q0 gives zero quality-cost share;
15. quality-cost shares sum correctly;
16. context parity identity if eta is source-verified;
17. context grid cannot exceed declared support silently;
18. out-of-IF3-box result is labelled / rejected from primary evidence;
19. IF1 unmapped domains never receive imputed quality;
20. no cross-scale IF2 coefficient is consumed.

Where practical include deliberately invalid fixtures proving the guards can
fail.

Never weaken a test to obtain green output.


======================================================================
16. REQUIRED ARTIFACTS
======================================================================

Use Q3-specific names only so concurrent T-011 work cannot collide.

Prefer a compact set such as:

    results/tables/q3-allocation.md
    results/tables/q3-context-sensitivity.md
    results/tables/q3-quality-cost-sensitivity.md

and, only if they support manuscript claims:

    results/figures/q3-allocation.*
    results/figures/q3-budget-share.*
    results/figures/q3-cost-sensitivity.*

Every numerical artifact must be generated by scripts.

Do not hand-edit numerical tables.

Do not create redundant figures merely because the integration plan contains a
placeholder inventory.


======================================================================
17. CLAIM LANGUAGE
======================================================================

Use the evidence hierarchy from paper/INTEGRATION_PLAN.md.

Canonical classic-IF3 optimization:

    model-conditional, not direct observation.

Exact algebraic identities:

    model-conditional.

IF1/IF2 1M scenario:

    bounded descriptive scenario.

Quality-cost family comparison:

    sensitivity analysis.

Outside-box allocation:

    extrapolation.

Do not use causal language.

Do not state that a residual compute-allocation result proves how real model
developers should allocate resources.


======================================================================
18. REPRODUCTION
======================================================================

Create a deterministic reproduction entry point for all tracked T-010 numerical
artifacts.

At minimum require:

- two complete passes produce identical tracked artifacts;
- raw organizer inputs remain unchanged;
- accepted IF1/IF2/IF3 hashes remain unchanged;
- seeds are explicit where randomness is used;
- output line endings are LF;
- recorded runtime equals environment.yml direct pins.

Do not rewrite accepted T-007/T-008 artifacts.


======================================================================
19. REVIEW PACKAGE
======================================================================

Create:

    reviews/T-010/HANDOVER.md

The handover must contain:

- task / owner / branch / Draft PR;
- accepted upstream interface hashes;
- Q3 first-party compute definitions and units;
- context-range provenance;
- exact canonical objective and constraint;
- analytical fixed-Q derivation;
- solver validation against the closed form;
- canonical N-D allocation findings;
- quality-variable disposition under classic IF3;
- IF1/IF2 scenario scope;
- quality-cost sensitivity result;
- context analysis and whether any interior transition exists;
- validity-box handling;
- uncertainty method;
- exact commands run;
- deterministic reproduction result;
- negative findings;
- limitations;
- exact downstream information T-012 may consume.

Do not paste raw data.

Do not include machine-specific absolute paths.


======================================================================
20. WORKLOG
======================================================================

Append one accurate T-010 block to:

    worklog/M2.md

Record:

- what was advanced;
- approximate active effort rounded to 0.5 h;
- validation actually run;
- important negative results / limitations;
- Draft PR state;
- AI assistance and how its output was validated, following repository policy.

Do not rewrite old entries.

Do not use commits, LOC, tokens or prompt count as contribution metrics.


======================================================================
21. GIT AND REVIEW DISCIPLINE
======================================================================

Create a short-lived task branch from synchronized accepted main:

    exp/m2-T010-q3-allocation

Do not work from a stale T-007 branch.

No rebase.
No force-push.

Commit subjects:

    type(scope): lowercase imperative [T-010]

ASCII, one line, <=72 characters.

Before every commit:

    git diff --check

and:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PreCommit

Before push:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PrePush

Before the final review checkpoint:

- fetch current main;
- if main advanced only because of concurrent T-011 or coordination work,
  merge current main into the branch;
- do not rebase;
- if the merge conflicts with another active owner's path, STOP and escalate.

Push the branch and open a Draft PR.

Do NOT merge.

Do NOT mark T-010 done.

Do NOT start T-012.


======================================================================
22. STOP CONDITIONS
======================================================================

STOP rather than improvise if:

- accepted IF1, IF2 or IF3 bytes do not match their reviewed hashes;
- shared interface validation fails;
- Q3 first-party compute semantics cannot be established;
- units cannot be reconciled;
- context range is ambiguous and a threshold claim would depend on guessing it;
- a canonical result requires an unaccepted Q-aware scaling law;
- IF2 would need to be used outside 1M;
- a nontrivial optimized Q requires feeding IF1 Q into B7 gamma;
- B8 would be required;
- the solver disagrees with the analytical fixed-Q benchmark;
- primary claims require operating outside the classic IF3 validity box;
- a necessary change touches src/interfaces.py;
- another active owner's path must be changed;
- raw data would need modification;
- public-safety fails.

A STOP in a quality-related subproblem does NOT invalidate the canonical
classic N-D allocation analysis if that analysis is independently complete.


======================================================================
23. FINAL RESPONSE
======================================================================

Return a concise structured report containing:

1. synchronized main SHA used;
2. final branch HEAD;
3. Draft PR number/state;
4. IF1 receipt/hash;
5. IF2 receipt/hash and 1M scope;
6. classic IF3 receipt/hash and validity box;
7. verified Q3 compute-cost formula and source;
8. fixed-Q analytical benchmark result;
9. numerical solver agreement with the analytical benchmark;
10. canonical N/D allocation result;
11. quality-variable disposition under the accepted classic law;
12. quality-cost sensitivity result;
13. context-length supported range and any identified transition;
14. uncertainty propagation result;
15. validity / extrapolation summary;
16. test summary;
17. deterministic reproduction summary;
18. public-safety result;
19. exact downstream claims T-012 may consume;
20. confirmation T-010 remains under review and unmerged.

End exactly:

T-010 Q3 ALLOCATION COMPLETE — DRAFT PR READY FOR SUPERVISORY REVIEWs
```

### Status

- Archived before execution; outcome **pending** at archival time.
- Evidence belongs in `reviews/T-010/HANDOVER.md` on branch
  `exp/m2-T010-q3-allocation` and its Draft PR.

---

## v2 — supervisory correction and scientific closure

| Field | Value |
| --- | --- |
| Task | T-010 |
| Prompt version | v2 |
| Type | supervisory delta to v1 |
| Level | L2 |
| Issued by | M1 |
| Executor | M2 |
| Archived | 2026-09-26T15:05:14+08:00 |
| Audited PR / head | #17 / `2047c336bf7ac75bf4020b3d79806cc9249bc6b1` |
| Execution status | archived prospectively; execution pending |

### Archival note

This block is appended; the v1 block above is unchanged, including its closing
line as issued. v2 is archived prospectively, before execution, from the issued
M1 archival carrier. Only the canonical text between that carrier's
`BEGIN CANONICAL V2` and `END CANONICAL V2` markers is archived, verbatim; the
carrier's M1 wrapper and the supervisory conversation are not repository
artifacts and are not archived. The carrier was already LF-only, so no
normalisation was applied. **Archival records the instruction only; it is not
scientific acceptance and does not unblock T-012.**

At archival, main was `9543181` and Draft PR #17 was open and unmerged at
`2047c33` with 24 changed files, all within T-010's paths, matching the
supervisory audit anchors. Execution requires a separate L1 launcher.

SHA-256 of the canonical block below (UTF-8, LF, final newline included):
`7a5701523d9037a1ef19cf17380eea8756f3a6ab32b06e3d958310c161fc0218`.

### Canonical execution prompt

```text
T-010 v2 -- SUPERVISORY CORRECTION AND SCIENTIFIC CLOSURE

Task: T-010
Execution owner: M2
Issuer and archival authority: M1
Level: L2
ARCHIVE: YES -- prospective formal supervisory correction

Execution requires successful remote archival by M1 and a subsequent separate
L1 launcher. This is a delta to v1, not a replacement. All compatible v1
requirements remain binding. Do not edit any executed specification block.

1. STARTUP, OWNERSHIP AND FIXED SCIENTIFIC CONTRACT

Read current governance, T-010 v1/v2, PR #17, the current T-010 handover,
accepted T-007/T-008 handovers, q1-if1-summary.md, q1-if2-summary.md,
q2-final-closure.md, q2-classic-fit.md, q2-uncertainty-robustness.md,
src/interfaces.py and the relevant first-party Q3 material.

Require T-008 and T-015 done, T-010 wip, T-011 done, and T-012 todo.
Use the existing exp/m2-T010-q3-allocation branch and PR #17. Fetch current
main and incorporate the prospective archive by a normal merge, not rebase.
Stop on ownership conflicts or conflicting scientific changes.

Allowed edits:
  src/alloc/
  scripts/q3_*
  results/tables/q3-*
  results/figures/q3-*
  reviews/T-010/
  worklog/M2.md

Do not modify TASKS.md, worklog/specs/, environment.yml, shared interfaces,
upstream implementations/results, paper/, or another owner's files.
Read only authorized local inputs without modifying them. Apply the source-security
and provenance gate in section 6 before new scientific execution. The budget
inequality remains the primary mathematical correction; the security clarification
does not justify postponing or weakening it once the source gate is satisfied.

Preserve:
  L(N,D) = E + A*N^(-alpha) + B*D^(-beta)
  raw parameter counts N and raw token counts D;
  the accepted IF3 validity box, read exactly from the immutable object;
  canonical semantic Q = Q0 and delta_g(Q0) = 0;
  C_total = D*[(6 + eta*L_ctx)*N + delta_g(Q)];
  the source-supported representative budgets and observed C7 context grid;
  exogenous L_ctx and the distinct 6/eta cost-parity identity.

No generalized quality law, IF1-to-B7 mapping, unmapped-quality imputation,
cross-scale IF2 use, or B8 scientific consumption is authorized.
No nonbaseline numeric Q0 is introduced. No upstream producer is rerun.
A baseline allocation is model-conditional, not empirical developer guidance.

Use the environment.yml direct pins and verify the actual runtime before
scientific execution: Python 3.11.16, NumPy 2.4.6, SciPy 1.17.1,
pandas 3.0.6, PyYAML 6.0.3. Stop on a mismatch; do not alter the environment
contract merely to pass.

2. CORRECT THE BUDGET INEQUALITY BEFORE ANY OTHER ALLOCATION ANALYSIS

V1 section 6 requires C_total <= C_budget. Budget saturation was derived for
interior optima; it is not a universal feasibility requirement in a bounded
validity box.

Solve the actual canonical problem:
  minimize L(N,D)
  subject to kappa*N*D <= C,
  N_min <= N <= N_max, D_min <= D <= D_max,
  where kappa = 6 + eta*L_ctx.

Establish:
  C_min = kappa*N_min*D_min
  C_box = kappa*N_max*D_max.

For C < C_min, the box contains no compute-feasible allocation.
For C_min <= C < C_box, an optimum saturates the budget.
For C = C_box, the upper corner is feasible and saturating.
For C > C_box, the optimum within the accepted box is (N_max,D_max), with
unused budget C - C_box. This follows from the strictly negative N and D
derivatives for the accepted positive parameters.

An exact general reduction is:
  N in [N_min, min(N_max, C/(kappa*D_min))]
  D_best(N) = min(D_max, C/(kappa*N)).
An equivalent analytically justified implementation is acceptable.

Do not report a large-budget row as NO_VALIDITY_BOX_ALLOCATION solely because
no equality-saturating point exists. The current valid 1e22 allocation is
already a feasibility witness under a 1e24 ceiling. Distinguish genuine
infeasibility from a validity-limited optimum with unused budget.

Retain the closed-form equality oracle only in its applicable regime.
Record all simultaneous active bounds at corners. Report actual compute,
unused compute, utilization, and clearly defined cost-share denominators:
shares of compute spent must not be mistaken for fractions of total budget.

Regenerate every affected 1e24 budget/context row and downstream statement.
The upper-corner result does not establish a global optimum beyond the box or
a physical limit on useful compute. Do not extrapolate to spend the remainder.

3. DEFINE AND IDENTIFY BUDGET-DRIVEN REGIME CHANGES

Verify the exact first-party Q3 wording and record a precise source pointer.
For each fixed observed C7 context, define a regime map R(C) using feasibility,
the complete active set of N/D bounds, and saturation versus budget slack.

A transition means a change in that declared mathematical regime. Identify
actual thresholds, not merely differences between three table rows.

Derive candidate thresholds from the fixed-Q stationary solution reaching
each box bound, together with C_min and C_box. Retain a candidate only if
the adjacent constrained solutions establish a genuine regime change.
Verify just below, at and just above each applicable threshold using
scale-aware tolerances and the corrected optimizer.

The three organizer-supported budgets remain the primary presentation grid.
This v2 additionally authorizes continuous-C mathematical regime analysis
within their declared span, explicitly labelled model-conditional continuation,
not newly observed or organizer-specified budget points. Do not silently
weaken the primary-grid guard or extend the context grid.

Distinguish:
  an interior smooth allocation curve;
  a validity-boundary active-set transition;
  the onset of budget slack at the validity ceiling;
  a boundary of the analysis/source support;
  an empirical physical transition, which is not established here.

Keep 30000 tokens, if source-verified, solely as base/attention cost parity.
Observed context rows may bracket a regime change; they do not identify an
exact continuous context threshold. Propagate uncertainty for any transition
claim actually retained.

4. PROPAGATE THE AVAILABLE JOINT LOO VECTORS

The accepted q2-uncertainty-robustness.md contains eight per-trajectory rows,
each with E, A, alpha, B and beta. Use complete rows together. Do not combine
marginal minima/maxima into invented parameter vectors.

The tracked table prints approximately six significant digits. By default,
propagate those published vectors and explicitly label the result:
  leave-one-trajectory-out robustness at published parameter precision.

A pre-existing, verifiably accepted full-precision object may be read if
available, with provenance recorded. Do not invent one, rerun T-008 to create
one, or modify/re-serialize accepted IF3 bytes.

Freeze the source artifact/ref/hash, preserve dropped-trajectory identifiers,
and validate all eight complete parameter vectors. Keep the accepted validity
box fixed. Use a separately labelled sensitivity parameter representation;
never describe a modified in-memory fit as the byte-locked accepted IF3.

Run the corrected allocation analysis for all primary budget/context pairs.
Report the nominal result, individual LOO outcomes, and LOO min/max for N*,
D* and minimum predicted loss. Report regime agreement/disagreement and LOO
ranges for parameter-dependent transition thresholds.

Geometry-only C_min/C_box thresholds do not acquire parameter uncertainty
when the box and cost coefficients are fixed. Degenerate N/D ranges at a
slack upper corner are legitimate; the predicted loss can still vary.

Disclose published-precision limitations. Assess whether rounding is material
to any very small reported sensitivity or regime claim; do not claim more
resolution than the released evidence supports.

These are robustness ranges, not confidence, bootstrap or posterior intervals.
Retain the separate bootstrap-marginal and profile evidence and their limits.
Do not synthesize joint bootstrap draws or convert profile objective ratios
into allocation confidence limits. Preserve the B1 generator-recovery caveat.

5. COMPLETE RAW QUALITY-COST FAMILY COMPARISON

Separate three questions:
  A. Comparing raw g(Q) functions requires no numeric Q0.
  B. Evaluating D*[g(Q)-g(Q0)]_+ requires an established numeric Q0.
  C. Optimizing nontrivial Q requires an accepted quality-benefit model.

Complete A over the exact first-party-supported Q domain. B remains
BLOCKED_UNTIL_Q0_DECLARED; C remains unsupported by classic IF3.

Verify the three formulas, coefficients and domain from the canonical DOCX
before use. The current receipt records:
  exponential: 1e7*exp(6Q)
  power: 5e9*Q^4
  logarithmic: 2e9*ln(1+10Q)
These are audit expectations, not substitute source evidence.

Numerically identify and bracket all pairwise crossings within the verified
domain; report ordering on each resulting interval and endpoint behavior.
Use analytical/monotonicity support or suitably checked root bracketing so a
coarse grid cannot silently miss a crossing near an endpoint.
If support is (0,1], distinguish the Q->0+ limit from an admissible Q=0 row.

No outcome-tuned normalization, additional calibration, numeric Q0 choice,
or inference from raw-g ordering to delta_g ordering is permitted.
Report units and a deterministic numerical method. Do not interpret cost
families as an accepted cross-scale loss-benefit model.

6. CLOSE SOURCE AND INTERFACE RECEIPTS

Preserve the existing whole-DOCX SHA-256 check. Add value-level verification
and precise locations for:
  the budget inequality and representative budget values;
  training coefficient and eta;
  all quality-family coefficients and the Q domain;
  Q0 source options;
  exogenous context semantics and the structural-transition requirement.

Use actual DOCX paragraph/table/OMML locations and correctly handle exponents.
Formula-name anchors alone, flattened exponent strings, and p72+1/p72+2
assumptions are insufficient for attributing individual numeric values.
Extract or explicitly verify each value against its exact canonical source
span before emitting the receipt. Preserve C7's direct-data provenance.

The source-hardening request does not assert that the current constants are
wrong. If canonical material contradicts the receipt/specification, stop and
report the exact discrepancy to M1; do not silently change scientific scope.

Source-security roles are distinct:
  docs_local/problem-f/source/problem_statement.docx
    Canonical methodological authority.
  docs_local/problem-f/source/data_description.original.pdf
    Quarantined original containing hidden / near-white small-font text.
    Its hidden fragments have ZERO evidentiary weight. The original PDF must
    not be mathematical authority, AI input, or PDF/text-extraction input.
  docs_local/problem-f/audit/data_description.sanitized.m2.pdf
    Derivative intended for safer human inspection, but currently
    NON-AUTHORITATIVE because creation provenance is not fully reconciled
    with the intake receipt. Do not promote it to first-party authority.

Neither PDF is an authorized automated T-010 input. Keep both local-only and
unchanged. Do not move, delete, overwrite, publish, or reopen the quarantined
original to perform this correction. Do not copy either PDF's contents into
prompts, tracked receipts, comments or review packages. Repository-relative
path/status metadata is permissible and conveys no scientific authority.

The user reports that M2 manually opened the original PDF in VS. Record this
as a reported operational source-handling deviation. Opening a file does not
by itself establish text extraction, AI ingestion or scientific reliance.
Record known facts and available workflow evidence without inventing details
about editor extensions, hidden text viewed, or downstream exposure.
Do not invalidate independently sourced results merely because of this event.

The audited PR #17 head has two different behaviors:
  q3_source_receipt.py actually uses the DOCX, C7 CSV and source manifest;
  its original-PDF reference is quarantined-material metadata.
  q3_reproduce.py recursively snapshots PROBLEM_F_SOURCE and hashes each
  file with read_bytes(). If the original PDF is present there, that code
  reads its bytes for an integrity digest.

Binary integrity hashing is not PDF text extraction or scientific evidence
consumption. Nevertheless, do not claim that all T-010 code currently avoids
reading the original PDF. Correct this overly broad traversal.

Replace recursive scanning of unrelated source trees with an explicit list
of actual, authorized Q3 input files resolved through src.paths:
  canonical problem_statement.docx;
  the actual C7 model_architecture_metadata.csv;
  the corresponding organizer source_manifest.json;
  accepted interfaces for their required identity/contract checks;
  the accepted tracked LOO evidence and any other explicitly justified
  Q3 dependency with a recorded role.

Exclude both PDFs from scientific loading, parsing, OCR, text extraction,
AI inputs, integrity-content reads and automatic source traversal. Reject
forbidden paths at actual input boundaries before opening their contents,
including hashing. A dictionary exclusion after read_bytes() is insufficient.

Use harmless temporary fixtures to test these guards; do not inspect either
actual PDF or reproduce its hidden content in a test. Ensure the legitimate
DOCX/C7/interface/LOO workflow still executes with the guard active.

Keep all source files immutable. Report the intentionally narrowed Q3 input
snapshot and its true file count. Do not claim that the former 1,977-file
inventory was revalidated if it was not; preserve that historical receipt in
Git history without carrying its broader count forward as current evidence.

Produce a concise itemized provenance ledger covering every load-bearing
formula, coefficient, budget, context value, quality-cost parameter, threshold
and conclusion. For each, record:
  authorized source or accepted upstream artifact;
  exact file/ref/hash and relevant locator;
  verified value/semantics;
  the derivation or transformation supporting the output;
  verification status and any remaining limitation.

Formula anchors and declared citations alone do not prove the provenance of
hard-coded numbers. Independently verify those values against the authorized
DOCX/attachments/interfaces. No result may rely solely on either PDF,
including through manual transcription, a cached extraction, a copied note
or an AI-produced intermediate. Do not silently relabel such a source.

If any load-bearing dependency on either PDF is found, STOP T-010 scientific
closure and report the affected inputs, results and dependency to M1 before
continuing dependent scientific work. A revised citation alone cannot cure
an unsupported result. Re-derivation from permitted evidence requires an
explicit supervisory disposition. Do not automatically invalidate unrelated,
independently supported results.

If the necessary canonical material is unavailable or a value cannot be
independently traced, mark that item UNVERIFIED and keep acceptance blocked;
do not guess, reopen a PDF as a substitute, or declare contamination without
evidence. A complete authorized-source trace with no PDF-dependent result
allows the manual-opening event to remain an operational deviation.

M3's social-media screenshots are anecdotal corroboration only. They are not
scientific evidence, parameter validation, provenance, or official competition
rules. Do not consume their scaling-law estimates, use them to select/tune
parameters, or put the screenshots into the scientific evidence chain.

Record the source-security audit, the scope of any binary reads, the manual
opening deviation, and the limits of what was verified in HANDOVER and a new
M2 worklog entry. Do not rewrite historical entries or claim knowledge of
unobserved local actions. This correction does not reopen accepted upstream
science without evidence of a specific affected dependency.

Restore v1 section 3's three-interface acceptance gate. IF1/IF2 being merely
PRESENT and unchanged across two runs is insufficient. Require the accepted
bytes and shared-contract validation, including IF2's 1M scope:
  IF1: b8ec99ca38f2e466b427fd1a7d88858048924cc590bbe881a966d141eb9bdeb8
  IF2: 9f2a83da45633858822c8416b5fa70a9537a06b07ac9056e50d871492b16ff91
  IF3: 720efea859d3be3b39ac2ee1976f8adaf7a31b8b5b1a71eb72e8ee8f987c8514

Cross-check expected values with accepted tracked receipts. Require and verify
all three before execution and after reproduction. Never regenerate or
re-serialize a missing/mismatched accepted interface.
Identity/contract checks do not authorize IF1/IF2 as canonical loss inputs.

7. EXECUTABLE VALIDATION

Close v1 section 15 with meaningful positive and negative fixtures.

Cover actual optimizer results, not only boundary-name helper methods:
  interior analytic/numeric agreement;
  each relevant single bound and coincident corners;
  genuine low-budget infeasibility;
  equality at C_min/C_box;
  high-budget feasible upper-corner slack;
  feasibility preservation and non-increasing minimum loss as C increases;
  correct units, cost shares, slack and scale-aware residuals;
  transition thresholds and both neighboring regimes.

Replace the existing high-budget tiny-box "empty validity box" expectation:
under <= that fixture is feasible with slack. This is a correction to the
test's mathematical oracle, not permission to weaken the contract.

Add executable enforcement of:
  IF2 absolute use outside 1M and all IF2 canonical-loss use being rejected;
  B8 never entering scientific input loading;
  no cross-scale IF2 coefficients or imputed IF1 quality;
  IF3 sentinel and semantic-Q0 restrictions;
  rejection of altered/missing required interfaces;
  complete LOO rows, provenance/precision labels and non-CI classification;
  raw-g analysis remaining separate from blocked nonbaseline allocation;
  numeric source-field verification and forbidden receipt mutations;
  both PDF paths rejected before content reads, including integrity hashing;
  the authorized-input workflow operating with PDF read guards active;
  rejection of PDF-only or screenshot-only load-bearing provenance.

Exercise actual consumer/loader boundaries or instrumented input allowlists.
Do not count grep results, an unused guard function, or a helper that can
never affect production execution as enforcement. No new IF2 scenario engine
is required merely to test that unauthorized use is rejected.

8. REPRODUCTION, REVIEW PACKAGE AND RETURN

Generate numerical tables through scripts. Extend q3_reproduce.py to include
all new or changed Q3 artifacts in two complete byte-identical passes.
Require LF-only output, runtime agreement with direct pins, immutable source
inputs, unchanged accepted-interface hashes/contracts, q3_selftest.py,
the shared interface self-test, git diff --check, and actual PreCommit and
PrePush public-safety checks. Record actual commands and results.

Update reviews/T-010/HANDOVER.md in place. Distinguish historical launch base
and implementation evidence head from a current PR snapshot with an explicit
as-of timestamp. Do not chase a self-referential commit SHA in a file that
changes that SHA; report the final live PR head separately after pushing.

Include:
  the corrected inequality solution and high-budget disposition;
  defined/verified budget transitions and claim levels;
  LOO source, precision, ranges and limitations;
  raw-g ordering/crossings and the separate quality gates;
  exact source/interface receipts and the itemized authorized-source ledger;
  source-security gate status, operational deviation and verification limits;
  meaningful test coverage and deterministic reproduction;
  deviations, retained negative results, and permitted T-012 claims.

Append a correction entry to worklog/M2.md; do not rewrite historical entries.
Explain the earlier saturation-only limitation and its scientific consequence
without erasing the old record.

Before final review, fetch main and normally merge permitted coordination
updates; stop on ownership conflicts. Use compliant [T-010] commit subjects,
approved author/committer identity, and no rebase or force-push.

Push only this task branch. Keep PR #17 Draft and unmerged, T-010 wip and
T-012 unstarted. Do not approve, mark ready, merge, or close the task.
A future merge requires a separate fresh scientific and GitHub-side author
metadata review; local user.email alone is not sufficient.

Return the synchronized main and final head, PR state, archival v2 reference,
each correction's evidence, source/interface and source-security receipts,
actual validation, remaining limitations and the precise downstream claim boundary.

End exactly:
T-010 V2 CORRECTIONS COMPLETE -- DRAFT PR #17 AWAITING SUPERVISORY REVIEW
```

### Status

- Archived before execution; outcome **pending** at archival time.
- Evidence belongs in `reviews/T-010/HANDOVER.md` on branch
  `exp/m2-T010-q3-allocation` and Draft PR #17.

---
