You are the primary research-and-implementation agent for M3 on Task T-009.

This is a FORMAL L2 execution stage issued by M1.

Task:

    T-009 — Q4 panel / eligibility / C8 pipeline

Owner:

    M3

Project role:

    M1 is the project coordinator, scientific integration owner, interface
    authority, and continuity/failover owner.

    M3 owns the implementation of T-009 while that ownership remains active in
    TASKS.md.

Your objective is to advance T-009 from its current partially completed state
to a scientifically defensible, reproducible REVIEW checkpoint that provides
the clean Q4 data foundation required by T-011.

Do NOT perform T-011 decomposition, forecasting, or final Q4 inference in this
session.

Do NOT write manuscript prose.

Do NOT modify canonical prompt specifications under `worklog/specs/`.
Those are maintained by M1 only.

The prompt is a specification, not a demand to manufacture a predetermined
answer. If executable evidence contradicts an assumption below, preserve the
evidence, report the contradiction, and use the least-invasive scientifically
defensible alternative within T-009.

======================================================================
0. START FROM THE ACTUAL REPOSITORY STATE
======================================================================

Open the local repository root in Claude Code.

Before making changes:

    git fetch origin
    git checkout main
    git pull --ff-only
    git status
    git log -n 12 --oneline

The working tree must be clean.

Read, in this order:

    README.md
    AGENTS.md
    TASKS.md
    CLAUDE.md
    reviews/README.md
    reviews/HANDOVER_TEMPLATE.md
    worklog/specs/README.md
    worklog/M3.md

Then inspect all existing T-009 work, including at minimum:

    src/panel/
    scripts/q4_panel_*
    results/tables/q4-c8-reconciliation.md
    src/paths.py
    src/interfaces.py

Do not redo already-validated work merely because you would have implemented
it differently.

The current T-009 checkpoint has already established that:

- C8 contains 1,863 model directories;
- 1,860 model records are usable;
- four malformed/truncated JSON files are explicitly logged rather than
  silently dropped;
- exact model-name joining reaches 1,858 / 1,860 C8 records;
- no fuzzy matching is used;
- IFEval, BBH, MUSR and MMLU-PRO reconcile against C1 at the existing
  acceptance threshold;
- MATH Lvl 5 and GPQA do NOT currently reconcile and remain quarantined;
- two earlier implementation defects in MMLU-PRO and MUSR normalization were
  found because the acceptance test genuinely failed and were subsequently
  corrected;
- the existing threshold was NOT weakened to manufacture a PASS.

Treat those as the current verified state.

======================================================================
1. VERIFY LOCAL-ONLY INPUTS
======================================================================

A Git clone does not contain organizer data.

Verify that the canonical local raw data exist under:

    data_local/problem-f/raw/real_attachments/

For this task, verify the required Attachment-C material, especially:

    C1  leaderboard_cleaned.csv
    C2  leaderboard_enhanced.csv
    C3  leaderboard_extended_timeseries.csv
    C4  epoch_all_ai_models.csv
    C7  model_architecture_metadata.csv
    C8  detailed_results/

C9 may be used only as a diagnostic/equivalent representation when useful.
It is not required merely to duplicate C1.

C5/C6 belong primarily to T-011's Loss–Benchmark bridge and should not become
a hidden dependency of T-009.

Resolve all paths through `src/paths.py`.

Never hard-code a local drive letter into tracked source, results, review
packages, or worklogs.

If required local inputs are absent or structurally inconsistent, STOP the
affected subtask and report the exact canonical repository-relative path.

Do not modify raw inputs.

======================================================================
2. VERIFY M3 GIT IDENTITY BEFORE THE FIRST NEW COMMIT
======================================================================

Before committing:

    git config --local --get user.name
    git config --local --get user.email
    git var GIT_AUTHOR_IDENT
    git var GIT_COMMITTER_IDENT

The email must be M3's own GitHub-provided noreply address and should normally
end with:

    @users.noreply.github.com

If the correct address is not already configured and you do not know the exact
value, STOP before committing and ask M3 to configure it.

Never guess an email address.

======================================================================
3. TASK BRANCH AND CONTINUITY REQUIREMENT
======================================================================

Create:

    exp/m3-T009-q4-panel

from the latest `main`.

Push the branch upstream early:

    git push -u origin exp/m3-T009-q4-panel

This project is M1-coordinated and must remain takeover-ready.

Do not keep substantial verified work only on the local machine.

After each major stable checkpoint:

    commit
    validate
    push

A major checkpoint means, for example:

- MATH/GPQA root-cause diagnosis;
- canonical C1/C2/C3/C4 loader;
- model-stratum coverage;
- date convention;
- detailed-task aggregation;
- final T-009 handover.

Do not create meaningless commits merely for frequency.

If execution must stop unexpectedly, the latest useful state should already be
recoverable from the remote branch.

Primary allowed tracked paths:

    src/panel/
    scripts/q4_panel_*
    results/tables/q4-*
    results/figures/q4-*
    reviews/T-009/HANDOVER.md
    worklog/M3.md

Do NOT modify without M1 approval:

    src/interfaces.py
    src/paths.py
    configs/
    environment.yml
    README.md
    AGENTS.md
    TASKS.md
    CLAUDE.md
    paper/
    worklog/specs/
    another member's worklog
    another task's review package

If a necessary change crosses those boundaries, report it to M1 rather than
silently editing the shared path.

======================================================================
4. FIRST PRIORITY — ROOT-CAUSE MATH AND GPQA
======================================================================

The existing C8 reconstruction correctly quarantines:

    MATH Lvl 5
    GPQA

Do not lower the reconciliation tolerance.

Do not introduce ad-hoc per-model corrections.

Do not fit an offset simply to make the dimensions pass.

Investigate whether the discrepancy is caused by a genuine source/run mismatch
rather than a normalization error.

For matched models, examine as much of the following as the supplied data
actually support:

- model identifier;
- evaluation result timestamp;
- submission timestamp;
- task/config metadata;
- task version;
- metric name;
- sample count;
- random-baseline definition;
- raw score;
- normalized score;
- repository/source lineage;
- duplicate or repeated evaluation records.

The first question is:

    Are C1 and C8 describing the same evaluation run/version for the same model?

For MATH specifically, test whether the C8 raw `exact_match = 0` values and the
nonzero C1 summary scores can be reconciled by any legitimate aggregation or
normalization rule.

If not, classify the mismatch as a source/run incompatibility rather than a
normalization failure.

For GPQA, test whether the approximately systematic small offset is consistent
with:

- evaluation-run drift;
- task-version drift;
- differing aggregation definitions;
- a reproducible normalization difference.

Do not choose among these explanations without evidence.

Produce a tracked diagnostic artifact, generated from code, recording:

    status per dimension:
        reconciled
        source-mismatch
        unresolved

    evidence
    sample sizes
    magnitude/distribution of discrepancy
    whether the dimension is usable for C8 detailed-task analysis

If MATH or GPQA cannot be reconciled scientifically, retaining the quarantine is
a valid result.

A truthful PARTIAL result is preferable to a fabricated six-of-six PASS.

======================================================================
5. BUILD A CANONICAL C1/C2/C3/C4 PANEL LOADER
======================================================================

Implement reusable loader/normalization code under:

    src/panel/

The loader must preserve source provenance rather than flattening all tables
into an apparently homogeneous panel.

Inspect the actual schemas before designing joins.

At minimum characterize:

C1:
    primary six-dimension leaderboard summary.

C2:
    enhanced leaderboard representation.

C3:
    extended time-series representation with mixed provenance.

C4:
    broader model metadata / historical scale information.

Determine and document:

- row counts;
- unique model identifiers;
- duplicates;
- missingness;
- relevant date fields;
- parameter-count fields and units;
- model-family fields;
- type/category fields;
- fields available only in some sources;
- deterministic join coverage.

Do not assume C1 and C2 are independent observations merely because they are
different files.

Do not assume C3 rows are all homogeneous observations.

Do not assume C4 model names match leaderboard names directly.

======================================================================
6. DETERMINISTIC JOIN POLICY
======================================================================

Use deterministic matching first.

Order of preference:

1. exact canonical identifier;
2. exact normalized identifier under a documented deterministic normalization;
3. explicit alias map with human-reviewable entries;
4. unmatched.

No blind fuzzy matching.

Do not accept nearest-string matches automatically.

An alias map must be:

- small enough to review;
- explicit;
- evidence-backed;
- stored as tracked project logic/data if used;
- deterministic.

Report for every major join:

    total rows
    matched rows
    unmatched rows
    ambiguous rows
    exact matches
    alias-assisted matches

An ambiguous candidate remains unmatched until resolved.

Do not silently drop unmatched models from coverage denominators.

======================================================================
7. DEFINE MODEL-TYPE STRATA BEFORE ANALYSIS
======================================================================

Maintain at least these explicit strata:

A. pretrained + continuously pretrained
B. chat / instruction-tuned chat
C. fine-tuned
D. model merges
E. other / multimodal

Do not silently discard fine-tuned models.

Do not merge "fine-tuned", "chat", and "merge" into one post-training class at
the data-engineering stage.

Preserve the most specific source label when available.

Create a deterministic classification rule and report:

- total model count;
- count per stratum;
- percentage per stratum;
- parameter-count coverage per stratum;
- date coverage per stratum;
- leaderboard-score coverage per stratum;
- C4 metadata coverage per stratum.

Measure coverage BEFORE selecting any primary analytical population.

Do not choose the primary population simply because it gives the cleanest
regression later.

Model merges should remain a separate analytical stratum unless a defensible
training-compute interpretation exists.

======================================================================
8. OPEN-MODEL ELIGIBILITY
======================================================================

T-011 will require a defensible frontier population.

T-009 must therefore define a reproducible eligibility framework, but it must
NOT yet estimate the technical-progress decomposition.

Define fields/rules that distinguish, where evidence permits:

- open weights / usable public model;
- model type;
- parameter count known/unknown;
- evaluation score availability;
- relevant date availability;
- scale/compute metadata availability;
- obvious duplicate/re-upload variants.

Do not infer "open" merely from appearing on a public leaderboard.

If the supplied data cannot establish a property, encode it as unknown rather
than guessing.

Produce eligibility counts under clearly named criteria.

Keep:

    data engineering eligibility

separate from:

    T-011 primary scientific population choice.

The latter remains a downstream modeling decision.

======================================================================
9. DATE CONVENTION
======================================================================

Q4 is time-dependent, so date semantics must be explicit.

Inspect the available date fields in C1/C2/C3/C4 and establish a deterministic
date hierarchy.

Preferred principle:

- use an actual model publication/release date when available from a
  trustworthy source and deterministically joined;
- otherwise use the best clearly labelled fallback available in the supplied
  data;
- leaderboard submission date is a fallback/proxy, not automatically the model
  release date.

For every analytical row preserve:

    analysis_date
    date_source
    date_confidence or equivalent provenance flag

Quantify:

- fraction using primary release/publication date;
- fraction using fallback submission/evaluation date;
- fraction with no defensible date;
- coverage by model stratum.

Do not silently substitute submission date for release date.

Do not manually invent dates from model names.

If an external first-party source is genuinely required to resolve a small
number of important dates, keep any alias/date mapping explicit and cite its
source. Do not perform uncontrolled web enrichment over thousands of models.

======================================================================
10. C1/C2/C3 RELATIONSHIP AND DUPLICATES
======================================================================

Quantify the relationship among C1, C2 and C3.

Determine which records are:

- exact duplicates;
- enhanced versions of the same observation;
- repeated observations of the same model;
- genuinely distinct time points;
- mixed-provenance historical extensions.

Do not count the same model/evaluation twice merely because it appears in more
than one supplied table.

Create a stable model/evaluation key where the available evidence supports one.

If a unique key cannot be established for some subset, report that limitation.

The canonical panel should preserve enough provenance to reconstruct which
source row contributed each observation.

======================================================================
11. C4 METADATA LINKAGE
======================================================================

Link leaderboard observations to C4 metadata conservatively.

Quantify coverage for at least:

    parameter count
    model family
    publication/release date
    training compute or compute proxy, if present
    organization/developer metadata, if relevant

Never treat missing C4 metadata as zero.

Do not use model-name fuzzy matching to inflate coverage.

If coverage is weak or highly nonrandom across strata, report it explicitly,
because that directly limits T-011's scale-vs-non-scale decomposition.

Produce a coverage table broken down by model stratum.

======================================================================
12. C8 DETAILED-TASK PIPELINE
======================================================================

Preserve the existing successful reconciliation logic.

Do not rewrite it unless a demonstrated defect requires change.

For each reconciled dimension, retain correct random-baseline normalization at
the subtask level before aggregation.

The pipeline must continue to fail closed when it cannot determine a required
multiple-choice baseline.

Preserve and report:

- unusable/corrupted JSON files;
- unmatched model records;
- incomplete dimensions;
- quarantined dimensions.

A damaged record must never disappear silently.

======================================================================
13. REQUIRED GENUINELY DETAILED C8 ANALYSIS
======================================================================

The problem requires at least one meaningful detailed-task aggregation beyond
the six summary scores.

Implement at least one such analysis using ONLY dimensions/subtasks whose
reconciliation status supports use.

A strong default is to construct a model × subtask normalized-score matrix for
the reconciled groups and report, by subtask:

    number of usable models
    missingness
    median score
    IQR or another robust spread
    cross-model variance/dispersion

Then add ONE scientifically useful aggregation, such as:

- identifying which subtasks contribute most to between-model score variance;
- measuring within-model imbalance across subtasks;
- comparing subtask profiles across the declared model strata.

Do not pick the analysis because it creates the strongest-looking effect.

Do not use quarantined MATH/GPQA task records in the primary detailed-task
result unless their reconciliation issue is resolved first.

Keep the analysis descriptive at T-009.
Do not turn it into T-011's technical-progress decomposition.

======================================================================
14. PANEL OUTPUT CONTRACT
======================================================================

The final T-009 loader should be able to produce a canonical local derived panel
without mutating raw data.

Large row-level derived panel files belong under a local ignored derived
location exposed by `src/paths.py`, not under Git.

Tracked output should contain only compact regenerable evidence, for example:

    results/tables/q4-panel-schema.md
    results/tables/q4-panel-coverage.md
    results/tables/q4-model-strata.md
    results/tables/q4-date-coverage.md
    results/tables/q4-c8-reconciliation.md
    results/tables/q4-detailed-task-analysis.md

Exact filenames may differ if the existing naming scheme strongly suggests a
better choice.

Avoid redundant tables.

Every tracked result must be regenerated by a script.

======================================================================
15. SCIENTIFIC CLAIM DISCIPLINE
======================================================================

Preserve the organizer/source provenance of every major field.

In particular:

- C1/C2/C3 are not automatically independent datasets;
- C3 has mixed provenance and must remain labelled as such;
- C4 metadata coverage may be selective;
- leaderboard submission date is not automatically release date;
- fine-tuned/chat/merge models represent different post-training mechanisms;
- a model merge does not have an obvious training-compute interpretation;
- C8 and C1 may come from different leaderboard result generations;
- four reconciled C8 dimensions do not imply six reconciled dimensions;
- a deterministic exact join with lower coverage is preferable to an
  unverifiable fuzzy join with higher coverage.

Do not use causal language in T-009.

Do not forecast.

Do not estimate technical-progress rates.

Those belong to T-011.

======================================================================
16. REPRODUCIBILITY AND IMPLEMENTATION QUALITY
======================================================================

Prefer small importable modules under:

    src/panel/

with no side effects on import.

Use:

    scripts/q4_panel_*.py

as entry points.

All random operations must use the project seed/config system.

If a new result-affecting dependency is genuinely required and would require
editing `environment.yml`, STOP and ask M1 rather than silently modifying that
shared file.

Do not introduce a dependency solely to avoid writing a small transparent
piece of code.

Do not write derived data into:

    data_local/problem-f/raw/

Raw input is read-only.

Do not hard-code numerical conclusions into result-generation scripts.

======================================================================
17. LOAD-BEARING TESTS
======================================================================

Add executable assertions/checks for assumptions that downstream Q4 will rely
on.

At minimum cover, where applicable:

- required source columns;
- duplicate-key behavior;
- deterministic join behavior;
- alias-map uniqueness;
- stratum classification completeness;
- date-source provenance;
- parameter-unit handling;
- C8 task normalization;
- quarantined-dimension exclusion;
- local derived output not written into raw input;
- deterministic regeneration.

Where practical, prove a new check can fail using an in-memory or disposable
fixture.

Do not weaken a check merely to obtain PASS.

======================================================================
18. M1-CENTRIC CONTINUITY / FAILOVER RULE
======================================================================

M3 is responsible for T-009 implementation, but M1 must be able to continue the
project if M3 becomes unavailable.

Therefore:

- keep the remote task branch current at every major stable checkpoint;
- do not leave essential transformations only in an interactive notebook;
- do not depend on undocumented manual edits;
- make derived outputs regenerable from tracked scripts;
- record unresolved assumptions in the handover;
- keep commits conceptually focused;
- avoid local-only state that M1 could not reconstruct from the canonical raw
  package.

If blocked:

1. preserve the last verified state;
2. commit only coherent verified work;
3. push it;
4. update the Draft PR/handover;
5. report the blocker to M1.

Do not improvise across another task's scope merely to keep moving.

M1 may later transfer ownership if a takeover is necessary.

======================================================================
19. REVIEW PACKAGE
======================================================================

Create/update:

    reviews/T-009/HANDOVER.md

using:

    reviews/HANDOVER_TEMPLATE.md

This is the formal supervisory review surface.

It must contain, concisely:

    Task / Owner / Status
    base commit
    branch HEAD
    Draft PR
    completed scope
    tracked evidence
    exact repo-relative commands actually run
    MATH status and root-cause evidence
    GPQA status and root-cause evidence
    C1/C2/C3/C4 relationship
    join coverage
    model-stratum coverage
    eligibility-rule summary
    date convention and date-source coverage
    C4 metadata coverage
    C8 detailed-task result
    quarantined or excluded data
    important negative findings
    known limitations
    downstream constraints for T-011
    exact next action

Do not paste raw AI conversations.

Do not include private reasoning.

Do not include ignored dataset content.

Do not include local absolute paths.

Do not include provider quotas, private endpoints, keys, tokens or account
information.

======================================================================
20. WORKLOG
======================================================================

Append an accurate T-009 block to:

    worklog/M3.md

Record:

- what was advanced;
- outputs;
- commit/PR;
- approximate active effort to 0.5 h;
- AI tool used;
- how the work was validated;
- important blocker/limitation if any.

Do not rewrite prior entries.

Do not treat:

    hours
    commit count
    LOC
    prompt count
    token usage

as contribution scores.

======================================================================
21. VALIDATION BEFORE FORMAL REVIEW
======================================================================

Actually run all relevant pipelines from the task branch.

At minimum verify:

1. the existing C8 reconciliation pipeline still produces the expected
   reconciled/quarantined status;
2. MATH/GPQA diagnostics regenerate;
3. C1/C2/C3/C4 canonical loaders run from raw inputs;
4. deterministic joins regenerate identically;
5. stratum coverage regenerates;
6. date coverage regenerates;
7. C4 metadata coverage regenerates;
8. detailed-task C8 analysis regenerates;
9. all new T-009 tests pass;
10. `python scripts/selftest_interfaces.py` still passes;
11. `git diff --check` passes;
12. `powershell -File scripts/check_public_safe.ps1 -Mode PreCommit` passes;
13. no organizer/raw/local-only data are staged;
14. no local absolute path appears in tracked changes;
15. no file owned by another active task was modified.

Then inspect:

    git status
    git diff --stat
    git diff

semantically.

A mechanically green diff is not enough.

======================================================================
22. COMMIT / PUSH / DRAFT PR
======================================================================

Use focused commit subjects:

    type(scope): lowercase imperative [T-009]

ASCII, one line, <=72 characters.

One conceptual change per commit.

Do not add manual co-author trailers.

Push checkpoints throughout execution.

Open a DRAFT pull request from:

    exp/m3-T009-q4-panel

to:

    main

Prefer opening the Draft PR after the first meaningful verified checkpoint,
then update it as the task progresses.

Do NOT merge it.

Do NOT mark T-009 done.

Do NOT modify TASKS.md.

The Draft PR is for M1/supervisory review.

======================================================================
23. STOP / ESCALATION CONDITIONS
======================================================================

STOP the affected subtask and report evidence if:

- a required C source is missing;
- a documented source role contradicts the actual table;
- a join requires unverifiable fuzzy matching;
- MATH/GPQA reconciliation would require arbitrary correction;
- model strata cannot be defined reproducibly from available evidence;
- date provenance cannot be distinguished reliably;
- C4 metadata coverage is too weak for a downstream claim;
- an existing C8 normalization assumption is disproved;
- a necessary fix requires another owner's/shared path;
- a required new dependency needs `environment.yml`;
- raw data would need modification;
- public-safety validation fails.

A blocked subtask does not invalidate independent completed work.

Preserve partial verified progress and push it before escalation whenever
possible.

======================================================================
24. ACCEPTANCE CRITERIA FOR THIS T-009 CHECKPOINT
======================================================================

A review-ready checkpoint requires:

- canonical C1/C2/C3/C4 loaders;
- deterministic join policy and measured coverage;
- explicit A-E model strata with coverage;
- reproducible open-model eligibility fields/rules;
- explicit date hierarchy and measured date-source coverage;
- C4 metadata coverage by stratum;
- MATH and GPQA either scientifically reconciled or explicitly root-caused /
  quarantined;
- at least one genuinely detailed C8 task-level aggregation beyond the six
  summary dimensions;
- reproducible tracked summary artifacts;
- current `reviews/T-009/HANDOVER.md`;
- current `worklog/M3.md`;
- all relevant validation green except any deliberately quarantined scientific
  mismatch that is explicitly documented.

T-009 may remain WIP if a scientifically material issue is unresolved.

Do not manufacture completion.

======================================================================
25. FINAL RESPONSE
======================================================================

After pushing the latest Draft PR state, return a concise execution report with:

1. starting main HEAD;
2. branch;
3. Draft PR number/link;
4. latest branch HEAD;
5. commits;
6. code/scripts created or modified;
7. tracked artifacts;
8. C1/C2/C3/C4 loader result;
9. deterministic join coverage;
10. model-stratum counts/coverage;
11. eligibility-rule result;
12. date convention and coverage;
13. C4 metadata coverage;
14. MATH root-cause/status;
15. GPQA root-cause/status;
16. C8 detailed-task result;
17. malformed/unmatched/quarantined-data accounting;
18. tests and public-safety result;
19. important negative results;
20. known limitations and exact downstream warnings for T-011;
21. confirmation raw data were not modified;
22. confirmation no local-only organizer data entered Git;
23. confirmation T-009 remains unmerged pending M1 review;
24. exact next action.

If the complete checkpoint is ready, end exactly:

T-009 REVIEW CHECKPOINT READY — DRAFT PR OPEN