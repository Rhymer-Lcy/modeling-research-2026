# T-008 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — generalized quality-aware scaling law

| Field | Value |
| --- | --- |
| Task | T-008 |
| Prompt version | v1 |
| Level | L2 |
| Issued by | M1 |
| Issued | 2026-09-23T14:40:00+08:00 |
| Execution role | A reasoning-oriented coding assistant with repository write access and a scientific Python environment. |

### Objective

Extend the accepted classic N-D scaling law to a quality-aware generalisation,
and advance every component that does not depend on the final numerical content
of IF1/IF2. Waiting for T-007 is not a reason to stop: the mathematics, the
provenance audits, the uncertainty work and the interface adapters can all be
completed and reviewed first.

The stage is explicitly permitted to conclude that a proposed term adds nothing.
A negative result, stated with its evidence, is a result.

### Prerequisites

- The accepted classic baseline: `src/scaling/`, `scripts/q2_fit_classic.py`,
  `results/tables/q2-classic-fit.md`.
- The organizer's attachment-B tables under the canonical local raw path,
  resolved through `src.paths`.
- The interface contract in `src/interfaces.py`.
- IF1/IF2 from T-007 are **not** prerequisites for this stage and must not be
  invented if absent.

### Allowed paths

    src/scaling/
    scripts/q2_*
    scripts/selftest_quality.py
    results/tables/q2-*
    results/figures/q2-*
    reviews/T-008/
    worklog/specs/T-008.md
    worklog/M1.md

Local-only outputs use the canonical derived and interface locations under the
ignored Problem-F data tree.

### Forbidden paths and safety constraints

- Do not modify M2- or M3-owned active paths, or change the ownership or status
  of T-007, T-009 or T-010.
- Do not change the shared interface schema in `src/interfaces.py` for
  convenience; a schema change needs a separately reviewable reason.
- The raw attachment tree is read-only. Derived material is written elsewhere.
- Do not invent IF1/IF2 values to make the pipeline appear complete.
- Do not weaken a failing check to obtain a pass.
- No absolute machine path, provider account, or real identity in tracked files.

### Standing findings this stage must preserve

The principal fit table is described as observed training logs, but its loss
column is numerically consistent, to approximately the rounding quantum, with a
deterministic evaluation of a published law. The supported statement is
**"consistent with formula evaluation / generator recovery"**, never "proven
fabricated", "malicious", or "definitely synthetic".

The consequence is load-bearing and must not be eroded: fitting that table
validates the estimator, not the world. Empirical support for real-model
scaling rests on the independent observed tables, and the narrow intervals the
principal table produces are not a measure of how precisely real model scaling
is known.

### Acceptance criteria

The stage fails if any of the following is not true.

1. The quality-bearing tables are audited for structure and provenance
   **before** a quality law is fitted, and the audit states whether each
   contains independent empirical variation or is predominantly a recovery of a
   supplied generator. Tables of different provenance are never pooled, and a
   table that labels its own rows by provenance is kept stratified.
2. At least two uncertainty diagnostics complementary to the clustered
   bootstrap are reported for every classic parameter, and no single interval
   family is presented as "the" uncertainty. A parameter the data cannot
   identify is exposed as such.
3. The generalized law is implemented as a tested callable. Its three partial
   derivatives are negative throughout the valid region, each is checked
   against a finite difference, and the iso-loss substitution `dN/dQ` is
   negative — better quality must not appear to require more parameters.
4. Parameter saving and equivalent capacity are implemented as distinct
   quantities with the correct signs, and the equivalent-capacity feasibility
   boundary raises an explicit error rather than returning an unexplained NaN.
5. A deliberately invalid fixture is used at least once to demonstrate that the
   sign checks can fail.
6. Every claim separates estimator recovery, internal predictive fit, observed
   external validation, and extrapolation. A quality exponent identified from
   semi-synthetic tables is reported as recovering the supplied mechanism.
7. Large-model tables are role-audited before use; a table of model output is
   never used as fitting data and then reported as agreement.
8. No raw input file is modified, and the public-safety gate passes.

Completing every independent component does **not** make T-008 complete: the
generalized Q/p closure legitimately awaits IF1/IF2, and the task stays `wip`
until then.

### Canonical execution prompt

```text
Run from the intended `modeling-research-2026` repository root.

STAGE B - T-008: GENERALIZED SCALING LAW

B1. START FROM CURRENT MAIN

Verify the existing T-008 baseline rather than rebuilding it. The accepted
baseline includes: the classic form L(N,D) = E + A N^(-alpha) + B D^(-beta);
robust Huber fitting on log Loss; multi-start numerical optimization;
model/trajectory-clustered bootstrap rather than row bootstrap; validation on
the independent observed tables; exclusion of same-family rows from the blind
cross-family validation; and a local-only classic IF3 with Q pinned to 1.

Preserve the provenance finding about the principal fit table described under
"Standing findings" above. Do not weaken or erase the distinction.

B2. USE A FORMAL TASK BRANCH

Create exp/m1-T008-generalized-scaling from the latest main. Push coherent
stable checkpoints. Do not modify another member's active paths.

B3. DO NOT WAIT IDLY FOR T-007

T-007/IF1/IF2 is a dependency for FINAL closure of the generalized Q/p model.
It is NOT a reason to stop. Advance every component independent of final
IF1/IF2 numerical values: structural and provenance diagnostics for the
quality-bearing tables; generator/fingerprint diagnostics; uncertainty analysis
for the classic law appropriate to only eight trajectories; generalized-law
mathematics and a tested callable implementation; quality/substitution/
elasticity formulas; interface adapters that can consume IF1/IF2 later;
large-model extrapolation-role audit; tests and tracked methodology artifacts.

Do NOT invent IF1/IF2 values to make the pipeline complete.

B4. AUDIT THE QUALITY-BEARING TABLES BEFORE FITTING A QUALITY LAW

Inspect the actual schemas and roles from disk; do not trust labels alone. For
each table determine row count, columns, N/D/Q ranges, model/family structure,
duplicate structure, interpolation/calibration flags, whether values are
observed, semi-synthetic, mixed or estimated, any explicit generator
parameters, and the relationship to the other tables.

Run numerical fingerprint diagnostics BEFORE scientific fitting, to answer: do
these tables contain independent empirical variation, or are they
predominantly recoveries of a known/generated formula? Where a table is
explicitly semi-synthetic, generator recovery is expected and should be
reported as such. Where a table mixes calibrated and extrapolated rows,
preserve those subsets separately. Never merge provenance classes into one
apparent validation set. Produce a tracked artifact summarizing this audit.

B5. GENERALIZED QUALITY-AWARE LAW

The principal candidate is

    L(N,D,Q) = E + A N^(-alpha) + B (Q^gamma D)^(-beta)

equivalently E + A N^(-alpha) + B Q^(-gamma*beta) D^(-beta).

Treat this as a model to be tested, not a truth to be forced. E, A, alpha, B,
beta must remain sign-consistent; gamma must have a clear interpretation; Q
must be on the declared IF1 scale; do not silently rescale Q between fits; do
not use outcome leakage to redefine Q; distinguish parameter identifiability
from optimizer convergence. Compare only a compact set of defensible
alternatives if the data require it; do not conduct a large unrestricted
formula search.

B6. COMPOSITION p AND QUALITY Q MUST NOT BE DOUBLE-COUNTED

The final callable is conceptually L(N, D, Q, p), but Q and p are not
automatically independent explanatory variables. When IF1/IF2 arrive,
determine whether composition contributes primarily through Q(p), or through
Q(p) PLUS a validated residual compositional effect. Do not automatically add
both a free Q term and a high-dimensional free p term. If residual p effects
are used, preserve the IF2 contrast basis, zero policy and provenance, use a
low-complexity representation, and test whether the incremental predictive
value survives held-out validation. If p adds no defensible information beyond
Q, the final law may legitimately depend on p only through Q(p). That is a
scientific result, not a failure.

B7. SEMI-SYNTHETIC QUALITY DATA CLAIM DISCIPLINE

Quality dependence recovered from semi-synthetic tables is not automatically
empirical evidence for real-world quality scaling. If gamma is primarily
identified from such tables, say explicitly that the estimator recovers or
calibrates the supplied quality mechanism, rather than that real models
empirically obey this quality exponent. Separate estimator recovery, internal
predictive fit, observed external validation, and extrapolation in every table
and conclusion. Do not allow large semi-synthetic sample counts to dominate the
apparent strength of evidence merely because n is large.

B8. CLASSIC-LAW UNCERTAINTY WITH ONLY EIGHT TRAJECTORIES

The clustered bootstrap is preferable to row resampling but has only eight
clusters, and its narrow intervals are additionally affected by the principal
table's near-deterministic structure. Add at least TWO complementary
diagnostics, preferably leave-one-trajectory-out / leave-one-family-out
stability, and a profile-likelihood or other identifiability profile. For each
of E, A, alpha, B, beta report the full-fit estimate, the cluster-bootstrap
interval, the leave-one-trajectory sensitivity, and profile information where
feasible. Do not present one interval family as "the" uncertainty. If the data
cannot identify a parameter independently, expose that.

B9. VALIDATION HIERARCHY

Preserve a strict evidence hierarchy for the N-D component: primary observed
external checks are the literature table and the cross-family table with
same-family rows excluded; the same table with those rows included is a leakage
contrast only; semi-synthetic tables are calibration; organizer-provided model
outputs are generated/estimated. Do not promote a table to independent
validation merely because a file exists or its values are close. If an
organizer-described file is not physically present in the canonical raw
attachments, record that and do not fabricate it from a description.

B10. LARGE-MODEL EXTRAPOLATION

Inspect the actual large-model files and roles. Establish whether each is
observed input, parameter specification, interpolation, estimated output, or a
target for extrapolation comparison. Do not use an estimated model output as
fitting data and then report agreement with it as validation. For any
large-model prediction carry validity-box status, extrapolation distance,
parameter uncertainty, quality-term provenance, and whether Q or p is observed,
mapped, inferred or unavailable. If IF1/IF2 is not yet available, prepare the
machinery but do not manufacture final Q-dependent numerical claims.

B11. MARGINAL EFFECTS AND ELASTICITIES

Derive and executable-test the relevant partials. The implementation must
satisfy dL/dN < 0, dL/dD < 0 and dL/dQ < 0 in the valid region. Derive and
report interpretable elasticities for N, D and Q, distinguishing the derivative
of total Loss, the derivative of excess reducible Loss, and the log elasticity;
do not present them interchangeably. Use symbolic derivation or independent
finite-difference checks to test signs and formulas.

B12. ISO-LOSS SUBSTITUTION - SIGN CORRECTNESS IS LOAD-BEARING

Along an iso-loss curve dN/dQ = -L_Q / L_N. Because both are negative,
dN/dQ < 0: better quality reduces the N required for the same Loss. Do not
reproduce the common sign error that makes better quality require MORE
parameters.

Where a quality improvement produces a reduction R in the data/quality loss
term, distinguish carefully:

  parameter saving      Delta_N_sub   = [N^(-alpha) + R/A]^(-1/alpha) - N   (negative)
  equivalent capacity   Delta_N_equiv = [N^(-alpha) - R/A]^(-1/alpha) - N   (positive)

the latter finite only when R < A N^(-alpha), diverging as that fails.
Implement domain guards, unit-test the signs and limiting behavior, and do not
hide an infeasible equivalent behind NaN without explanation.

B13. WAITING FOR IF1 / IF2

When the interfaces arrive: validate them against the existing contract; verify
the hashes and provenance reported by their producer; regenerate locally from
tracked producer code when practical; do not consume values blindly from a chat
report; only then finalize the generalized Q/p fit and the final IF3. Until
then T-008 remains WIP, and independent verified progress should still be
pushed.

B14. FINAL IF3 REQUIREMENTS

Do not overwrite the classic IF3 and present it as the generalized result. When
the generalized model is scientifically justified, emit a distinct final IF3
satisfying the existing contract, including functional form, fitted parameters,
covariance or an explicit reason it is unavailable, model-cluster bootstrap
metadata, a validity box for N, D and Q, validation by provenance class,
q-term provenance, and overall provenance. The validity box must reflect the
data that actually identify each term. If Q variation is semi-synthetic, that
fact travels in the q-term provenance. If p is consumed only through Q(p), say
so in the model description.

B15. TRACKED ARTIFACTS

Produce concise, regenerable tracked evidence only. Do not manually edit
generated numerical tables. Large sweeps and intermediates stay in ignored
local space.

B16. TESTING

Add executable tests for the load-bearing mathematics and data roles: fitted
positive parameter-domain constraints; monotonicity signs; finite-difference
agreement for key derivatives; iso-loss substitution sign; equivalent-capacity
feasibility boundary; provenance split of the tables; no validation set
entering tuning; deterministic seed use; interface validation behavior; no
raw-data writes. Where practical use a deliberately invalid fixture once to
confirm a new check can fail. Do not weaken a failing test to make it green.

B17-B21. REVIEW, WORKLOG, GIT DISCIPLINE, VALIDATION, ESCALATION

Maintain reviews/T-008/HANDOVER.md covering the starting main HEAD, branch
HEAD, draft PR, baseline status, the principal table's generator-recovery
finding, observed external validation, the quality-table audit, added
uncertainty diagnostics, the implemented generalized mathematics, substitution
formulas and tests, large-model role audit, what remains blocked on IF1/IF2,
scientific claims refused for now, tracked evidence with exact regeneration
commands, known limitations, and the next action. Do not claim T-008 complete
merely because all independent work is complete.

Append an accurate T-008 block to worklog/M1.md recording work performed,
outputs, approximate active effort to 0.5 h, AI tool, validation actually run,
key negative/provenance findings, and the exact dependency still pending.

Use focused commits `type(scope): lowercase imperative [T-008]`, ASCII, one
line, <= 72 characters, no manual co-author trailers, on the task branch. Open
a DRAFT PR once there is a coherent reviewable checkpoint, and do not merge it
merely to make the branch disappear.

Before reporting a checkpoint, regenerate the evidence and verify: the classic
baseline still reproduces; the principal table's fingerprint still reproduces;
the observed-table metrics still reproduce; uncertainty diagnostics are
deterministic; derivative and substitution tests pass; no raw file changed; no
ignored dataset was staged; no absolute machine path entered Git; no other
member's active path changed. Review the final diff semantically.

Stop the affected subtask rather than improvising if the actual table structure
contradicts the assumptions above; if a table's provenance cannot be resolved
for its intended role; if the quality exponent is non-identifiable; if a
proposed p term merely duplicates Q and cannot be separated; if the large-model
tables would create fit/validation leakage; if IF1/IF2 is unavailable, fails
its contract, or cannot be regenerated; if a scientifically necessary change
requires another member's owned paths or a shared schema change; or if a
public-safety check fails. Push coherent independent progress before escalating
where possible.
```

### Outcome

- Commit / PR: see `reviews/T-008/HANDOVER.md`
- Review package: `reviews/T-008/HANDOVER.md`
- Result: independent work delivered; generalized Q/p closure awaits IF1/IF2,
  so the stage remains open rather than accepted.

---

## v2 — supervisory delta: validity box, claim language, IF3 provenance, B8 probe

| Field | Value |
| --- | --- |
| Task | T-008 |
| Prompt version | v2 |
| Type | supervisory delta |
| Level | L2 |
| Issued by | M1 |
| Issued | 2026-09-23T17:10:00+08:00 |

### Objective

Narrowly scoped correction of three review findings against the v1 checkpoint,
plus one bounded diagnostic. The broader T-008 design is not reopened, the
draft pull request is not merged, and no T-007 output is consumed or inferred
unless a verified IF1/IF2 handover actually exists in the repository.

### Prerequisites

The v1 checkpoint on the T-008 task branch, with its quality module, audit,
uncertainty and substitution artifacts already present.

### Allowed paths

    src/scaling/quality.py
    scripts/q2_substitution.py
    scripts/q2_audit_quality_data.py
    scripts/q2_fit_classic.py
    scripts/selftest_quality.py
    results/tables/q2-substitution.md
    results/tables/q2-quality-data-audit.md
    reviews/T-008/HANDOVER.md
    worklog/specs/T-008.md
    worklog/M1.md

### Forbidden

Reopening the broader design; merging the draft pull request; consuming or
inferring T-007 outputs absent a verified handover; touching another member's
active paths; changing the shared interface schema; overwriting the classic
interface object with a generalized one; relabelling the organizer's source
category dishonestly; weakening an existing tested sign derivation.

### Acceptance criteria

1. Every quality target reported by the substitution artifact satisfies
   `Q_base < Q_target <= Q_max`, with `Q_min`/`Q_max` derived from the fitted
   validity box, duplicates removed after clipping, and a scenario with no
   valid upward target omitted explicitly rather than fabricated. An executable
   assertion proves this over the targets actually emitted.
2. The substitution artifact never calls the quality-aware form "adopted" and
   never says the estimator "recovers the supplied quality mechanism". It
   separates analytical identities conditional on the candidate form from
   numerical illustrations using the misspecified fit, and labels the latter as
   model-internal illustrative sensitivity that is not the final empirical
   substitution result. The mathematics is retained intact.
3. The classic interface object's provenance notes carry, without a schema
   change: the organizer's label for the principal table; the numerical
   fingerprint finding; the consequence that the fitted parameters support
   estimator and generator recovery rather than precise empirical knowledge of
   real scaling; and the statement that empirical external support comes from
   the cross-family table with overlapping rows excluded and from the
   literature table.
4. A compact reversal diagnostic is appended to the existing quality-data audit
   rather than creating a new artifact. Its conclusion is restricted to one of:
   reversal is numerically consistent with a possible opposite-oriented score;
   reversal does not resolve the incompatibility; or the result remains
   ambiguous. It must not conclude that the quarantined table means
   "corruption", "noise", or "one minus quality", and the table stays
   quarantined from the primary generalized fit.

### Canonical execution prompt

```text
Run from the intended `modeling-research-2026` repository root, on the existing
T-008 task branch and its draft pull request.

This is a FORMAL L2 delta issued after supervisory review. Do NOT reopen the
broader T-008 design. Do NOT merge the draft pull request. Do NOT consume or
infer T-007 outputs unless a verified IF1/IF2 handover is actually available in
the repository. Do NOT touch another member's active paths.

1. ARCHIVE THIS AS T-008 v2

Append a new version block to worklog/specs/T-008.md labelled prompt version
v2, type supervisory delta. The archived specification must be
machine-independent and must not contain absolute local paths. Record only the
substantive instructions.

2. FIX OUT-OF-VALIDITY Q TARGETS

In the substitution script, the current target construction can produce Q
values above the fitted validity box, for example 1.05 and 1.35 while the box
ends at Q = 1. Fix this structurally: derive the quality minimum and maximum
from the fitted validity box; require every reported target to satisfy
Q_base < Q_target <= Q_max; do not silently report an extrapolated target as
interpolation; remove duplicates after clipping and filtering; and if no valid
upward target remains, omit that scenario explicitly rather than fabricating
one. Add or extend an executable assertion proving that every reported target
lies inside the fitted validity box. Regenerate the substitution artifact and
verify no target exceeds the ceiling.

3. CORRECT THE SCIENTIFIC LANGUAGE OF THE SUBSTITUTION ARTIFACT

The artifact overstates the status of the quality-aware form. The repository
evidence establishes that the effective-token form is a CANDIDATE, that it is
misspecified on the quality table at storage precision, that the quality
exponent is not a recovered generator constant, that no generalized interface
object has been adopted, and that the numerical fit is only a best
approximation on a designed, unreplicated, semi-synthetic grid.

Revise the script-generated wording and the regenerated artifact so that it
never calls this the adopted form; never says the estimator recovers the
supplied quality mechanism; clearly separates analytical identities conditional
on the candidate form from numerical illustrations using the misspecified fit;
labels numerical marginal and substitution examples as model-internal or
illustrative sensitivity only; and explicitly states they are NOT the final Q2
empirical substitution result. Do not remove the useful mathematics and do not
weaken the already-tested sign derivations.

4. HARDEN CLASSIC INTERFACE PROVENANCE

Inspect the code emitting the existing classic scaling-law interface object.
Without changing the shared schema, ensure its provenance notes carry the
distinction between the organizer's label for the principal training-log table
and the numerical fingerprint showing its loss column is consistent with
deterministic evaluation of the published law down to approximately storage
precision; the consequence that the fitted classic parameters support estimator
and generator recovery rather than precise empirical knowledge of real scaling;
and that empirical external support comes from the cross-family table with
overlapping rows excluded and from the literature table. Do not relabel the
organizer's source category dishonestly, do not change the schema, and do not
overwrite the classic object with a generalized one. Regenerate and validate.

5. ADD ONE BOUNDED SEMANTICS DIAGNOSTIC FOR THE QUARANTINED TABLE

Add a small diagnostic testing whether simple reversible interpretations such
as the complement of the quality score, or rank reversal within the existing
scale, would make the quarantined table's direction consistent with the others.
It may report sign consistency, monotonicity, residual or fingerprint behaviour
under the transformed variable, and whether the transformation merely flips
direction or also improves model compatibility.

STRICT INTERPRETATION RULE: even if a reversed transform fits well, do NOT
conclude that the table means corruption, noise, or one minus quality. The
official materials do not currently establish that semantic interpretation. The
only permitted conclusion is one of: reversal is numerically consistent with a
possible opposite-oriented score; reversal does not resolve the
incompatibility; or the result remains ambiguous. The table must remain
quarantined from the primary generalized fit unless independent provenance
resolves its semantics. Keep the diagnostic compact and append it to the
existing quality-data audit rather than creating artifact sprawl.

6. VALIDATION

Run the substitution, audit, classic-fit, quality self-test and interface
self-test scripts, plus the whitespace check and the public-safety gate in
pre-commit mode. Verify explicitly that no reported quality target exceeds its
fitted validity box; that the substitution wording no longer implies an adopted
or validated quality law; that the classic interface object carries the
provenance caveat; that the quarantined table remains excluded from primary
fitting; that no raw data changed; that no other member's path changed; that no
interface schema changed; and that no absolute machine path entered version
control.

7. UPDATE REVIEW SURFACES

Update the task review package and the coordinator worklog with the v2 delta
only: validity-box bug fixed, claim-language correction, classic interface
provenance hardening, reversal diagnostic result, and validation status. Do not
rewrite prior history. Use focused task commits, push to the existing branch,
and update the draft pull request without merging it.
```

### Outcome

- Commit / PR: see `reviews/T-008/HANDOVER.md`, v2 delta section.
- Review package: `reviews/T-008/HANDOVER.md`
- Result: delta applied; the task remains open pending IF1/IF2.

---

---

## v4 — final IF1/IF2 closure

| Field | Value |
| --- | --- |
| Task | T-008 |
| Prompt version | v4 |
| Level | L2 |
| Stage | final IF1/IF2 consumption, unit closure, model-selection decision, final IF3 decision, review checkpoint |
| Issued by | M1 |
| Execution status | dispatched before archival |

### Archival note

This exact formal instruction was dispatched before being appended here. It is
archived from the original issued prompt, not reconstructed from memory.
**Archival records the instruction only; it does not imply scientific
acceptance or task completion.**

**On the version gap.** No `v3` block exists in this file. No T-008 v3
instruction was ever issued: this task ran v1 (generalized quality-aware
scaling law) and then v2 (supervisory delta). Because no exact v3 source is
available, none was reconstructed, and no approximate historical prompt was
invented to fill the numbering gap — as the v4 instruction itself requires.
The numbering therefore skips from v2 to v4 deliberately.

Only the line endings were normalised to LF, as required for a tracked file in
this repository; the prompt text itself is unaltered. The v1 and v2 blocks
above are preserved byte-for-byte.

### Canonical execution prompt

```text
You are continuing M1 Task T-008 in the EXISTING M1 scientific session.

FORMAL EXECUTION REQUIREMENT
============================

Model:
    Claude Opus 5

Reasoning effort:
    xhigh

Automatic model fallback:
    OFF

If Claude Opus 5 at xhigh is unavailable, STOP and report the execution
constraint. Do not silently continue under another model or lower reasoning
setting.

This is a FORMAL L2 scientific closure stage.

ARCHIVE:
    YES

Task:
    T-008 — Q2 generalized scaling law

Stage:
    final IF1/IF2 consumption, unit closure, model-selection decision,
    final IF3 decision, and review checkpoint

Continue from the EXISTING repository, branch, working tree and Draft PR:

    branch:
        exp/m1-T008-generalized-scaling

    Draft PR:
        #8

Do NOT restart T-008 from scratch.
Do NOT discard the accepted v1/v2 work.
Do NOT rebase or force-push the task branch.

The previous T-008 checkpoint is inherited evidence that must be verified,
not blindly trusted.


======================================================================
0. SCIENTIFIC OBJECTIVE
======================================================================

Close T-008 rigorously now that T-007 is accepted.

The purpose is NOT to force a quality-aware law into existence.

The purpose is to determine, from the actual accepted evidence, what Q2 can
scientifically support and what downstream T-010 / T-011 may legitimately
consume.

The final outcome may be either:

A. a scientifically justified generalized IF3 with an explicitly limited,
   provenance-carrying Q term;

or

B. the classic N-D IF3 as the accepted downstream scaling interface, with the
   Q/p evidence retained separately and explicitly NOT promoted into a
   cross-scale generalized law.

A negative generalized-law decision is a valid completion of T-008.

Do not optimize for producing a more impressive formula.
Optimize for a defensible model/evidence contract.


======================================================================
1. READ BEFORE ACTING
======================================================================

Read, at minimum:

    README.md
    AGENTS.md
    CLAUDE.md
    TASKS.md

    worklog/specs/README.md
    worklog/specs/T-008.md

    reviews/T-008/HANDOVER.md

    src/interfaces.py

    src/scaling/
    scripts/q2_*
    scripts/selftest_quality.py

    results/tables/q2-classic-fit.md
    results/tables/q2-quality-data-audit.md
    results/tables/q2-substitution.md
    results/tables/q2-uncertainty-robustness.md

Then read the ACCEPTED T-007 review/evidence surfaces from current main:

    reviews/T-007/HANDOVER.md
    results/tables/q1-if1-summary.md
    results/tables/q1-if2-summary.md
    results/tables/q1-reproduction.md
    results/tables/q1-a16-coverage.md
    results/tables/q1-mixture-validation.md
    results/tables/q1-cross-scale.md

Inspect the actual accepted local interface objects under the canonical ignored
Problem-F interface directory as resolved through src.paths.

Do not consume values from chat text as authority when the accepted repository
or regenerated interface object can be inspected instead.


======================================================================
2. PROMPT-SPEC GOVERNANCE
======================================================================

This exact instruction is a formal prompt specification and must be archived in:

    worklog/specs/T-008.md

Do not create a separate tracked prompt file.

The tracked canonical policy remains:

    one scientific task -> one append-only prompt-spec file

Do not rewrite v1 or v2.

This instruction is intended as:

    T-008 v4 — final IF1/IF2 closure

If an exact previously issued T-008 v3 instruction is already present in the
canonical file, append this as v4 normally.

If v3 is absent but the EXACT previously issued v3 source is available verbatim
in the current session or an original dispatch source, it may be appended first
as an after-execution omission discovery, explicitly labelled as such and never
reconstructed from memory.

If the exact v3 source is NOT available:

    DO NOT reconstruct it.

Do not invent an approximate historical prompt merely to fill a numbering gap.

Append this exact current instruction as v4 and record only a concise archival
note that no missing historical prompt was reconstructed.

Prompt archival records instructions, not scientific acceptance.


======================================================================
3. SYNCHRONIZE THE EXISTING T-008 BRANCH
======================================================================

Before scientific work:

    git fetch --all --prune
    git status
    git branch --show-current
    git rev-parse HEAD
    git rev-parse origin/main

Verify that you are on:

    exp/m1-T008-generalized-scaling

The task branch was created before the accepted T-007 and T-009 closures.

Merge the latest accepted main into the T-008 branch:

    merge, not rebase
    no force-push
    no history rewrite

Resolve only genuine conflicts.

Do not overwrite T-008 work with stale main content.

After synchronization, verify the accepted main contains:

    T-007 = done
    T-009 = done
    T-008 = wip
    T-010 = todo
    T-011 = todo

Do not start T-010 or T-011.

Do not close T-008 in TASKS.md during this run.
Final task closure belongs to M1 only after supervisory review of PR #8.


======================================================================
4. ACCEPTED T-007 INTERFACE CONTRACT
======================================================================

Validate the accepted IF1 and IF2 objects against the current shared schema.

Verify producer hashes/provenance from the accepted review package and, where
practical, regenerate the interfaces using the accepted T-007 producer code.

The load-bearing accepted facts include:

IF1
---

IF1 is a real complete-case domain-quality interface.

It contains:

- seven observed quality domains;
- the frozen A1-based Q construction;
- native indicator-direction provenance;
- conditional uncertainty;
- partial A16 mapping into the 17-domain mixture system.

A16 coverage is empirical mixture-mass coverage, not mapped-domain count.

The accepted A4 empirical mapped mass is approximately:

    0.564636

Six mixture domains map to observed quality domains.
Eleven mixture domains remain unmapped.

Do not invent quality values for unmapped domains.

Do not treat IF1's conditional intervals as covering:
- cross-source variation;
- method-choice uncertainty;
- unidentified source dependence.

Do not interpret IF1 Q as causal quality.

IF2
---

IF2 is accepted ONLY as:

    READY FOR REVIEW / accepted for integration
    1M SCOPE ONLY

Its fit scale is:

    1M

Its fitting and deterministic selection source is:

    A4/A5 only

Its release validation is:

    A6/A7
    held-out same-scale 1M

Its negative evidence MUST remain active:

    A8/A9 60M absolute transfer:
        FAILED / not certified

    A10/A11 1B out-of-design shape:
        FAILED

    centered normalized RMSE:
        2.207985640835773

    predeclared maximum:
        2.0

    broad release_pass:
        false

    scale_invariance_supported:
        false

    absolute IF2 use outside 1M:
        PROHIBITED

    fitted 10B/70B extrapolation:
        NOT RELEASED

The frozen IF2/model fingerprints must match the accepted producer evidence.

Do not weaken or reinterpret these restrictions.


======================================================================
5. CRITICAL SCALE-SEMANTICS RULE
======================================================================

Do NOT silently equate:

    IF1 Q

with:

    B6/B7 Q_score

merely because both lie on a bounded numerical scale or are called "quality".

These are separate constructs with separate provenance.

Before using one as a numerical substitute for the other, establish whether the
available first-party evidence defines a legitimate semantic and numerical
mapping.

If no such mapping is established:

    do not identify the B7 gamma as an empirical elasticity of IF1 Q.

A monotone relationship, similar range, or convenient normalization is not
sufficient evidence of scale equivalence.

If a comparison is scientifically useful, label it as a diagnostic alignment
or sensitivity only.

Never silently rescale IF1 Q to make B7 fit.


======================================================================
6. PRESERVE THE EXISTING CLASSIC N-D EVIDENCE
======================================================================

Reproduce the accepted classic law and its diagnostics before changing any
final-interface decision.

Preserve the existing finding that the principal B1 loss column is numerically
consistent with deterministic evaluation of the published classic law to
approximately storage precision.

The permitted interpretation remains:

    estimator / generator recovery evidence

not:

    precise empirical knowledge of real-world scaling

and never:

    proof of fabrication or malicious synthetic data

Retain the real external-support hierarchy:

- B4 with overlapping Pythia rows excluded;
- B5 literature evidence;

while:

- B4 with overlapping rows included is a leakage contrast only;
- B6/B7 are quality calibration evidence;
- B8 remains quarantined unless semantics are independently resolved;
- B9 is specification / metadata rather than validation;
- B10 is organizer-provided model output and cannot validate a fit trained
  against itself.

Do not degrade this evidence hierarchy.


======================================================================
7. REASSESS THE GENERALIZED QUALITY-AWARE CANDIDATE
======================================================================

The current candidate is:

    L(N,D,Q) = E + A N^(-alpha) + B (Q^gamma D)^(-beta)

equivalently:

    E + A N^(-alpha) + B Q^(-gamma beta) D^(-beta)

The previous checkpoint established:

- the mathematics and derivative signs are correct;
- B7 contains B6 exactly, so they are not independent datasets;
- B7 gives the correct monotone direction;
- the fitted candidate does NOT reproduce B7 to storage precision;
- gamma is therefore NOT an exact recovered generator constant;
- this finding is a reproduction-accuracy result, not a statistical rejection;
- B8 has the opposite Q direction and remains unresolved.

Do NOT make either of these invalid logical jumps:

INVALID JUMP 1:
    "fingerprint ratio != 1, therefore the candidate is scientifically
     impossible."

INVALID JUMP 2:
    "the candidate has a good-looking fit, therefore real models obey this
     quality exponent."

Instead answer the actual question:

    Is this candidate a useful and defensible approximate scaling model under
    the evidence available, and if so, with what validity and provenance?

Use the existing compact candidate family only.
Do not reopen a broad formula search.

Do not use B8 to select the model.


======================================================================
8. GENERALIZED-LAW ADOPTION GATE
======================================================================

A generalized IF3 may be emitted only if ALL of the following are supported.

GQ1. Parameter-domain validity
------------------------------

The fitted parameters satisfy the intended sign/domain constraints.

gamma is not being "identified" merely because an optimizer returned a number.

Report:
- estimate;
- bounds;
- distance to bounds;
- profile behavior;
- stability diagnostics.

GQ2. Directional stability
---------------------------

The usable quality-bearing evidence supports:

    dL/dQ < 0

under the declared candidate and declared Q semantics.

Do not use B8 to satisfy this gate.

GQ3. Internal predictive usefulness
-----------------------------------

Compare the quality-aware candidate to the corresponding classic/no-Q baseline
on B7 using a blocked or otherwise structure-respecting diagnostic.

Do not perform unrestricted hyperparameter/model search.

Report error reduction and stability honestly.

This diagnostic may support approximate usefulness.
It does NOT convert the semi-synthetic table into observed real-world evidence.

GQ4. Robustness / identifiability
---------------------------------

Use structure-aware sensitivity such as:
- leave-one-Q-level-out;
- leave-one-trajectory / N-D block-out where meaningful;
- profile identifiability;
- existing cluster-based uncertainty where applicable.

Expose instability or non-identifiability.

Do not hide a bound-pinned or weakly identified gamma behind an optimizer
success flag.

GQ5. Real-data consistency check
--------------------------------

Use accepted IF1/IF2 only within their validity to ask whether the real 1M
composition-response evidence is at least directionally compatible with the
candidate's qualitative quality interpretation.

This is a DIAGNOSTIC, not a fresh blind validation set.

A6-A11 are already observed evidence and may not be re-labelled as newly blind
after this decision.

Do not claim that IF2 validates cross-scale Q dependence.

GQ6. Provenance discipline
---------------------------

If the generalized law is adopted, its q-term provenance must state explicitly
that the numerical Q dependence is primarily identified/calibrated from the
usable semi-synthetic quality table, with real IF1/IF2 providing only whatever
limited 1M consistency evidence is actually demonstrated.

GQ7. Validity discipline
------------------------

The generalized validity box must be the intersection of the data that
actually identify the terms.

Do not inherit a broad classic N-D range automatically if the Q term was only
identified on a narrower grid.

Do not include B8's Q range merely because B8 has a Q column.

If any adoption gate fails materially:

    DO NOT emit a generalized final IF3.

Retain the classic IF3 as the accepted downstream scaling interface and state
the failed generalized gate(s) explicitly.


======================================================================
9. Q(p) VERSUS RESIDUAL COMPOSITION p
======================================================================

Use IF1 and IF2 to resolve the conceptual question from the original T-008
specification:

    Does composition p contribute primarily through Q(p), or is there
    defensible residual composition information beyond Q?

This stage must be especially conservative because:

- IF1 maps only part of the 17-domain mixture mass;
- five A4 designs have zero mapped quality mass;
- eleven mixture domains are unmapped;
- IF2 is valid only at 1M;
- IF2 is a composition-response surface, not a causal experiment;
- A6/A7 has already been used for IF2 release validation.

Construct Q(p) only from the documented A16 mapping and accepted IF1 values.

For zero mapped mass:

    Q(p) is undefined.

Do not impute a corpus quality value.

For partial mapped mass:

report both:
- mapped mass;
- mapped-mass conditional Q(p).

Do not silently pretend the mapped subset is the whole mixture.

Perform only low-complexity diagnostics needed to answer the scientific
question.

Possible diagnostics include:

- relationship between mapped-mass Q(p) and the frozen 1M IF2 response;
- whether a Q-only summary explains meaningful variation in the frozen IF2
  surface;
- whether substantial residual p structure remains after Q(p);
- stability with respect to mapped-mass coverage;
- target-by-target sign consistency.

Do NOT outcome-search a new high-dimensional p model.

Do NOT use this stage to re-select IF2.

Do NOT claim causal mediation.

If residual p effects remain large, record that Q is not a sufficient summary
of composition at 1M.

If Q explains much of the response, record that as a 1M descriptive finding,
not proof of scale-invariant quality mediation.

Under no circumstance may the 1M IF2 coefficients be exported as cross-scale p
effects.


======================================================================
10. B8 REMAINS QUARANTINED UNLESS PROVEN OTHERWISE
======================================================================

Preserve the existing B8 findings:

- supplied Q direction is opposite to B6/B7 in every audited cell;
- complement/rank reversal flips the sign;
- transformed fits push gamma to its upper bound;
- fingerprint ratios remain in the thousands;
- 362 rows lie at the exact 0.5 loss floor.

The permitted conclusion remains:

    a reversed interpretation is directionally compatible with a possible
    opposite-oriented score, but the semantic meaning remains unresolved.

Do NOT conclude that B8 means:
- corruption;
- noise;
- 1 - quality;
- badness;
- any other semantic interpretation

without independent first-party provenance.

Unless such provenance now exists in accepted source material:

    B8 stays quarantined.

Do not use B8:
- in primary parameter estimation;
- to enlarge the validity box;
- to claim replication;
- to validate gamma.


======================================================================
11. G2-U — UNIT EQUIVALENCE IS A HARD GATE
======================================================================

The externally consumed IF3 unit convention MUST be:

    N = raw parameter count
    D = raw token count

not "billions".

Internal fitting may use scaled coordinates such as billions for numerical
conditioning, but then ALL of the following must transform consistently:

- N;
- D;
- A;
- B;
- validity ranges;
- covariance / uncertainty representation where applicable;
- any stored reference values;
- any prediction helper.

For a generic internal scaling:

    N_internal = N_raw / s_N
    D_internal = D_raw / s_D

the classic law:

    L = E + A_raw N_raw^(-alpha) + B_raw D_raw^(-beta)

is equivalent to:

    L = E
        + A_internal N_internal^(-alpha)
        + B_internal D_internal^(-beta)

where:

    A_internal = A_raw * s_N^(-alpha)
    B_internal = B_raw * s_D^(-beta)

and conversely:

    A_raw = A_internal * s_N^(alpha)
    B_raw = B_internal * s_D^(beta)

For the quality-aware candidate:

    L = E + A N^(-alpha) + B (Q^gamma D)^(-beta)

Q is dimensionless and the same D transformation applies to B.

Do NOT hard-code s_N = s_D = 1e9 unless that is the actual implemented
internal convention.

Derive the transformation from the implementation.

Add an executable prediction-invariance test.

At minimum compare raw-unit and internal-unit predictions over:

- validity-box corners;
- several deterministic interior points;
- any accepted benchmark/example points.

Require numerical equality to an explicitly justified floating-point tolerance.

Also test validity-box round-trip conversion.

The serialized external IF3 must use raw N / raw D consistently.

If parameters are internally fitted in billions, transform them before external
serialization.

No mixed-unit interface is acceptable.

If G2-U fails:

    T-008 cannot close.


======================================================================
12. UNCERTAINTY AND IDENTIFIABILITY
======================================================================

Preserve and regenerate the classic uncertainty surface:

- full-fit estimate;
- model/trajectory clustered bootstrap;
- leave-one-trajectory-out;
- profile / identifiability diagnostic.

The principal table has only eight trajectories.
Do not overinterpret narrow intervals.

If a generalized law is adopted, provide analogous defensible diagnostics for
the Q term where the data structure permits.

Do not invent precision where the designed semi-synthetic grid cannot support
it.

Do not present bootstrap, profile and leave-one-out intervals as interchangeable
objects.

State what each measures.


======================================================================
13. FINAL IF3 DECISION
======================================================================

Make one explicit final decision.

CASE A — generalized IF3 adopted
--------------------------------

Only if all generalized adoption gates pass:

- keep the existing classic IF3 unchanged as a retained baseline artifact;
- emit a DISTINCT generalized IF3;
- do not overwrite the classic object;
- use the existing shared IF3 schema;
- external N and D units are raw;
- validity_box contains N, D and Q supported by the identifying evidence;
- q_term_provenance clearly records semi-synthetic identification plus any
  limited real 1M consistency diagnostic;
- bootstrap metadata uses the correct structural resampling unit;
- validation is stratified by provenance;
- no B8 evidence enters the accepted fit;
- no 1M IF2 composition coefficient is represented as a cross-scale effect.

The functional-form string must accurately describe whether p enters:
- only through Q(p);
- separately as a residual 1M diagnostic;
- or not at all.

Do not encode a high-dimensional p response into IF3 unless the schema and
evidence genuinely support it.

CASE B — generalized IF3 not adopted
------------------------------------

If the evidence does not justify a generalized interface:

- make that the formal scientific decision;
- retain classic IF3 as the accepted downstream scaling interface;
- clearly identify it as the final T-008 interface for downstream use;
- do not manufacture a generalized object merely to satisfy the original plan;
- carry the tested quality-aware mathematics as analytical/sensitivity
  machinery only;
- carry IF1 and 1M-scoped IF2 separately under their own validity;
- state explicitly that downstream tasks may NOT synthesize those pieces into
  an empirically validated cross-scale Q law.

A scientifically justified negative result is sufficient to close T-008.


======================================================================
14. DOWNSTREAM CONTRACT FOR T-010 AND T-011
======================================================================

The T-008 handover must say exactly what future tasks may consume.

For T-010, state:

- which IF3 object is canonical;
- its external units;
- validity box;
- whether Q dependence is part of the accepted scaling law;
- if Q is not accepted in IF3, what quality/composition information remains
  scenario-only or 1M-only;
- which extrapolations are prohibited.

For T-011, state:

- which scaling predictions may be compared to historical/frontier data;
- whether a Q-aware term is empirical, semi-synthetic, diagnostic, or absent;
- the B8 quarantine;
- the fact that 1M IF2 does not establish cross-scale composition transfer.

Do not allow downstream code to infer a stronger claim from file existence than
the handover permits.


======================================================================
15. TRACKED EVIDENCE
======================================================================

Prefer extending existing q2 artifacts rather than creating redundant files.

At minimum the final review surface must make it easy to inspect:

- classic fit and provenance;
- classic external validation hierarchy;
- quality-table audit;
- B8 quarantine;
- generalized-candidate fit status;
- generalized adoption-gate decision;
- Q(p) / residual-p diagnostic;
- G2-U unit equivalence;
- uncertainty / identifiability;
- final IF3 identity and validity;
- exact downstream restrictions.

If one new concise closure table is useful, use a Q2-prefixed tracked artifact
under:

    results/tables/

and generate it from code.

Do not manually edit generated numerical values.

Large exploratory sweeps remain ignored/local.


======================================================================
16. EXECUTABLE TESTS
======================================================================

Extend tests only where needed.

The final executable surface must include guards for:

1. classic baseline reproduction;
2. generalized derivative signs;
3. iso-loss sign correctness;
4. parameter-saving and equivalent-capacity transforms;
5. equivalent-capacity infeasibility boundary;
6. B8 quarantine;
7. gamma lower AND upper bound detection;
8. validity-box quality targets;
9. IF1 contract validation;
10. IF2 contract validation;
11. IF2 1M-only scope receipt;
12. no use of IF2 as cross-scale absolute response;
13. no silent IF1-Q / B7-Q scale equivalence;
14. Q(p) undefined when mapped mass is zero;
15. raw/internal unit prediction invariance;
16. validity-box unit round trip;
17. final IF3 `.validate()`;
18. deterministic regeneration;
19. no raw-data writes;
20. no held-out partition entering model selection.

Where practical, use mutation/invalid fixtures to prove the new guards can
actually fail.

Do not weaken a test to obtain green output.


======================================================================
17. REPRODUCTION AND SAFETY
======================================================================

Run the complete relevant T-008 validation surface in the intended project
environment.

At minimum:

    python scripts/q2_fit_classic.py
    python scripts/q2_audit_quality_data.py
    python scripts/q2_uncertainty.py
    python scripts/q2_substitution.py
    python scripts/selftest_quality.py
    python scripts/selftest_interfaces.py

plus any new final-closure / unit-invariance / IF1-IF2 integration entry points
you add.

Use the actual project Python environment.

Regenerate the final artifacts from accepted inputs.

Verify:

    git diff --check

and:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PreCommit

Before push also run:

    powershell -NoProfile -ExecutionPolicy Bypass `
      -File scripts/check_public_safe.ps1 -Mode PrePush

Verify no:
- raw organizer input changed;
- local-only dataset was staged;
- absolute machine path entered tracked content;
- credential/provider information entered tracked content;
- M2/M3 active path was modified;
- shared interface schema changed.

If a shared schema change appears scientifically necessary:

    STOP and escalate.

Do not silently modify src/interfaces.py.


======================================================================
18. UPDATE THE T-008 REVIEW PACKAGE
======================================================================

Update:

    reviews/T-008/HANDOVER.md

Do not rewrite history.

Preserve the previous negative findings and add a clearly separated final
closure section.

The final handover must include:

- task / owner / branch / Draft PR;
- synchronized main base;
- final branch HEAD;
- accepted T-007 IF1 hash/provenance check;
- accepted T-007 IF2 hash/scope check;
- classic law status;
- B1 fingerprint interpretation;
- B4/B5 external evidence;
- B6/B7 relationship;
- B8 quarantine;
- generalized candidate result;
- whether gamma is adopted, diagnostic, weakly identified, or rejected;
- Q(p) / residual-p finding;
- explicit statement that IF2 is 1M only;
- G2-U transformation and invariance result;
- final IF3 path;
- final IF3 SHA-256;
- final functional form;
- external units;
- validity box;
- uncertainty methods;
- exact commands run;
- deterministic reproduction status;
- scientific limitations;
- exact downstream permissions/prohibitions for T-010/T-011.

Do not call a semi-synthetic Q exponent an empirical law of real models.

Do not erase negative results.


======================================================================
19. WORKLOG
======================================================================

Append one accurate T-008 final-closure block to:

    worklog/M1.md

Record:

- what was advanced;
- approximate active effort rounded to 0.5 h;
- Claude Opus 5 / xhigh as the formal execution configuration;
- validation actually run;
- final generalized-law decision;
- G2-U result;
- important negative result / limitation;
- Draft PR state.

Do not rewrite previous entries.

Do not use:
- LOC;
- commit count;
- token count;
- prompt count

as standalone contribution metrics.


======================================================================
20. COMMIT / PUSH / PR DISCIPLINE
======================================================================

Use focused commits:

    type(scope): lowercase imperative [T-008]

ASCII, one line, <=72 characters.

Do not add manual co-author trailers.

Push to the existing branch:

    exp/m1-T008-generalized-scaling

No force-push.

Update existing Draft PR #8.

Do NOT merge PR #8.
Do NOT mark T-008 done.
Do NOT edit TASKS.md to close T-008.

The purpose of this run is to produce the FINAL REVIEWABLE T-008 checkpoint.

Update the PR body so it no longer says IF1/IF2 are unavailable.

The PR body must accurately summarize:

- accepted T-007 consumption;
- final model decision;
- classic/generalized status;
- B8 status;
- unit convention;
- final IF3;
- validation;
- negative results;
- downstream limitations.

Keep PR #8 Draft and unmerged pending supervisory review.


======================================================================
21. STOP CONDITIONS
======================================================================

STOP the affected scientific subtask rather than improvising if:

- accepted IF1 or IF2 fails its contract;
- accepted hashes/provenance cannot be reconciled;
- IF1 Q and B7 Q_score require an unsupported semantic mapping;
- the generalized Q exponent remains materially non-identifiable;
- generalized adoption requires B8;
- a p term would duplicate Q without separable evidence;
- a proposed residual-p model requires reusing seen validation as though blind;
- IF2 would need to be used outside 1M;
- G2-U raw/internal predictions are not invariant;
- final IF3 would mix raw and billion-unit conventions;
- a necessary schema change touches shared src/interfaces.py;
- raw inputs would need modification;
- another member's owned path would need modification;
- public-safety fails.

A STOP in one generalized-law route does NOT invalidate the already accepted
classic IF3.

If the generalized route fails but the classic IF3 is valid and fully closed,
record the negative result and complete T-008 with the classic interface.


======================================================================
22. FINAL RESPONSE
======================================================================

Return a concise structured report containing:

1. synchronized main SHA used;
2. final T-008 branch HEAD;
3. Draft PR #8 state and head;
4. IF1 verification result and hash;
5. IF2 verification result, hash, and exact 1M scope;
6. classic IF3 status;
7. generalized candidate decision:
       ADOPTED
       or
       NOT ADOPTED
8. exact reason for that decision;
9. gamma status, if fitted;
10. Q(p) / residual-p finding;
11. B8 status;
12. G2-U raw/internal prediction-invariance result;
13. final canonical IF3 path;
14. final IF3 SHA-256;
15. external N/D unit convention;
16. final validity box;
17. uncertainty diagnostics run;
18. test / reproduction summary;
19. public-safety / whitespace result;
20. downstream permissions and prohibitions for T-010 and T-011;
21. confirmation T-008 remains wip and PR #8 remains Draft/unmerged.

End exactly:

T-008 FINAL CLOSURE COMPLETE — DRAFT PR #8 READY FOR SUPERVISORY REVIEW
```

### Status

- Dispatched to the existing T-008 task branch and Draft PR #8.
- Execution and supervisory review outcome **pending** at archival time.
- Final scientific evidence belongs in `reviews/T-008/HANDOVER.md`.
- T-008 remains `wip`; Draft PR #8 remains the review surface.

---
