# T-008 execution specifications

One file per task. **Append** a new version block for each formal (L2 or L3)
stage; never edit a block that has already been executed. Only M1 writes here.

---

## v1 — generalized quality-aware scaling law

| Field | Value |
| --- | --- |
| Task | T-008 |
| Prompt version | v1 |
| Level | L2 |
| Issued by | M1 |
| Issued | 2026-09-23T14:40:00+08:00 |
| Execution role | A reasoning-oriented coding assistant with repository write access and a scientific Python environment. |

### Objective

Extend the accepted classic N-D scaling law to a quality-aware generalisation,
and advance every component that does not depend on the final numerical content
of IF1/IF2. Waiting for T-007 is not a reason to stop: the mathematics, the
provenance audits, the uncertainty work and the interface adapters can all be
completed and reviewed first.

The stage is explicitly permitted to conclude that a proposed term adds nothing.
A negative result, stated with its evidence, is a result.

### Prerequisites

- The accepted classic baseline: `src/scaling/`, `scripts/q2_fit_classic.py`,
  `results/tables/q2-classic-fit.md`.
- The organizer's attachment-B tables under the canonical local raw path,
  resolved through `src.paths`.
- The interface contract in `src/interfaces.py`.
- IF1/IF2 from T-007 are **not** prerequisites for this stage and must not be
  invented if absent.

### Allowed paths

    src/scaling/
    scripts/q2_*
    scripts/selftest_quality.py
    results/tables/q2-*
    results/figures/q2-*
    reviews/T-008/
    worklog/specs/T-008.md
    worklog/M1.md

Local-only outputs use the canonical derived and interface locations under the
ignored Problem-F data tree.

### Forbidden paths and safety constraints

- Do not modify M2- or M3-owned active paths, or change the ownership or status
  of T-007, T-009 or T-010.
- Do not change the shared interface schema in `src/interfaces.py` for
  convenience; a schema change needs a separately reviewable reason.
- The raw attachment tree is read-only. Derived material is written elsewhere.
- Do not invent IF1/IF2 values to make the pipeline appear complete.
- Do not weaken a failing check to obtain a pass.
- No absolute machine path, provider account, or real identity in tracked files.

### Standing findings this stage must preserve

The principal fit table is described as observed training logs, but its loss
column is numerically consistent, to approximately the rounding quantum, with a
deterministic evaluation of a published law. The supported statement is
**"consistent with formula evaluation / generator recovery"**, never "proven
fabricated", "malicious", or "definitely synthetic".

The consequence is load-bearing and must not be eroded: fitting that table
validates the estimator, not the world. Empirical support for real-model
scaling rests on the independent observed tables, and the narrow intervals the
principal table produces are not a measure of how precisely real model scaling
is known.

### Acceptance criteria

The stage fails if any of the following is not true.

1. The quality-bearing tables are audited for structure and provenance
   **before** a quality law is fitted, and the audit states whether each
   contains independent empirical variation or is predominantly a recovery of a
   supplied generator. Tables of different provenance are never pooled, and a
   table that labels its own rows by provenance is kept stratified.
2. At least two uncertainty diagnostics complementary to the clustered
   bootstrap are reported for every classic parameter, and no single interval
   family is presented as "the" uncertainty. A parameter the data cannot
   identify is exposed as such.
3. The generalized law is implemented as a tested callable. Its three partial
   derivatives are negative throughout the valid region, each is checked
   against a finite difference, and the iso-loss substitution `dN/dQ` is
   negative — better quality must not appear to require more parameters.
4. Parameter saving and equivalent capacity are implemented as distinct
   quantities with the correct signs, and the equivalent-capacity feasibility
   boundary raises an explicit error rather than returning an unexplained NaN.
5. A deliberately invalid fixture is used at least once to demonstrate that the
   sign checks can fail.
6. Every claim separates estimator recovery, internal predictive fit, observed
   external validation, and extrapolation. A quality exponent identified from
   semi-synthetic tables is reported as recovering the supplied mechanism.
7. Large-model tables are role-audited before use; a table of model output is
   never used as fitting data and then reported as agreement.
8. No raw input file is modified, and the public-safety gate passes.

Completing every independent component does **not** make T-008 complete: the
generalized Q/p closure legitimately awaits IF1/IF2, and the task stays `wip`
until then.

### Canonical execution prompt

```text
Run from the intended `modeling-research-2026` repository root.

STAGE B - T-008: GENERALIZED SCALING LAW

B1. START FROM CURRENT MAIN

Verify the existing T-008 baseline rather than rebuilding it. The accepted
baseline includes: the classic form L(N,D) = E + A N^(-alpha) + B D^(-beta);
robust Huber fitting on log Loss; multi-start numerical optimization;
model/trajectory-clustered bootstrap rather than row bootstrap; validation on
the independent observed tables; exclusion of same-family rows from the blind
cross-family validation; and a local-only classic IF3 with Q pinned to 1.

Preserve the provenance finding about the principal fit table described under
"Standing findings" above. Do not weaken or erase the distinction.

B2. USE A FORMAL TASK BRANCH

Create exp/m1-T008-generalized-scaling from the latest main. Push coherent
stable checkpoints. Do not modify another member's active paths.

B3. DO NOT WAIT IDLY FOR T-007

T-007/IF1/IF2 is a dependency for FINAL closure of the generalized Q/p model.
It is NOT a reason to stop. Advance every component independent of final
IF1/IF2 numerical values: structural and provenance diagnostics for the
quality-bearing tables; generator/fingerprint diagnostics; uncertainty analysis
for the classic law appropriate to only eight trajectories; generalized-law
mathematics and a tested callable implementation; quality/substitution/
elasticity formulas; interface adapters that can consume IF1/IF2 later;
large-model extrapolation-role audit; tests and tracked methodology artifacts.

Do NOT invent IF1/IF2 values to make the pipeline complete.

B4. AUDIT THE QUALITY-BEARING TABLES BEFORE FITTING A QUALITY LAW

Inspect the actual schemas and roles from disk; do not trust labels alone. For
each table determine row count, columns, N/D/Q ranges, model/family structure,
duplicate structure, interpolation/calibration flags, whether values are
observed, semi-synthetic, mixed or estimated, any explicit generator
parameters, and the relationship to the other tables.

Run numerical fingerprint diagnostics BEFORE scientific fitting, to answer: do
these tables contain independent empirical variation, or are they
predominantly recoveries of a known/generated formula? Where a table is
explicitly semi-synthetic, generator recovery is expected and should be
reported as such. Where a table mixes calibrated and extrapolated rows,
preserve those subsets separately. Never merge provenance classes into one
apparent validation set. Produce a tracked artifact summarizing this audit.

B5. GENERALIZED QUALITY-AWARE LAW

The principal candidate is

    L(N,D,Q) = E + A N^(-alpha) + B (Q^gamma D)^(-beta)

equivalently E + A N^(-alpha) + B Q^(-gamma*beta) D^(-beta).

Treat this as a model to be tested, not a truth to be forced. E, A, alpha, B,
beta must remain sign-consistent; gamma must have a clear interpretation; Q
must be on the declared IF1 scale; do not silently rescale Q between fits; do
not use outcome leakage to redefine Q; distinguish parameter identifiability
from optimizer convergence. Compare only a compact set of defensible
alternatives if the data require it; do not conduct a large unrestricted
formula search.

B6. COMPOSITION p AND QUALITY Q MUST NOT BE DOUBLE-COUNTED

The final callable is conceptually L(N, D, Q, p), but Q and p are not
automatically independent explanatory variables. When IF1/IF2 arrive,
determine whether composition contributes primarily through Q(p), or through
Q(p) PLUS a validated residual compositional effect. Do not automatically add
both a free Q term and a high-dimensional free p term. If residual p effects
are used, preserve the IF2 contrast basis, zero policy and provenance, use a
low-complexity representation, and test whether the incremental predictive
value survives held-out validation. If p adds no defensible information beyond
Q, the final law may legitimately depend on p only through Q(p). That is a
scientific result, not a failure.

B7. SEMI-SYNTHETIC QUALITY DATA CLAIM DISCIPLINE

Quality dependence recovered from semi-synthetic tables is not automatically
empirical evidence for real-world quality scaling. If gamma is primarily
identified from such tables, say explicitly that the estimator recovers or
calibrates the supplied quality mechanism, rather than that real models
empirically obey this quality exponent. Separate estimator recovery, internal
predictive fit, observed external validation, and extrapolation in every table
and conclusion. Do not allow large semi-synthetic sample counts to dominate the
apparent strength of evidence merely because n is large.

B8. CLASSIC-LAW UNCERTAINTY WITH ONLY EIGHT TRAJECTORIES

The clustered bootstrap is preferable to row resampling but has only eight
clusters, and its narrow intervals are additionally affected by the principal
table's near-deterministic structure. Add at least TWO complementary
diagnostics, preferably leave-one-trajectory-out / leave-one-family-out
stability, and a profile-likelihood or other identifiability profile. For each
of E, A, alpha, B, beta report the full-fit estimate, the cluster-bootstrap
interval, the leave-one-trajectory sensitivity, and profile information where
feasible. Do not present one interval family as "the" uncertainty. If the data
cannot identify a parameter independently, expose that.

B9. VALIDATION HIERARCHY

Preserve a strict evidence hierarchy for the N-D component: primary observed
external checks are the literature table and the cross-family table with
same-family rows excluded; the same table with those rows included is a leakage
contrast only; semi-synthetic tables are calibration; organizer-provided model
outputs are generated/estimated. Do not promote a table to independent
validation merely because a file exists or its values are close. If an
organizer-described file is not physically present in the canonical raw
attachments, record that and do not fabricate it from a description.

B10. LARGE-MODEL EXTRAPOLATION

Inspect the actual large-model files and roles. Establish whether each is
observed input, parameter specification, interpolation, estimated output, or a
target for extrapolation comparison. Do not use an estimated model output as
fitting data and then report agreement with it as validation. For any
large-model prediction carry validity-box status, extrapolation distance,
parameter uncertainty, quality-term provenance, and whether Q or p is observed,
mapped, inferred or unavailable. If IF1/IF2 is not yet available, prepare the
machinery but do not manufacture final Q-dependent numerical claims.

B11. MARGINAL EFFECTS AND ELASTICITIES

Derive and executable-test the relevant partials. The implementation must
satisfy dL/dN < 0, dL/dD < 0 and dL/dQ < 0 in the valid region. Derive and
report interpretable elasticities for N, D and Q, distinguishing the derivative
of total Loss, the derivative of excess reducible Loss, and the log elasticity;
do not present them interchangeably. Use symbolic derivation or independent
finite-difference checks to test signs and formulas.

B12. ISO-LOSS SUBSTITUTION - SIGN CORRECTNESS IS LOAD-BEARING

Along an iso-loss curve dN/dQ = -L_Q / L_N. Because both are negative,
dN/dQ < 0: better quality reduces the N required for the same Loss. Do not
reproduce the common sign error that makes better quality require MORE
parameters.

Where a quality improvement produces a reduction R in the data/quality loss
term, distinguish carefully:

  parameter saving      Delta_N_sub   = [N^(-alpha) + R/A]^(-1/alpha) - N   (negative)
  equivalent capacity   Delta_N_equiv = [N^(-alpha) - R/A]^(-1/alpha) - N   (positive)

the latter finite only when R < A N^(-alpha), diverging as that fails.
Implement domain guards, unit-test the signs and limiting behavior, and do not
hide an infeasible equivalent behind NaN without explanation.

B13. WAITING FOR IF1 / IF2

When the interfaces arrive: validate them against the existing contract; verify
the hashes and provenance reported by their producer; regenerate locally from
tracked producer code when practical; do not consume values blindly from a chat
report; only then finalize the generalized Q/p fit and the final IF3. Until
then T-008 remains WIP, and independent verified progress should still be
pushed.

B14. FINAL IF3 REQUIREMENTS

Do not overwrite the classic IF3 and present it as the generalized result. When
the generalized model is scientifically justified, emit a distinct final IF3
satisfying the existing contract, including functional form, fitted parameters,
covariance or an explicit reason it is unavailable, model-cluster bootstrap
metadata, a validity box for N, D and Q, validation by provenance class,
q-term provenance, and overall provenance. The validity box must reflect the
data that actually identify each term. If Q variation is semi-synthetic, that
fact travels in the q-term provenance. If p is consumed only through Q(p), say
so in the model description.

B15. TRACKED ARTIFACTS

Produce concise, regenerable tracked evidence only. Do not manually edit
generated numerical tables. Large sweeps and intermediates stay in ignored
local space.

B16. TESTING

Add executable tests for the load-bearing mathematics and data roles: fitted
positive parameter-domain constraints; monotonicity signs; finite-difference
agreement for key derivatives; iso-loss substitution sign; equivalent-capacity
feasibility boundary; provenance split of the tables; no validation set
entering tuning; deterministic seed use; interface validation behavior; no
raw-data writes. Where practical use a deliberately invalid fixture once to
confirm a new check can fail. Do not weaken a failing test to make it green.

B17-B21. REVIEW, WORKLOG, GIT DISCIPLINE, VALIDATION, ESCALATION

Maintain reviews/T-008/HANDOVER.md covering the starting main HEAD, branch
HEAD, draft PR, baseline status, the principal table's generator-recovery
finding, observed external validation, the quality-table audit, added
uncertainty diagnostics, the implemented generalized mathematics, substitution
formulas and tests, large-model role audit, what remains blocked on IF1/IF2,
scientific claims refused for now, tracked evidence with exact regeneration
commands, known limitations, and the next action. Do not claim T-008 complete
merely because all independent work is complete.

Append an accurate T-008 block to worklog/M1.md recording work performed,
outputs, approximate active effort to 0.5 h, AI tool, validation actually run,
key negative/provenance findings, and the exact dependency still pending.

Use focused commits `type(scope): lowercase imperative [T-008]`, ASCII, one
line, <= 72 characters, no manual co-author trailers, on the task branch. Open
a DRAFT PR once there is a coherent reviewable checkpoint, and do not merge it
merely to make the branch disappear.

Before reporting a checkpoint, regenerate the evidence and verify: the classic
baseline still reproduces; the principal table's fingerprint still reproduces;
the observed-table metrics still reproduce; uncertainty diagnostics are
deterministic; derivative and substitution tests pass; no raw file changed; no
ignored dataset was staged; no absolute machine path entered Git; no other
member's active path changed. Review the final diff semantically.

Stop the affected subtask rather than improvising if the actual table structure
contradicts the assumptions above; if a table's provenance cannot be resolved
for its intended role; if the quality exponent is non-identifiable; if a
proposed p term merely duplicates Q and cannot be separated; if the large-model
tables would create fit/validation leakage; if IF1/IF2 is unavailable, fails
its contract, or cannot be regenerated; if a scientifically necessary change
requires another member's owned paths or a shared schema change; or if a
public-safety check fails. Push coherent independent progress before escalating
where possible.
```

### Outcome

- Commit / PR: see `reviews/T-008/HANDOVER.md`
- Review package: `reviews/T-008/HANDOVER.md`
- Result: independent work delivered; generalized Q/p closure awaits IF1/IF2,
  so the stage remains open rather than accepted.

---
