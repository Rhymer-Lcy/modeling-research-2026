# M4 工作日志

本文件只由 M4 追加，其他成员不要编辑。新条目追加在文件末尾。

条目格式（照抄下面这一段，逐行填写，目标是 1 分钟内写完）：

```
## T-0xx · M4 · YYYY-MM-DD
- 内容:
- 产出:
- 提交:
- 用时:
- AI:  · 校验:
- 状态:
- 备注:
```

字段说明：

- `内容` 实际做了什么。
- `产出` 实际改动/新增的路径，多个用 ` · ` 分隔。
- `提交` commit 短哈希；如果走了 PR，追加 ` · PR #n`。
- `用时` 近似有效工时，按 0.5 h 取整，自报即可。
  工时、提交数、代码行数、提示词条数、token 用量都**不是**贡献度分数，
  不会被单独拿来衡量任何人。贡献度由任务范围、产出、校验方式、提交、
  评审与集成工作、以及日志共同还原。
- `AI` 用了哪个工具；没用就写"无"。`校验` 是**必填**：说明你如何确认
  产出是对的。任务的人类负责人始终是作者，并对结果负责。
- `状态` todo / wip / review / done / blocked / dropped。
- `备注` 阻塞、遗留问题、移交说明。

---

## T-011 · M4 · 2026-09-26
- 内容: 按 `worklog/specs/T-011.md` v1（科学规范）+ v2（负责人移交），从同步后的 main
  （1e487fb）重新执行 Q4，未使用 M3 的任何本地状态。先核验本地源数据面：C 附件
  1,973 个文件逐一对 intake 清单比对大小与 SHA-256，清单本身哈希与 intake 回执一致；
  经典 IF3 哈希 720efea8… 取自已跟踪的 T-008 收尾表而非手写，`.validate()` 通过，
  原始 N / 原始 D 单位，Q=[1,1] 仅为无质量哨兵。经 T-009 代码路径重建面板
  （4,497×48），九张 T-009 表与已提交 blob 逐字节一致。在拟合任何结果之前冻结主分析
  总体（1,385 个模型：A 预训练 215，BC 对话/微调 1,170）与时间口径（T-009
  `analysis_date`，保留 `date_source`；新增日期一致性规则 R7，移除 5 行），锚点
  2025-03-13。六个维度均核实为同一“越高越好”基线重标尺度，宏分数 = C1 `Average`。
  按组委会元数据划分桥接层级（S1 7 / S2 36 / S3 32），先声明三种单调形式再比较误差，
  留一锚点交叉验证按一个标准误规则选定常数形式并发布 IF4。完成参数尺度与训练算力
  尺度两类分解、匹配尺度诊断、C4 算力子集分析，12 个月主预测与 24 个月压力外推、
  滚动起点回测（6 个月、4 个起点）和稳健性矩阵。
- 产出: src/evolution/ · scripts/q4_evolution_run.py · scripts/q4_evolution_selftest.py ·
  scripts/q4_evolution_reproduce.py · results/tables/q4-evolution-population.md ·
  results/tables/q4-bridge.md · results/tables/q4-decomposition.md ·
  results/tables/q4-frontier-forecast.md · results/tables/q4-forecast-robustness.md ·
  results/tables/q4-evolution-reproduction.md · reviews/T-011/HANDOVER.md · worklog/M4.md
  （IF4 仅本地：data_local/problem-f/interfaces/q4-if4-loss-benchmark-bridge.json，
  SHA-256 f4aad9de…）
- 提交: 566660a · 85deb79 · 460df14 · 交接包与本日志见其后一条提交 · PR #15（Draft，未合并）
- 用时: 1.5h
- AI: Claude Opus 5.5 · 校验: `scripts/q4_evolution_selftest.py` PASS（60 条断言，覆盖
  规范要求的 22 项守卫，多数带必须抛出指定异常的变异夹具）；
  `scripts/q4_evolution_reproduce.py` PASS（两遍逐字节一致、仅 LF、C 附件树 / IF3 /
  T-009 表均未变、运行时与 environment.yml 一致），并用 `git show HEAD:<path> | sha256sum`
  独立核对已提交表与复现记录一致；`q4_panel_selftest.py` PASS（28）、
  `selftest_interfaces.py` PASS（21）；`git diff --check` 干净；public-safety PreCommit
  与 PrePush 均 PASS。检查确实失败过并被修复而非放宽：尾随空白守卫拦下一行生成文本；
  锚点拼写变异暴露 `match_anchors` 在未匹配锚点上崩溃（pandas 把 None 存为 NaN）；
  模糊匹配扫描误报 docstring 中的单词，已收窄到导入与调用并加反例；因果措辞扫描命中
  一句否定式免责声明，已改写。复现检查在扰动一张 T-009 表和伪造 numpy 版本时确实报
  FAIL（文件已还原）。
- 状态: wip（Draft PR #15 待 M1 监督审查；未合并；未开始 T-012）
- 备注: 负面结果：在 IF3 损失尺度上识别不出 loss→score 斜率（S1 锚点均处于基准地板，
  IF4 为常数 5.66±0.42，仅对损失 2.0933–2.5978 有效），跨家族文献损失不可迁移，
  桥接式前沿预测不成立；IF3 主分解不支持（框内仅 6 行）；预先声明的加性分位数前沿
  分解未通过自身一致性检查，前沿拆分改用前沿集精确 OLS 恒等式（交接包中已披露为
  偏差）；A 组无法回测，只作未验证外推。最大敏感性是日期口径（BC 12 个月点
  50.37–65.31），且 6 个月回测全部低估前沿。C2 出版日期对衍生模型通常是上游家族
  日期（组委会模糊匹配），属 T-009 层面的观察，已报告、未修改。按 v2，M3 无任何
  变更，故没有移出负责人的日志条目；本文件按现有模式新建，未复制他人条目。本机
  git 作者邮箱原不在批准名单内，首次提交前已改为本人 GitHub noreply 并核验；HTTPS
  凭据被拒，改用 SSH 推送到同一仓库（origin 配置未改）；Draft PR 由负责人在网页端
  创建。

## T-011 · M4 · 2026-09-26
- 内容: 执行 `worklog/specs/T-011.md` v3 监督修正（只改解释与下游契约，不重开分析）。
  先按 AGENTS.md 同步：fetch 后用祖先关系确认 origin/main 含 4096a97、分支自已审
  7309fa6 派生、PR #15 仍为 Draft 未合并；main 的新增只有 T-011 规范的 v3 追加
  （v1/v2 字节级不变），以普通合并提交 648efaf 并入，未 rebase、未强推。
  - 核心更正：参数量增长 ≠ 训练算力增长。log-N 情景改名为
    parameter-scale-growth scenario（数值实现不变）；BC 该分量为 -0.55，故不约束。
  - 50.89 / 60.80 改标为 historical / direct-score continuation。
  - 题目要求的算力放缓只用 assumption-based transferred compute-slowdown
    sensitivity 回答：12 个月 47.64 / 44.39，24 个月 54.29 / 47.78，保留转移份额区间。
  - 日期口径敏感性（50.37 / 50.89 / 65.31）紧贴头条，并算出其跨度大于模型预测带宽。
  - IF4 明确为退化的无斜率桥；前沿集拆分标为失败后采用的模型条件分解。
  - 交接包新增 T-012 必须保留的 C3 / C4 / C8 覆盖契约。
  - 新增 `src/evolution/claims.py` 作为措辞契约与检查器。
- 产出: src/evolution/claims.py · src/evolution/config.py · src/evolution/dynamics.py ·
  scripts/q4_evolution_run.py · scripts/q4_evolution_selftest.py ·
  results/tables/q4-bridge.md · results/tables/q4-decomposition.md ·
  results/tables/q4-frontier-forecast.md · results/tables/q4-forecast-robustness.md ·
  results/tables/q4-evolution-reproduction.md · reviews/T-011/HANDOVER.md · worklog/M4.md
- 提交: 648efaf（合并 main）· d0be937（v3 修正） · 交接包与本日志见其后一条提交 · PR #15（Draft，未合并）
- 用时: 0.5h
- AI: Claude Opus 5.5 · 校验: `q4_evolution_selftest.py` PASS（78 条断言：原 60 条
  加 8 项 v3 守卫与数值保持守卫，均带必须被检出的植入违例）；`q4_evolution_reproduce.py`
  PASS；`q4_panel_selftest.py` PASS（28）；`selftest_interfaces.py` PASS（21）。以 7309fa6
  为基准逐表比对，表格行中的数字无一变化；唯一消失的数字是 v3 要求删除的定义句中的
  乘数 1。IF4 字节不变（f4aad9de…）。`git diff --check` 与 public-safety PreCommit /
  PrePush 均 PASS。
- 状态: wip（Draft PR #15 待 M1 最终审查；未合并；未开始 T-012）
- 备注: 旧交接包把不约束的 x0.5 行的区间当作头条区间引用；v3 把头条改为延续行（x1，
  等同 time-only），该行本就在已审表中（预测区间 [45.72, 56.07]）。两行数值均未变，
  只是引用的行改了。v3 未改变任何科学估计，也未修改 TASKS.md、规范文件、T-009 产物或
  共享接口。

## T-010 · M4 · 2026-09-27
- 内容: 按 `worklog/specs/T-010.md` v3 从 M2 受控接管 T-010，并完整执行 v2 科学修正。
  - 接管：在 `scratch/t010-m2-takeover/` 就地核验包内 4 个文件的 SHA-256 与字节数，
    与 M2 报告值一致；10 个 TAR 成员哈希与 MANIFEST 一致。包在本次开始前已被移到
    scratch，根目录下不存在，故 M4 未执行也未观察搬移（见交接包 Deviations）。
    从 PR #17 已审头 2047c33 建分支，普通合并 main 583b8db（4558cd8，无冲突）。
    补丁 21 个目标、TAR 10 个成员均在 T-010 路径内，`git apply --check` 通过后才导入。
  - 导入内容一律视为未审草稿：求解器的 D_best 约化与候选集经独立推导确认后保留；
    其余逐项修正或重写（路径被 normcase 小写化、LOO 替代拟合冒用已接受 IF3 的哈希、
    连续预算分析绕过预算守卫且把区间外候选当作转移、来源回执只核对被压平的公式锚点、
    复现只记录不强制运行时版本、IF1/IF2/B8 守卫是生产路径无法触发的辅助函数）；
    草稿生成的 14 个结果表全部作废并由 M4 代码重新生成。
  - v2 结果：预算不等式 `kappa N D <= C` 下 1e24 改为上角点 `(N_max, D_max)` 且有
    未用预算（利用率 0.023–0.116），撤销 NO_VALIDITY_BOX_ALLOCATION；每个观测上下文
    在声明预算区间内恰有两次结构转移（D_max 生效、C_box 处出现松弛）；LOO 以发布精度
    传播，N/D 范围可分辨，损失范围在发布精度下不可分辨；原始 g(Q) 三对各恰一个交点；
    非基线 Q 仍 BLOCKED_UNTIL_Q0_DECLARED。
  - 来源：30 项数值与措辞在 DOCX 的段落/OMML 定位处逐项核验；8 个文件的显式白名单；
    审计钩子守卫在打开前拒绝任何 PDF、白名单外本地文件、写入与目录遍历；42 项来源
    台账无 PDF 依赖、无 UNVERIFIED 项。
- 产出: src/alloc/ · scripts/q3_* · results/tables/q3-* · reviews/T-010/HANDOVER.md · worklog/M4.md
- 提交: 4558cd8（合并 main）· 37803c5（修正实现）· 4a39d6e（重新生成结果表）· 交接包与本日志见其后一条提交 · 替代 Draft PR 编号在创建后另行报告
- 用时: 1.5h
- AI: Claude Opus 5.5 · 校验: `q3_selftest.py` PASS（170 条断言，含临时夹具上的反例与子进程
  中的守卫探测）；`q3_reproduce.py` PASS（两遍逐字节一致、仅 LF、运行时等于
  environment.yml 固定版本、输入与接口不变、守卫拒绝 0 次）；`selftest_interfaces.py`
  PASS（21）；以 15 个生产代码缺陷做变异测试，14 个被检出，剩余 1 个被白名单兜底拒绝；
  提交的 blob 逐字节确认仅 LF；`git diff --check` 与 public-safety PreCommit 均 PASS。
- 状态: wip（替代 Draft PR 待 M1 监督审查；未合并；PR #17 保持 Draft 未合并；未开始 T-012）
- 备注: 用户报告 M2 曾在编辑器中手动打开原始 PDF；记为操作性偏差，M4 未掌握更多事实，
  且所有承重数值均可追溯到授权来源，未发现 PDF 依赖。M4 本次未打开、解析、哈希任一 PDF，
  也未列举其所在目录。T-010 路径外的观察（未修改）：`src/paths.require()` 对仓库根
  以外的缺失路径抛出 ValueError 而非 RawDataMissing。未修改 TASKS.md、规范文件、
  environment.yml、共享接口或 worklog/M2.md。
