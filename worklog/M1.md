# M1 工作日志

本文件只由 M1 追加，其他成员不要编辑。新条目追加在文件末尾。

条目格式（照抄下面这一段，逐行填写，目标是 1 分钟内写完）：

```
## T-0xx · M1 · YYYY-MM-DD
- 内容:
- 产出:
- 提交:
- 用时:
- AI:  · 校验:
- 状态:
- 备注:
```

字段说明：

- `内容` 实际做了什么。
- `产出` 实际改动/新增的路径，多个用 ` · ` 分隔。
- `提交` commit 短哈希；如果走了 PR，追加 ` · PR #n`。
- `用时` 近似有效工时，按 0.5 h 取整，自报即可。
  工时、提交数、代码行数、提示词条数、token 用量都**不是**贡献度分数，
  不会被单独拿来衡量任何人。贡献度由任务范围、产出、校验方式、提交、
  评审与集成工作、以及日志共同还原。
- `AI` 用了哪个工具；没用就写"无"。`校验` 是**必填**：说明你如何确认
  产出是对的。任务的人类负责人始终是作者，并对结果负责。
- `状态` todo / wip / review / done / blocked / dropped。
- `备注` 阻塞、遗留问题、移交说明。

---

## T-001 · M1 · 2026-09-22
- 内容: 建立仓库骨架：忽略规则、LaTeX 构建链（模板本地置备 + 身份回退）、
  依赖定义、公开安全检查脚本与其变异自检。
- 产出: .gitignore · .gitattributes · README.md · AGENTS.md · CLAUDE.md ·
  TASKS.md · environment.yml · paper/ · src/paths.py · configs/default.yaml ·
  scripts/
- 提交: 9a3cfd4
- 用时: 3.0h
- AI: Claude Opus 5 · 校验: 构建脚本分别从仓库根、其上级目录与一个无关目录
  调用，三次都解析到同一仓库并构建成功；team.tex 存在/缺失两条分支分别构建并核对
  封面取值；渲染页面逐页查看（提取文本对中文不可靠，已记入 README）；
  检查脚本先做变异自测（假凭据 / 超大文件 / 干净样本）再投入使用。
- 状态: done
- 备注: 无。

## T-002 · M1 · 2026-09-22
- 内容: 公开发布前定稿：字体回退做了一次对照测试并改用 FandolHei；公开安全
  检查改为结构化保护（作者邮箱必须是 GitHub noreply），不再维护任何个人身份
  数据；文档同步更新贡献者首次提交前的邮箱设置步骤。
- 产出: paper/main.tex · scripts/check_public_safe.ps1 ·
  scripts/selftest_checker.ps1 · README.md · AGENTS.md · TASKS.md
- 提交: 9d16608 · 8663e59 · f0e78b4
- 用时: 1.5h
- AI: Claude Opus 5 · 校验: 两种字体各构建一次并逐页比对（封面页裁切
  SHA-256 一致，说明封面不走该字族；第 2 页仅全角冒号字重不同）；pdffonts
  确认替换确实生效；变异自测在仓库外临时库中跑出 FAIL/FAIL/PASS；
  构建回归覆盖三个调用目录、team.tex 缺失回退与全新克隆。
- 状态: done
- 备注: 不再需要任何真实姓名 / 机构 / 队号信息。

## T-003 · M1 · 2026-09-22
- 内容: 终审清理：README 中 docs_local 的描述仍残留已废弃的“身份输入”字样，
  改为只描述本地参考与机器相关笔记；补全 T-002 的提交哈希以便追溯；
  将 main 分支保护的限制扩展到仓库管理员。
- 产出: README.md · TASKS.md · worklog/M1.md · GitHub main 分支保护配置
- 提交: 2eb5a52 · PR #1
- 用时: 0.5h
- AI: Claude Opus 5 · 校验: 合并前跑 PreCommit 并构建一次（8 页 A4、无
  Missing character）；逐行核对 PR diff 只含上述三个文件；合并后跑 PrePush、
  核对 local main 与 origin/main 一致、工作区干净；分支保护改动后重新查询
  接口确认 enforce_admins=true 且 force push / 删除仍为 false。
- 状态: done
- 备注: 按仓库自身规则，README 属高影响面，故走短生命周期分支 + PR 合并。

## T-004 · M1 · 2026-09-22
- 内容: 修正邮箱闸门的策略本身。原先的不变式“每个作者/提交者邮箱都必须以
  @users.noreply.github.com 结尾”过严：GitHub 在服务端做 squash 合并时，
  作者取自账号的公开资料邮箱、提交者是 web-flow 机器人，二者都不符合该后缀。
  改为“每个邮箱都必须被仓库的公开邮箱策略显式允许”，策略落在受版本控制的
  configs/git-email-policy.txt：后缀规则覆盖所有人的 GitHub noreply 地址，
  另外显式批准 web-flow 提交者地址，以及 M1 已经公开、并经仓库所有者明确
  确认可用于公开历史的那个邮箱。这样“误发未批准邮箱”依然被拦，而“本人主动
  公开某地址”必须改那个文件、留下可审阅的痕迹。M2/M3 的要求不变：首次提交前
  仍应配置自己的 GitHub noreply 地址。
- 产出: configs/git-email-policy.txt · scripts/check_public_safe.ps1 ·
  scripts/selftest_checker.ps1 · README.md · AGENTS.md · TASKS.md
- 提交: (见本条目所属 PR 的 squash 提交)
- 用时: 1.0h
- AI: Claude Opus 5 · 校验: 变异自测扩到 9 条，在仓库外临时库中分别构造
  approved-suffix / web-flow / approved-exact / 未批准 四种邮箱组合并跑
  PrePush，结果 PASS/PASS/PASS/FAIL；另含凭据、超大文件、绝对路径、干净样本
  四项；合并前跑 PreCommit 并构建一次；合并后对全量可达历史跑 PrePush。
- 状态: done
- 备注: T-003 本身没有问题，其实现是正确的；此前那次 PrePush 失败是本条所修正
  的策略过严导致的，且是在那次合法的服务端 squash 合并之后才暴露出来。
  本次未重写任何历史、未强推、未重建仓库、未改动账号级邮箱设置。

## T-005 · M1 · 2026-09-23
- 内容: Problem-F 接入守卫与公开安全门修复，并先行登记 T-005..T-012 任务图。
  两个缺陷：(1) 主办方 Problem-F 素材在工作区里是 untracked 但**未被忽略**，
  一次 `git add -A` 会暂存 2,014 个文件约 552 MB，其中含带注入文本层的 PDF 与
  本地审计证据；(2) 安全门把 Git 对非 ASCII 路径的引号转义形式直接交给
  `Test-Path -LiteralPath`，路径里的引号是非法字符，报的是非终止错误，于是该
  文件被**跳过**——体积与凭据检查从未运行，门却报告成功。超过 10 MB 失败线的
  三个文件正是这样被放过的。修复采用双重防御：`core.quotePath=false` 加把控制台
  编码钉到 UTF-8；若钉不住则退回 Git 默认引号形式（纯 ASCII，与编码无关）再显式
  解码；两种模式都可能有残留引号，故无条件解码。无法解释的路径现在是**失败而非
  跳过**，并新增 `quoted paths: N decoded, M uninterpretable` 计数器。
- 产出: TASKS.md · .gitignore · scripts/check_public_safe.ps1 ·
  scripts/selftest_checker.ps1 · README.md
- 提交: 9e0dc17 · 8c75a7b · 08e5b0e · 5aee0aa · a829c74 · PR #3
- 用时: 2.5h
- AI: Claude Opus 5 · 校验: 自检从 9 条扩到 11 条，两条新用例都在仓库**外**的
  一次性沙盒里建非 ASCII 目录：超大文件必须报出体积违规（判别性用例——跳过路径
  的门同样会 exit 0），干净小文件必须被计入 files examined（守另一个方向）。
  两条都同时断言计数器而非只断言退出码，因此“静默跳过”的实现无法通过。解码器
  另做单元测试：真实引号形式 + 三种畸形输入，畸形输入全部抛出，调用方才能
  fail closed。合并前后各跑 PreCommit/PrePush，均 PASS；编辑后扫描控制字符与
  字面转义串，干净。
- 状态: done
- 备注: 本次重构自己引入过一个缺陷并被自检抓到：把历史扫描改走新助手函数后，
  `git log --all -p` 的裸 `-p` 被 PowerShell 前缀匹配绑到公共参数
  `-PipelineVariable`，PrePush 的历史扫描整个不再运行，自检 1-3 条随即失败并
  指名。改为显式数组传参。记在这里而不是悄悄修掉。
  另：用脚本改 .ps1 时把行尾从 CRLF 写成了 LF，.gitattributes 声明
  `*.ps1 text eol=crlf`，提交后重新检出已恢复 CRLF，工作区干净。
  本任务未迁移、未删除任何素材，也未清洗 PDF——那是 T-006。

## T-013 · M1 · 2026-09-23
- 内容: 建立两条轻量协作机制，并登记 T-013 作为长期协调任务。
  (1) `reviews/`：每个任务一份**受版本控制**的评审包 `reviews/T-0xx/HANDOVER.md`。
  动机是评审证据此前只存在于评审者够不到的地方——聊天记录、被忽略的本地目录、
  或者助手对自己工作的总结，这三样都不可评审。评审包是“指向证据的综合”，不是证据
  本身：代码已经在仓库里、产物已经在 results/ 里，评审包说的是做了什么、能支持什么
  结论、不能支持什么、以及怎么重跑。所有权沿用第 2 节同一套规则，归该任务**当前**
  负责人，不另立一套。
  (2) `worklog/specs/`：正式任务阶段的**规范执行说明**，L1/L2/L3 分级——L1 本地
  操作性提示词不归档；L2 正式阶段归档；L3 项目级决策必须归档且由 M1 控制。判据不是
  提示词长短，而是“事后审计时是否需要这条指令才能理解结果为什么长这样”。
- 产出: reviews/README.md · reviews/HANDOVER_TEMPLATE.md ·
  worklog/specs/README.md · worklog/specs/PROMPT_SPEC_TEMPLATE.md ·
  AGENTS.md（新增第 8 节，原 8-11 节顺延为 9-12）· README.md · TASKS.md
- 提交: 946fe0b · 4fa8410 · 388e23a · d7fb2ee · 727422c · PR #6
- 用时: 1.5h
- AI: Claude Opus 5 · 校验: 逐条核对：科学文件零改动（src/ scripts/ results/
  configs/ paper/ environment.yml 的改动数为 0）；T-007…T-012 的 Owner 与 Status
  全部原样；六个任务各自的 `reviews/T-0xx/` 在 TASKS.md 里各出现且仅出现一次；
  `reviews/` 与 `worklog/specs/` 确认未被忽略、已被跟踪；新增行里没有绝对机器路径、
  没有任何厂商名、没有密钥/额度/账号细节（grep 命中的都是“禁止清单”本身的措辞）；
  `git diff --check` 通过；PreCommit 通过；两套自检 11 条与 21 条保持全绿；
  编辑后扫描控制字符与字面转义串，干净。
- 状态: wip
- 备注: T-013 是协调任务，**不进科学依赖图**，既不是 T-007…T-012 的前置也不被其
  阻塞；它在项目期间保持 wip，因为规范本身需要持续维护。
  语义复审发现并修掉了我自己引入的一个版式缺陷：最初把 `### Review packages`
  插在 Collaboration 小节中部，导致其后的提交规范、邮箱配置、AI 协助与工时段落
  在视觉上都被归进了这一小节；已移到该节末尾，正好接在“人类负责人对结果负责”
  之后。仓库合同保持厂商中立：没有把任何商业模型写成 M1/M2/M3 的强制工具。
  按要求未回填任何历史：没有为 T-007/T-008/T-009 编造评审包，也没有把既往对话
  整理成规范提示词；归档从下一批正式提示词开始，既往活动仍以各自 worklog 为准。

## T-014 · M1 · 2026-09-23
- 内容: 接收组委会 2026 年官方稿件格式文件，归档为**只在本地**的权威来源，并据此
  逐条审计现有 LaTeX 实现、只做必要的合规修正。
  (1) 归档：两份原件先算 SHA-256 与字节数，按字节复制到 `docs_local/gmcm-2026/
  source/` 后重算并**要求完全相等**，证明相等之后才删除仓库根目录的副本，删除后
  再校验一次；派生 PDF 另放 `audit/` 并标注为派生，转换前后复核原件哈希未变。
  (2) 审计：19 条要求里 14 条本就合规、5 条不合规并已修正——摘要页与正文之间的
  目录（违反“摘要页下一页开始正文”）、正文页眉、页码印在右上角而非页脚居中、
  行距 1.38 而非单倍、参考文献中书籍缺起止页码。
  (3) 实现：新增受版本控制的 `paper/format_2026.tex`，在文档类之后加载、只覆盖
  2026 规范要求的部分；第三方文档类保持被忽略且未被修改。
- 产出: paper/format_2026.tex · paper/main.tex · paper/FORMAT_2026.md ·
  paper/sections/05-4-model-q4.tex · paper/references.bib ·
  worklog/specs/T-014.md · TASKS.md · reviews/T-014/HANDOVER.md
- 提交: a831c08 · b51ebd2 · 952cc5c · d70e6b8 · af9ca23 · b00c963
- 用时: 2.5h
- AI: Claude Opus 5 · 校验: `build_paper.ps1 -Clean` 退出 0、无 Missing character；
  逐页渲染成图片**逐页看过**：封面为第二十三届且无页码、摘要页页脚居中 1、正文紧接
  摘要页、页码 1-4 连续且全部页脚居中、全文无页眉、一级标题四号黑体居中、正文单倍
  行距；PreCommit 与 PrePush 均通过；`git diff --check` 通过；新增跟踪文件扫描绝对
  机器路径为 0。
- 状态: done
- 备注: 三条负面发现值得记住。
  (1) **预期错了位置**：原以为标题图元过期，渲染后发现它本来就是 2026 的；真正过期
  的是相邻的校徽图元——它画的是上一届承办高校，并且携带一层**不可见文字**，写着上一届
  届次和一套重复的身份标签，所以可见封面正确而 PDF 文字层却报错届次。用两个各只引入
  一个图元的最小文档分别抽取文字，才把来源钉死。裁剪只能挡住图形、挡不住文字层，
  该图元又是被忽略的第三方文件不得原地修改，因此改为打补丁去掉整行校徽，补丁匹配
  不到就**报错终止**，避免将来悄悄恢复。代价是封面不再有校徽行，已在 FORMAT_2026.md
  记为保留的取舍：组委会自己的图元不能进版本库，而印上一届承办校是事实性错误。
  (2) **自己写的检查器过不了变异测试**：第一版机器路径扫描器对着已知含盘符的字符串
  报“干净”——shell 转义层把字符集压成了只匹配正斜杠。重写并重新变异测试通过之后，
  才敢相信它给出的“干净”。
  (3) **先断言后验证**：曾直接写下“参考文献按引用次序排列”，而该 bst 里确实有 SORT；
  改用两条目夹具验证——字母序最靠后的作者先被引用，拿到了 [1]——矩阵改为引用这次
  实测而非对样式内部的猜测。另：给 references.bib 加注释时写进了 at 符号，BibTeX
  没有注释语法、把注释当条目读，构建当场失败；立刻重建才发现。

## T-008 · M1 · 2026-09-23
- 内容: 在不等待 T-007 的 IF1/IF2 的前提下，推进广义（含数据质量 Q）标度律中
  所有不依赖其数值的部分：数学实现、可执行检验、质量表结构与来源审计、经典律的
  补充不确定性诊断、边际效应与等损失替代。
  按规范要求，**先审计后拟合**——结论是两条否定性发现，比“拟合出一个 gamma”
  重要得多。
- 产出: src/scaling/quality.py · scripts/q2_audit_quality_data.py ·
  scripts/q2_uncertainty.py · scripts/q2_substitution.py ·
  scripts/selftest_quality.py · results/tables/q2-quality-data-audit.md ·
  results/tables/q2-uncertainty-robustness.md · results/tables/q2-substitution.md ·
  worklog/specs/T-008.md · reviews/T-008/HANDOVER.md
- 提交: 见 exp/m1-T008-generalized-scaling 分支与其草稿 PR
- 用时: 3.0h
- AI: Claude Opus 5 · 校验: selftest_quality 25 条全过（含一次“故意喂错模型”
  的变异测试：gamma 取负时三条符号断言必须翻转，否则说明符号检查是摆设）；
  经典基线与 selftest_interfaces 重跑结果不变；PreCommit 通过。
- 状态: wip
- 备注: 两条承重的否定性发现。
  (1) **B8 的 Q_score 与 B6/B7 方向相反**，且不是个别单元格——逐个 (N,D) 单元格
  测 d logL/d logQ：B6 45/45 为负、B7 45/45 为负，而 B8 校准段 90/90 为正、
  外推段 60/60 为正，240 个单元格零例外。候选式要求质量降低损失，正斜率根本
  表达不出来，所以对 B8 拟合不是“估出一个质量指数”，而是把 gamma 顶到下界
  0.001 再报一个没有含义的数——审计第一版确实这么报了，现在改为**拒绝拟合并
  说明原因**。这正是“按字段名推断字段含义”那类错误：三张表列名完全相同、行为
  相反。B8 在语义查清之前不参与质量律估计，也不作为佐证。
  另：B8 有 362/1704 行卡在恰好 0.5 的地板上，B6/B7 的最小值各只出现 1 次。
  (2) **即便在方向正确的 B7 上，候选式也不是生成器**：指纹比值约 865（不是 ~1），
  中位相对残差约 1.7%，而存储量化只有 2e-05。与 B1 上经典律那种“复原生成器”
  完全不同。所以 gamma≈1.19 只是误设形式下的最佳近似，不能当作复原出来的常数
  引用。据此**不发布广义 IF3**。
  IF1/IF2 目前不在规范接口目录里（只有 IF3_classic.json），B6 那条“p 是否只
  通过 Q(p) 起作用”的问题根本还打不开，故 T-008 保持 wip。

## T-008 v2 · M1 · 2026-09-23
- 内容: 按监督评审意见做一次窄范围 delta，只修三处问题、加一个受限诊断，不重开
  T-008 设计、不合并 PR #8。
  (1) **Q 目标越界**：替代情景表用固定倍率乘基准 Q，在拟合有效域上界 Q=1 之外
  打印了 1.05 和 1.35，把外推当成插值报了出来。改为从拟合 validity box 取
  Q_min/Q_max、裁剪到上界、只保留 Q_base < Q_target <= Q_max、裁剪后去重；
  基准已在上界则**明确写"该情景省略"**而不是编一个出来。构造函数放进
  src/scaling/quality.py 便于被测试，脚本另对**实际打印出的**目标做硬断言。
  (2) **措辞收口**：不再称其为"采用的形式"，不再说估计器"复原了所供质量机制"。
  文档拆成两类陈述——依赖候选式本身的**解析恒等式**（与数据无关、已机器验证），
  与代入 B7 误设拟合得到的**数值示例**（标注为模型内部的示意性敏感度，并明确
  声明"不是 Q2 最终经验替代结果"）。数学一行未删，符号推导一处未弱化。
  (3) **经典 IF3 出处加固**：不改共享 schema，也不把组委会的 trust 类别偷偷降级
  （仍为 observed），而是在 notes 里按序写清：组委会标签 / 数值指纹 / 因此这组
  参数支撑的是估计器与生成器复原而非对真实标度的精确经验认识（窄区间不得如此
  解读）/ 经验外部支撑来自剔除重叠 Pythia 行的 B4 与 B5。
  (4) **B8 语义受限诊断**：并入既有审计，不另起产物。
- 产出: src/scaling/quality.py · scripts/q2_substitution.py ·
  scripts/q2_audit_quality_data.py · scripts/q2_fit_classic.py ·
  scripts/selftest_quality.py · results/tables/q2-substitution.md ·
  results/tables/q2-quality-data-audit.md · worklog/specs/T-008.md (v2 块) ·
  reviews/T-008/HANDOVER.md
- 用时: 1.5h
- AI: Claude Opus 5 · 校验: selftest_quality 47 条全过（由 25 条增至 47，新增覆盖
  上界、严格改进、去重、1.05/1.35 两个具体缺陷值、以及"无可用目标"的空结果）；
  selftest_interfaces 21 条通过；三个 q2 脚本均可重跑；git diff --check 干净；
  PreCommit 通过；原始数据、接口 schema、M2/M3 路径均未改动。
- 状态: wip
- 备注: **本次 delta 自身的检查又踩了单向陷阱。** 反向诊断第一版报"gamma 离开了
  下界，所以该项是被估计出来的"——实际上它是跑到了**上界** 10，而我的
  gamma_at_bound 只比了下界。顶到天花板和贴着地板一样是"没识别出来"，优化器只是
  被允许走到这么远。现已把边界导出为 GAMMA_BOUNDS 由施加它的模块统一提供、两端都
  测、报告里写明顶的是哪一端，并据此把结论降级为：反向只在**方向上**与"可能是反向
  计分"一致，并未解决候选式与该表的不兼容，结论仍为**存疑**；B8 继续隔离。
## T-012 pre-integration · M1 · 2026-09-24
- 内容: **只做集成前的准备，不做科学集成本身。** 依赖图不变，T-010 与 T-011 未完成，
  因此 T-012 最终集成仍被阻塞，TASKS.md 中保持 todo。
  分支从已验收的 main 切出（87fbce3），不从任何任务分支切，也未合并/摘取/复制
  T-007、T-008、T-009 三条分支的任何内容。
  产出四件：跨问题接口矩阵（IF1-IF4：生产者/消费者/契约/量纲/出处/有效域/所需不确定度/
  验收门/当前就绪度/**下游禁止的解读**）；证据到论断映射（Q1-Q4 及横切类，凡证据不存在
  的一律写 `BLOCKED - DO NOT WRITE AS A RESULT`，不写占位数值）；A-E 五级论断层级；
  图表清单（**不生成任何最终图**，且在产出任务被验收前不锁定图型）；正文各节就绪度与
  各自的"何时才可定稿"触发条件；G1-G12 终验收门；交接顺序；复现清单。
- 产出: paper/INTEGRATION_PLAN.md · results/tables/t012-integration-readiness.md ·
  reviews/T-012/HANDOVER.md · worklog/specs/T-012.md
- 用时: 2.0h
- AI: Claude Opus 5 · 校验: selftest_interfaces 21 条通过；git diff --check 干净；
  PreCommit 通过；就绪度审计里每一条事实都回读源文件核对过（kappa=6.0、拟合脚本的
  1e9 换算、两个有效域区间、面板重标定公式、15/4/3 方向计数），不是凭印象写的。
- 状态: 准备性检查点；最终集成仍被阻塞（G1-G5 全未满足）
- 备注: 三处值得记住。
  (1) **量纲上 Q2 内部存在两套制式**：经典律按原始参数量/原始 token 拟合（有效域
  N 7.054e+07..1.197e+10、D 1.340e+08..2.999e+11），而带质量的表一律按"十亿"处理。
  各自自洽，但将来若交叉比较而不声明制式，会在两个维度上各差 1e9，且结果仍然是有限
  且看起来合理的数——这种错不会自己暴露。
  (2) **指标方向是风险最高的一条**：已验收契约记录的是各指标的**原生**方向
  （15 正向 / 4 反向 / 3 非单调，合计 22），"统一成越大越好"是另一道独立工序。
  IF1 的 indicator_directions 存的是原生方向；若被当成"22 个都是正向"，会把 4 个指标
  反过来用，而下游看不出任何异常。
  (3) **三处约定含糊，本阶段刻意不裁决**：时间基准（发表日 vs 评测日）、上下文长度的
  单位与口径、基准分方向。已分派给最先需要它们的任务（T-011 / T-010 / T-011）。
  在集成规划任务里替科学问题做决定，正是本阶段被禁止的事。
  另外发现正文骨架两处过期：00-abstract 与 02-analysis 都只列了"问题一/二/三"，缺第四
  问——是 T-014 补了 Q4 正文节却没动这两个模板文件。属模板陈旧、非科学错误；
  paper/sections/ 不在本阶段可写路径内，故**只记录不修**。
