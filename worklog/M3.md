# M3 工作日志

本文件只由 M3 追加，其他成员不要编辑。新条目追加在文件末尾。

条目格式（照抄下面这一段，逐行填写，目标是 1 分钟内写完）：

```
## T-0xx · M3 · YYYY-MM-DD
- 内容:
- 产出:
- 提交:
- 用时:
- AI:  · 校验:
- 状态:
- 备注:
```

字段说明：

- `内容` 实际做了什么。
- `产出` 实际改动/新增的路径，多个用 ` · ` 分隔。
- `提交` commit 短哈希；如果走了 PR，追加 ` · PR #n`。
- `用时` 近似有效工时，按 0.5 h 取整，自报即可。
  工时、提交数、代码行数、提示词条数、token 用量都**不是**贡献度分数，
  不会被单独拿来衡量任何人。贡献度由任务范围、产出、校验方式、提交、
  评审与集成工作、以及日志共同还原。
- `AI` 用了哪个工具；没用就写"无"。`校验` 是**必填**：说明你如何确认
  产出是对的。任务的人类负责人始终是作者，并对结果负责。
- `状态` todo / wip / review / done / blocked / dropped。
- `备注` 阻塞、遗留问题、移交说明。

---

## T-009 · M3 · 2026-09-23
- 内容: Q4 逐任务评测管线与端到端验收测试。从 C8 的 1,863 个模型目录重建六维
  榜单得分，再与 C1 汇总表对账。关键点：C8 存的是**原始**正确率，C1 存的是按各
  任务随机基线重标定后的分数，两者不在同一尺度，直接比较等于混用两套口径；
  能否从原始记录复现出已公布的汇总值，就是这条管线的验收测试。
  归一化为 max(0,(raw-baseline)/(1-baseline))*100，基线尽量从该子任务自己记录的
  doc_to_choice 读（k 选项→1/k，生成式→0），分组任务先逐子任务归一再平均。
  连接用 JSON 自带的 model_name 精确匹配 C1 的 Model，不做模糊匹配，匹配不上的
  就留作未匹配并计数（1,858/1,860 命中，2 条未匹配）。
- 产出: src/panel/__init__.py · src/panel/leaderboard.py ·
  scripts/q4_panel_reconcile.py · results/tables/q4-c8-reconciliation.md
- 提交: (见 git log)
- 用时: 2.0h
- AI: Claude Opus 5 · 校验: 验收测试本身就是校验，且**它真的失败过**。第一版
  六维里只有 IFEval/BBH 对得上，另外四维不对；逐项定位后发现两处是我的缺陷：
  MMLU-PRO 的 doc_to_choice 存的是**函数源码字符串**而不是选项列表，于是基线
  错误地退化成 0（基线取 0.1 后重建值 51.851 对已公布 51.8506，精确吻合）；
  MuSR 三个子任务根本没有 doc_to_choice 且选项数各不相同（2/3/5），用各自选项数
  后重建 13.50 对已公布 13.499，也精确吻合。修完之后 IFEval/BBH/MUSR/MMLU-PRO
  四维中位偏差均为 0.0000、容差内比例 97.6%-98.3%。
- 状态: wip
- 备注: **MATH Lvl 5 与 GPQA 两维暂时隔离，不得用于逐任务分析**，并且没有为了
  让测试通过而放宽容差。诊断：MATH 是 C8 里若干模型的原始 exact_match 就是 0，
  而 C1 给的是 60 分段——这是底层数值的差异而非归一化差异，与《数据说明》里
  C1 来自 open-llm-leaderboard-old/results、C8 来自 open-llm-leaderboard/results
  这两个不同仓库相符；GPQA 是约 0.4 分的小幅系统偏移，量级对、形状像跑批差异
  而非标度错误。两者的根因需在 T-011 之前查清，或在论文中按“仅四维可复现”的
  口径使用并说明。
  另外 4 个损坏 JSON 已逐个记录文件名与错误位置，跳过并计数，不做静默丢弃。

## T-009 · M3 · 2026-09-24
- 内容: 在首日六维对账基础上完成 Q4 面板主体。从 C1/C2/C3/C4 建立规范加载器并保留
  出处；用确定性阶梯（hf_id→org→name→别名表，无模糊匹配）把榜单 4,497 个模型与
  Epoch AI(C4) 关联（命中 94 个、2.09%）；按主办方 Type 归并出 A–E 五类模型阶层
  （A 324 / B 695 / C 1752 / D 1712 / E 14）；定义开源资格字段与规则（仅正信号判开、
  缺执照判 unknown 不判 closed）并只报计数，不替 T-011 选总体；明确日期口径（Epoch
  发布日期为主、榜单提交日期为回退，逐行记 source）。保留首日对账并新增一个真正
  逐任务的聚合：逐子任务得分的一方方差分解（BBH 0.410 / MATH 0.604 / MUSR 0.516）。
  11 个无日期模型经核查是源数据真实缺失（如 granite-3.0-*、dbrx-base、OLMo-1.7-7B-hf），
  非管线 bug。
- 产出: src/panel/{sources,matching,config,strata,dates,eligibility,c4_link,panel,detail}.py
  (新) · src/panel/leaderboard.py(改) · scripts/q4_panel_{diagnose,build,detail,selftest}.py(新)
  · scripts/q4_panel_reconcile.py(改) · results/tables/q4-*.md(9 个) · reviews/T-009/HANDOVER.md
- 提交: 28cf330
- 用时: 3.0h
- AI: Claude Code · 校验: 20 条断言的变异自测（scripts/q4_panel_selftest.py，每条
  断言都破坏-复原过目标，证明它真的能失败）；scripts/selftest_interfaces.py 21 断言
  PASS；check_public_safe.ps1 PreCommit PASS（74 文件）；git diff --check 干净。对账
  脚本保留 0.5pt 容差不放宽，MATH 隔离而非硬凑通过。
- 状态: wip
- 备注: MATH Lvl 5 逐子任务记录仍隔离（C1 来自 open-llm-leaderboard-old/results、C8
  来自 open-llm-leaderboard/results，两个不同上游），T-011 前需换用与 C1 同源的 C8
  或仅用其汇总分。**推送被阻塞**：origin 是 gitclone.com 只读镜像（push 返回 504），
  直连 github.com 被重置、SSH 被墙；28cf330 及后续提交目前仅在本机，需 M1 提供可写
  远端或代理后才能推送评审。

## T-009 · M3 · 2026-09-24
- 内容: 纠正与优化轮，把 T-009 调成可被 M1 接管的 REVIEW checkpoint。(1) Git 流程纠正：
  建立/推进 exp/m3-T009-q4-panel 分支，后续全部提交落在分支，main 停在 25febc5 未动
  未强推。(2) detailed-task 纠正：首轮主表还带着隔离中的 MATH ICC，现把隔离做成代码级
  允许清单（RECONCILED_DIMENSIONS/QUARANTINED_DIMENSIONS），主结果只剩 BBH/MUSR，并新增
  逐子任务可用模型数/缺失率/median/IQR/跨模型方差、within-model imbalance；MATH 只出现
  在 quarantine 附录。(3) GPQA 证据强化：成对差异全分布（pooled 中位绝对差 0.0000、
  98.0% 在 0.5pt 内 vs 旧口径 0.3952、59.1%），确认是通用池化规则而非逐模型修正，按
  不变规则判 reconciled。(4) MATH 诊断强化：逐维状态表 + C8 元数据审计（metric、样本数、
  基线、task 版本/哈希、git、时间戳），维持 source-mismatch。(5) C4 关联审计：重复 C4
  身份键改为判歧义不链接（不再取首行），新增顺序审计报告；strata 增 C4 覆盖列与主办方
  原始 Type 分布（证明 D=1712 全部来自"base merges and moerges"标签）。(6) C3 溯源：
  79 行重复 (model|year|source) 键、79 组重复模型均带不同分数向量，保持辅助时序表不入
  面板。(7) 日期：11 个无日期行逐行验证为源数据缺失并列出。
- 产出: src/panel/detail.py · src/panel/c4_link.py · scripts/q4_panel_detail.py ·
  scripts/q4_panel_diagnose.py · scripts/q4_panel_build.py · scripts/q4_panel_selftest.py ·
  results/tables/q4-*.md(9 个) · reviews/T-009/HANDOVER.md
- 提交: 4e92001 · 8e141b3 · 2fb31be · bbef60f（+HANDOVER 提交）
- 用时: 4.0h
- AI: Claude Code · 校验: 28 条断言变异自测（新增 8 条：隔离 MATH 进不了主抽取、
  歧义 C4 键不链接、审计覆盖全部面板行、GPQA 池化等于固定公式且不等于等权子任务平均；
  其中一条在开发中真的失败过——测试自身调用签名写错——修正后通过）；selftest_interfaces
  21 断言 PASS；reconcile 保持 PARTIAL（按设计退出码 1，容差未放宽）；check_public_safe
  PreCommit PASS（75 文件）；git diff --check 干净；9 个结果文件全部由脚本再生、无手改数值。
- 状态: wip
- 备注: 推送仍被阻塞（gitclone.com 只读镜像 502/504，直连 github.com 被重置，SSH 被墙），
  分支上 5 个提交仅在本机，Draft PR 无法开启，需 M1 提供可写远端/代理。对 T-011 的硬
  约束：不得使用 C8 MATH 逐子任务记录；C4 关联只有 94/4497 且集中于 A 类，凡以 C4 字段
  做 scale/非scale 分解必须显式报告该子集；C3 不得作为面板行；训练算力字段仅 62 行非空。
